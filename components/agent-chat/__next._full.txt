1:"$Sreact.fragment"
2:I[172844,["/_next/static/chunks/0ekqw6yi_zy68.js","/_next/static/chunks/1l1ztpmvvr6un.js"],""]
3:I[229427,["/_next/static/chunks/0ekqw6yi_zy68.js","/_next/static/chunks/1l1ztpmvvr6un.js"],"CommandSearchProvider"]
a:I[684167,["/_next/static/chunks/0ekqw6yi_zy68.js","/_next/static/chunks/1l1ztpmvvr6un.js"],"default",1]
:HL["/_next/static/chunks/00d9be-48vpo2.css","style"]
:HL["/_next/static/chunks/2jq731gt7y9oq.css","style"]
:HL["/_next/static/media/234d9015b631dab9-s.p.33gca1im1ku45.woff2","font",{"crossOrigin":"","type":"font/woff2"}]
:HL["/_next/static/media/70bc3e132a0a741e-s.p.3t6q91iet4nsy.woff2","font",{"crossOrigin":"","type":"font/woff2"}]
8:X
0:{"P":null,"c":["","components","agent-chat",""],"q":"","i":false,"f":[[["",{"children":["components",{"children":[["slug","agent-chat","d",[]],{"children":["__PAGE__",{},"$undefined","$undefined",4608]},"$undefined","$undefined",4608]},"$undefined","$undefined",4608]},"$undefined","$undefined",4624],[["$","$1","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/_next/static/chunks/00d9be-48vpo2.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}],["$","link","1",{"rel":"stylesheet","href":"/_next/static/chunks/2jq731gt7y9oq.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}],["$","script","script-0",{"src":"/_next/static/chunks/0ekqw6yi_zy68.js","async":true,"nonce":"$undefined"}],["$","script","script-1",{"src":"/_next/static/chunks/1l1ztpmvvr6un.js","async":true,"nonce":"$undefined"}]],["$","html",null,{"lang":"en","suppressHydrationWarning":true,"className":"inter_9cfd2fd2-module__mkxXHa__variable jetbrains_mono_32f24d5e-module__zeawOW__variable h-full antialiased","children":["$","body",null,{"className":"min-h-full flex flex-col bg-background-full","children":[["$","$L2",null,{"id":"zeus-theme","strategy":"beforeInteractive","children":"(function(){try{var dark=localStorage.getItem(\"zeus:theme\")===\"dark\";document.documentElement.classList.toggle(\"dark\",dark)}catch(e){document.documentElement.classList.remove(\"dark\")}})();"}],["$","$L3",null,{"groups":[{"id":"pages","title":"Pages & guides","kind":"page","entries":[{"title":"Home","href":"/","description":"Curated components for your agent","keywords":["Zeus","collections","discovery"]},{"title":"All components","href":"/components/","description":"Browse the full component library","keywords":["catalog","library","search"]},{"title":"Documentation","href":"/foundations/","description":"Get started with Zeus","keywords":["docs","guide","foundations","help"]},{"title":"Installation","href":"/foundations/#installation","description":"Add Zeus to your project","keywords":["setup","install","SDK","npm","CLI"]},{"title":"Library organization","href":"/foundations/#library-model","description":"Families, design systems and foundations","keywords":["docs","documentation","getting started"]},{"title":"Design tokens","href":"/foundations/#tokens","description":"Colors, typography and themes","keywords":["foundations","CSS","styling"]},{"title":"MCP server","href":"/foundations/#mcp","description":"Connect your coding agent to Zeus","keywords":["agents","tools","AI","configuration"]},{"title":"Dashboard gallery","href":"/dashboards/","description":"Explore complete dashboard layouts","keywords":["examples","community","templates"]},{"title":"Animation playground","href":"/playground/","description":"Tune motion and copy working React examples","keywords":["animations","controls","preview"]},{"title":"Chat playground","href":"/chat/","description":"Explore the agent chat interface","keywords":["AI","streaming","composer","conversation"]}]},{"id":"components-buttons","title":"Buttons","kind":"component","entries":[{"title":"Animated Icon Button","href":"/components/animated-icon-button/","description":"A circular icon background fills the button while icons cycle diagonally","component":"animated-icon-button","keywords":["animated-icon-button","Buttons","animations","animation","Animations","independent","animation","annnimate","icon-button","hover"]},{"title":"Background Fill Button","href":"/components/background-fill-button/","description":"A rounded background rises from below as the button gently compresses","component":"background-fill-button","keywords":["background-fill-button","Buttons","animations","animation","Animations","independent","animation","annnimate","background-fill-button","hover"]},{"title":"Button","href":"/components/button/","description":"Primary, secondary, ghost and danger actions in three sizes, with icon slots.","component":"button","keywords":["button","Buttons","base","component","Components","Zeus","action","cta","form","gradient"]},{"title":"Button Group","href":"/components/button-group/","description":"Fused row of selectable buttons for view switches and toolbars.","component":"button-group","keywords":["button-group","Buttons","base","component","Components","Zeus","action","toggle","segmented","toolbar"]},{"title":"Circle Fill Button","href":"/components/circle-fill-button/","description":"A circle grows from the cursor entry point to fill every corner in the same duration","component":"circle-fill-button","keywords":["circle-fill-button","Buttons","animations","animation","Animations","independent","animation","annnimate","circle-fill-button","hover"]},{"title":"Close Button","href":"/components/close-button/","description":"The dismiss ×, in four sizes with matching stroke weights.","component":"close-button","keywords":["close-button","Buttons","base","component","Components","Zeus","action","dismiss","modal","icon"]},{"title":"Curve Fill Button","href":"/components/curve-fill-button/","description":"A curved wave enters from the nearest pointer edge and rolls back out","component":"curve-fill-button","keywords":["curve-fill-button","Buttons","animations","animation","Animations","independent","animation","annnimate","curve-fill-button","hover"]},{"title":"Icon Button","href":"/components/icon-button/","description":"Square action button, icon only, in two sizes.","component":"icon-button","keywords":["icon-button","Buttons","base","component","Components","Zeus","action","icon","toolbar","compact"]},{"title":"Link Button","href":"/components/link-button/","description":"Inline text action styled like a link, rendering an anchor when given an href.","component":"link-button","keywords":["link-button","Buttons","base","component","Components","Zeus","action","link","inline","navigation"]},{"title":"Magnetic Button","href":"/components/magnetic-button/","description":"The button and its label follow the pointer independently before springing home","component":"magnetic-button","keywords":["magnetic-button","Buttons","animations","animation","Animations","independent","animation","annnimate","magnetic-button","hover"]},{"title":"Rainbow Button","href":"/components/rainbow-button/","description":"A dark button with a flowing rainbow border and a glow that expands on hover","component":"rainbow-button","keywords":["rainbow-button","Buttons","animations","animation","Animations","independent","animation","annnimate","rainbow-button","hover"]},{"title":"Social Button","href":"/components/social-button/","description":"Twenty-four OAuth brands in colourful, black and outlined appearances.","component":"social-button","keywords":["social-button","Buttons","base","component","Components","Zeus","auth","oauth","sso","brand","sign-in"]},{"title":"Text Underline","href":"/components/text-underline/","description":"An underline draws from the left and erases toward the right on hover or focus","component":"text-underline","keywords":["text-underline","Buttons","animations","animation","Animations","independent","animation","annnimate","text-underline","hover"]}]},{"id":"components-media","title":"Images & media","kind":"component","entries":[{"title":"3D Card Flip","href":"/components/3d-card-flip/","description":"A perspective card with pointer tilt, moving glare and a shifting shadow","component":"3d-card-flip","keywords":["3d-card-flip","Images & media","animations","animation","Animations","independent","animation","annnimate","pointer","hover","3d"]},{"title":"Animated Grid","href":"/components/animated-grid/","description":"Image columns advance at different rates during scrolling, producing a multi-column parallax composition with selectable motion variations","component":"animated-grid","keywords":["animated-grid","Images & media","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"ASCII Overlay","href":"/components/ascii-overlay/","description":"Image-aware WebGL character overlays with scroll-driven density and a static media fallback","component":"ascii-overlay","keywords":["ascii-overlay","Images & media","animations","animation","Animations","independent","animation","gsap","annnimate","ascii-overlay"]},{"title":"Background Color","href":"/components/background-color/","description":"Foreground and background colors interpolate between authored scroll zones instead of switching abruptly at section boundaries","component":"background-color","keywords":["background-color","Images & media","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Card Fan","href":"/components/card-fan/","description":"Hovering a stacked group spreads images into an arc; targeting one card raises it and widens neighboring spacing before the stack closes again.","component":"card-fan","keywords":["card-fan","Images & media","animations","animation","Animations","independent","animation","annnimate","hover"]},{"title":"Carousel","href":"/components/carousel/","description":"Snap track with arrows, dots, configurable gap and alignment.","component":"carousel","keywords":["carousel","Images & media","base","component","Components","Zeus","media","slider","gallery","swipe"]},{"title":"Circular Slider","href":"/components/circular-slider/","description":"Cards travel along the visible upper arc of a large circle; dragging or arrows advances the sequence and the nearest card snaps into position.","component":"circular-slider","keywords":["circular-slider","Images & media","animations","animation","Animations","independent","animation","annnimate","click","drag"]},{"title":"Curtain Slider","href":"/components/curtain-slider/","description":"A full-bleed image carousel with staggered column wipes and rolling titles","component":"curtain-slider","keywords":["curtain-slider","Images & media","animations","animation","Animations","independent","animation","gsap","annnimate","curtain-slider"]},{"title":"Dither Cursor Trail","href":"/components/dither-cursor-trail/","description":"Pointer marks break into ordered dots as their trail fades away","component":"dither-cursor-trail","keywords":["dither-cursor-trail","Images & media","animations","animation","Animations","independent","animation","annnimate","shader","dither-cursor-trail","hover"]},{"title":"Dithering","href":"/components/dithering/","description":"Ordered dot rendering with a smooth lens that restores original image colors","component":"dithering","keywords":["dithering","Images & media","animations","animation","Animations","independent","animation","annnimate","shader","dithering","hover"]},{"title":"Element Reveal","href":"/components/element-reveal/","description":"Elements enter using selectable fade, translation, scale, clipping, blur, or rotation treatments, with optional group staggering","component":"element-reveal","keywords":["element-reveal","Images & media","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Expanding Strip","href":"/components/expanding-strip/","description":"Hovering a narrow image column widens that column, compresses its siblings, and reveals its caption inside the expanded area.","component":"expanding-strip","keywords":["expanding-strip","Images & media","animations","animation","Animations","independent","animation","annnimate","hover"]},{"title":"Flip Zone","href":"/components/flip-zone/","description":"One media element moves and resizes between successive layout frames as scroll advances, maintaining a continuous visual identity","component":"flip-zone","keywords":["flip-zone","Images & media","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Gooey Hover Reveal","href":"/components/gooey-hover-reveal/","description":"A decaying liquid mask reveals content under the moving pointer","component":"gooey-hover-reveal","keywords":["gooey-hover-reveal","Images & media","animations","animation","Animations","independent","animation","annnimate","shader","gooey-hover-reveal","hover"]},{"title":"Hoverable List","href":"/components/hoverable-list/","description":"Pointer entry fills each list row with a curved moving surface whose bend depends on whether the pointer approached from above or below.","component":"hoverable-list","keywords":["hoverable-list","Images & media","animations","animation","Animations","independent","animation","annnimate","hover"]},{"title":"Image Dissolve Scroll","href":"/components/image-dissolve-scroll/","description":"Scroll dissolves an image through irregular noise boundaries and a glowing edge","component":"image-dissolve-scroll","keywords":["image-dissolve-scroll","Images & media","animations","animation","Animations","independent","animation","annnimate","shader","image-dissolve-scroll","scroll"]},{"title":"Image Fly-In","href":"/components/image-fly-in/","description":"A continuous image depth field with cursor parallax and a clear central content zone","component":"image-fly-in","keywords":["image-fly-in","Images & media","animations","animation","Animations","independent","animation","gsap","annnimate","image-fly-in"]},{"title":"Image Follow List","href":"/components/image-follow-list/","description":"Hovering a project row displays its image near the pointer; the preview tracks movement and switches content across rows.","component":"image-follow-list","keywords":["image-follow-list","Images & media","animations","animation","Animations","independent","animation","annnimate","hover"]},{"title":"Image Glitch","href":"/components/image-glitch/","description":"Analog scan lines, separated color channels and digital block tearing on hover","component":"image-glitch","keywords":["image-glitch","Images & media","animations","animation","Animations","independent","animation","annnimate","shader","image-glitch","hover"]},{"title":"Image Trail","href":"/components/image-trail/","description":"Pointer movement emits overlapping images that lag, rotate, shrink and fade","component":"image-trail","keywords":["image-trail","Images & media","animations","animation","Animations","independent","animation","annnimate","pointer","hover","images"]},{"title":"Index Wipe","href":"/components/index-wipe/","description":"Hovering indexed chapter names changes the full background through a directional wipe and rolls the numeric index to the active item.","component":"index-wipe","keywords":["index-wipe","Images & media","animations","animation","Animations","independent","animation","annnimate","hover"]},{"title":"Infinite Draggable Grid","href":"/components/infinite-draggable-grid/","description":"A repeating horizontal set of visual items supports pointer dragging and inertial continuation with no visible end boundary.","component":"infinite-draggable-grid","keywords":["infinite-draggable-grid","Images & media","animations","animation","Animations","independent","animation","annnimate","drag"]},{"title":"Infinite Marquee","href":"/components/infinite-marquee/","description":"Repeated content travels horizontally or vertically in a seamless loop and can be dragged to scrub before automatic movement resumes.","component":"infinite-marquee","keywords":["infinite-marquee","Images & media","animations","animation","Animations","independent","animation","annnimate","drag","ambient"]},{"title":"Infinite Parallax Slider","href":"/components/infinite-parallax-slider/","description":"A looping horizontal carousel offsets internal layers at different rates, supports autonomous playback, and pauses when hovered.","component":"infinite-parallax-slider","keywords":["infinite-parallax-slider","Images & media","animations","animation","Animations","independent","animation","annnimate","drag","ambient"]},{"title":"Lens Rail","href":"/components/lens-rail/","description":"A draggable horizontal image rail enlarges cards near its center and compresses distant cards; arrow controls move the focal position.","component":"lens-rail","keywords":["lens-rail","Images & media","animations","animation","Animations","independent","animation","annnimate","drag","click"]},{"title":"Mask Reveal","href":"/components/mask-reveal/","description":"Scroll opens or closes a configurable shape mask over content, including circular, rectangular, organic, or supplied outlines","component":"mask-reveal","keywords":["mask-reveal","Images & media","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Mesh Gradient","href":"/components/mesh-gradient/","description":"Moving blended color fields with pointer displacement and a static fallback","component":"mesh-gradient","keywords":["mesh-gradient","Images & media","animations","animation","Animations","independent","animation","annnimate","shader","mesh-gradient","hover"]},{"title":"Multi Flip","href":"/components/multi-flip/","description":"An overlapping irregular card pile transitions into an ordered grid with scroll, preserving each card while changing its position and rotation","component":"multi-flip","keywords":["multi-flip","Images & media","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Parallax","href":"/components/parallax/","description":"Multiple elements respond to scroll with different displacement rates, creating relative depth between foreground and background layers","component":"parallax","keywords":["parallax","Images & media","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Photo Stack Gallery","href":"/components/photo-stack-gallery/","description":"Clicking a compact image stack opens a thumbnail strip and large feature image; choosing another thumbnail exchanges their positions.","component":"photo-stack-gallery","keywords":["photo-stack-gallery","Images & media","animations","animation","Animations","independent","animation","annnimate","click"]},{"title":"Radial Gallery","href":"/components/radial-gallery/","description":"Image cards occupy a slowly rotating circular arrangement sized to the viewport; the ring can appear whole or partially outside the frame.","component":"radial-gallery","keywords":["radial-gallery","Images & media","animations","animation","Animations","independent","animation","annnimate","hover","ambient"]},{"title":"Random Rotate","href":"/components/random-rotate/","description":"Items appear with independently varied rotational offsets, giving their final arrangement an irregular placed-by-hand character","component":"random-rotate","keywords":["random-rotate","Images & media","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Scale Slider","href":"/components/scale-slider/","description":"An exponential image conveyor with drag inertia, keyboard navigation and optional drift","component":"scale-slider","keywords":["scale-slider","Images & media","animations","animation","Animations","independent","animation","gsap","annnimate","scale-slider"]},{"title":"Scatter Cluster","href":"/components/scatter-cluster/","description":"A central overlapping pile of image and statistic cards separates into a surrounding arrangement with scroll progress, revealing the headline beneath","component":"scatter-cluster","keywords":["scatter-cluster","Images & media","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Spiral Gallery","href":"/components/spiral-gallery/","description":"Image planes orbit through a vertical helix; dragging changes rotation velocity, release preserves momentum, and the composition continues drifting.","component":"spiral-gallery","keywords":["spiral-gallery","Images & media","animations","animation","Animations","independent","animation","annnimate","drag","ambient"]},{"title":"SVG Draw Path","href":"/components/svg-draw-path/","description":"Scroll progress reveals an SVG stroke from its beginning to its end, following the supplied path geometry","component":"svg-draw-path","keywords":["svg-draw-path","Images & media","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Velocity Clip","href":"/components/velocity-clip/","description":"The visible media region contracts and releases according to scrolling speed, with stronger clipping during faster movement","component":"velocity-clip","keywords":["velocity-clip","Images & media","animations","animation","Animations","independent","animation","annnimate","scroll"]}]},{"id":"components-layout","title":"Layout & structure","kind":"component","entries":[{"title":"App Shell","href":"/components/app-shell/","description":"The page frame: sidebar, mobile drawer, breadcrumb header and content column.","component":"app-shell","keywords":["app-shell","Layout & structure","blocks","block","Blocks","Zeus","layout","shell","page","responsive"]},{"title":"Capsule Hero","href":"/components/capsule-hero/","description":"Large letterforms part around a rounded media aperture that expands on activation and reverses on close","component":"capsule-hero","keywords":["capsule-hero","Layout & structure","animations","animation","Animations","independent","animation","annnimate","click"]},{"title":"Divider","href":"/components/divider/","description":"Single, double and filled separators that can carry any content in the middle.","component":"divider","keywords":["divider","Layout & structure","base","component","Components","Zeus","layout","separator","rule","section"]},{"title":"Dock Footer","href":"/components/dock-footer/","description":"Scroll uncovers a staggered footer and a translucent navigation dock that expands at the page bottom","component":"dock-footer","keywords":["dock-footer","Layout & structure","animations","animation","Animations","independent","animation","annnimate","scroll","click"]},{"title":"Drift Gallery","href":"/components/drift-gallery/","description":"Product cards separate on grab and travel with release inertia that controls grain and color intensity","component":"drift-gallery","keywords":["drift-gallery","Layout & structure","animations","animation","Animations","independent","animation","annnimate","drag"]},{"title":"Flip Filter","href":"/components/flip-filter/","description":"Category and grid/list changes preserve product cards through measured, interruptible FLIP layout transitions","component":"flip-filter","keywords":["flip-filter","Layout & structure","animations","animation","Animations","independent","animation","annnimate","click"]},{"title":"Gradient Footer","href":"/components/gradient-footer/","description":"A scroll-revealed footer combines a bottom glow, width-filling brand typography and animated link underlines","component":"gradient-footer","keywords":["gradient-footer","Layout & structure","animations","animation","Animations","independent","animation","annnimate","scroll","hover"]},{"title":"Grid Chapters","href":"/components/grid-chapters/","description":"Pinned chapter headings exchange masked lines while image columns pass independently through a visible grid","component":"grid-chapters","keywords":["grid-chapters","Layout & structure","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Network Globe","href":"/components/network-globe/","description":"A draggable dotted sphere rotates toward selected locations and draws raised routes between them","component":"network-globe","keywords":["network-globe","Layout & structure","animations","animation","Animations","independent","animation","annnimate","drag","click","ambient"]},{"title":"Step Wipe","href":"/components/step-wipe/","description":"A pinned product sequence synchronizes separate background, foreground, caption and numbering wipes with scroll","component":"step-wipe","keywords":["step-wipe","Layout & structure","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Testimonial Globe","href":"/components/testimonial-globe/","description":"Portraits rotate on a draggable sphere beneath translucent quotation cards with a lighter mobile presentation","component":"testimonial-globe","keywords":["testimonial-globe","Layout & structure","animations","animation","Animations","independent","animation","annnimate","drag","ambient","click"]},{"title":"Wipe Slider","href":"/components/wipe-slider/","description":"Directional background wipes coordinate masked slide text and a rolling percentage index","component":"wipe-slider","keywords":["wipe-slider","Layout & structure","animations","animation","Animations","independent","animation","annnimate","click"]}]},{"id":"components-navigation","title":"Navigation","kind":"component","entries":[{"title":"Breadcrumb","href":"/components/breadcrumb/","description":"Path trail whose items take arbitrary leading content — avatars, icons, dots.","component":"breadcrumb","keywords":["breadcrumb","Navigation","base","component","Components","Zeus","navigation","path","hierarchy","header"]},{"title":"Custom Cursor","href":"/components/custom-cursor/","description":"A smoothly following cursor with contextual labels and native touch input","component":"custom-cursor","keywords":["custom-cursor","Navigation","animations","animation","Animations","independent","animation","annnimate","pointer","hover","cursor"]},{"title":"Dropdown","href":"/components/dropdown/","description":"Composable menu primitives — trigger, popover, groups, items and dividers.","component":"dropdown","keywords":["dropdown","Navigation","base","component","Components","Zeus","menu","popover","overlay","actions"]},{"title":"Fullscreen Slide Menu","href":"/components/fullscreen-slide-menu/","description":"Navigation turns the page away and reveals a full-screen menu with changing imagery","component":"fullscreen-slide-menu","keywords":["fullscreen-slide-menu","Navigation","animations","animation","Animations","independent","animation","annnimate","menu","click","fullscreen"]},{"title":"Gooey Menu","href":"/components/gooey-menu/","description":"Floating actions separate through liquid connections in arc, radial, row or grid arrangements","component":"gooey-menu","keywords":["gooey-menu","Navigation","animations","animation","Animations","independent","animation","annnimate","menu","click","gooey"]},{"title":"Hide Header","href":"/components/hide-header/","description":"The navigation hides during downward movement and returns when direction reverses, with thresholds and zones that keep it visible","component":"hide-header","keywords":["hide-header","Navigation","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Mega Menu","href":"/components/mega-menu/","description":"A shared dropdown resizes between navigation groups with mobile accordion navigation","component":"mega-menu","keywords":["mega-menu","Navigation","animations","animation","Animations","independent","animation","annnimate","menu","hover","dropdown"]},{"title":"Morphing Island","href":"/components/morphing-island/","description":"A section capsule expands through navigation and summaries before scrolling to a destination","component":"morphing-island","keywords":["morphing-island","Navigation","animations","animation","Animations","independent","animation","annnimate","morphing-island","click"]},{"title":"Morphing Pill Menu","href":"/components/morphing-pill-menu/","description":"A compact pill expands into navigation with staggered and rolling links","component":"morphing-pill-menu","keywords":["morphing-pill-menu","Navigation","animations","animation","Animations","independent","animation","annnimate","menu","click","morphing"]},{"title":"Morphing Tabs","href":"/components/morphing-tabs/","description":"A measured curved bridge follows the selected tab while its content changes","component":"morphing-tabs","keywords":["morphing-tabs","Navigation","animations","animation","Animations","independent","animation","annnimate","morphing-tabs","click"]},{"title":"Multi-Level Drawer Menu","href":"/components/multi-level-drawer-menu/","description":"Two-panel navigation with animated category previews and a mobile drill-down","component":"multi-level-drawer-menu","keywords":["multi-level-drawer-menu","Navigation","animations","animation","Animations","independent","animation","gsap","annnimate","multi-level-drawer-menu"]},{"title":"Pagination","href":"/components/pagination/","description":"Page numbers with truncation and prev/next controls.","component":"pagination","keywords":["pagination","Navigation","base","component","Components","Zeus","navigation","pages","table","list"]},{"title":"Segmented Control","href":"/components/segmented-control/","description":"Sliding pill option switcher for two to four short choices.","component":"segmented-control","keywords":["segmented-control","Navigation","base","component","Components","Zeus","navigation","toggle","filter","period"]},{"title":"Sidebar","href":"/components/sidebar/","description":"The 260px dashboard sidebar with collapsing nav, team switcher and user menu.","component":"sidebar","keywords":["sidebar","Navigation","blocks","block","Blocks","Zeus","navigation","shell","sidebar","menu"]},{"title":"Tabs","href":"/components/tabs/","description":"Underlined tabs with optional icons and counts, plus a pill variant.","component":"tabs","keywords":["tabs","Navigation","base","component","Components","Zeus","navigation","tabs","panels","sections"]}]},{"id":"components-data","title":"Data & charts","kind":"component","entries":[{"title":"Activity Rings","href":"/components/activity-rings-card/","description":"Concentric progress arcs, one per goal, drawn as plain SVG.","component":"activity-rings-card","keywords":["activity-rings-card","Data & charts","charts","chart","Charts","Zeus","chart","radial","progress","goals","health","svg"]},{"title":"Area Chart","href":"/components/area-chart-card/","description":"One or more series as soft filled bands, stacked into a total by default.","component":"area-chart-card","keywords":["area-chart-card","Data & charts","charts","chart","Charts","Zeus","chart","area","stacked","recharts","trend"]},{"title":"Bar List","href":"/components/bar-list-card/","description":"A ranked list where each row's bar is its share of the top — no axis, no library.","component":"bar-list-card","keywords":["bar-list-card","Data & charts","charts","chart","Charts","Zeus","chart","bar","ranking","top-n","list"]},{"title":"Chart Card","href":"/components/chart-card/","description":"The shared frame every chart card is built on: header, count-up headline, delta chip and legend.","component":"chart-card","keywords":["chart-card","Data & charts","charts","chart","Charts","Zeus","chart","shell","layout","primitive"]},{"title":"Combo Chart","href":"/components/combo-chart-card/","description":"independent left and right axes, hover dimming, a pulsing active dot and stat tiles","component":"combo-chart-card","keywords":["combo-chart-card","Data & charts","charts","chart","Charts","Zeus","chart","data","visualization"]},{"title":"Contributions","href":"/components/contributions-card/","description":"GitHub-style contributions heat grid for React + Tailwind CSS with a swappable accent family","component":"contributions-card","keywords":["contributions-card","Data & charts","charts","chart","Charts","Zeus","chart","data","visualization"]},{"title":"Counter","href":"/components/counter/","description":"Numbers animate to target values on entry, using either smooth counting or independently rolling digit columns with optional units","component":"counter","keywords":["counter","Data & charts","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Data Table","href":"/components/data-table/","description":"TanStack Table with sorting, selection, filters, search, density and pagination.","component":"data-table","keywords":["data-table","Data & charts","blocks","block","Blocks","Zeus","data","table","tanstack","sorting","filtering","pagination"]},{"title":"Earnings Chart","href":"/components/earnings-chart-card/","description":"Bar chart card for React + Tailwind CSS with a period switcher, count-up headline, and hover outline","component":"earnings-chart-card","keywords":["earnings-chart-card","Data & charts","charts","chart","Charts","Zeus","chart","data","visualization"]},{"title":"Funnel Chart","href":"/components/funnel-chart-card/","description":"curved or sharp tapers between stages, centred conversion pills, mono option, and a value/name footer","component":"funnel-chart-card","keywords":["funnel-chart-card","Data & charts","charts","chart","Charts","Zeus","chart","data","visualization"]},{"title":"Heatmap Chart","href":"/components/heatmap-chart-card/","description":"rows × columns of tinted cells with a theme-following ramp, hover-linked headline, and Less → More legend","component":"heatmap-chart-card","keywords":["heatmap-chart-card","Data & charts","charts","chart","Charts","Zeus","chart","data","visualization"]},{"title":"Important Alerts","href":"/components/important-alerts-card/","description":"A dated alert feed with severity icons and a week range pill.","component":"important-alerts-card","keywords":["important-alerts-card","Data & charts","charts","chart","Charts","Zeus","health","alerts","feed","timeline"]},{"title":"Line Chart","href":"/components/line-chart-card/","description":"Several series compared over one axis, smooth or sharp, with a dashed comparison.","component":"line-chart-card","keywords":["line-chart-card","Data & charts","charts","chart","Charts","Zeus","chart","line","series","recharts","comparison"]},{"title":"Most Active Days","href":"/components/most-active-days-card/","description":"Continuous vertical month calendar for React + Tailwind CSS with per-day mini activity rings","component":"most-active-days-card","keywords":["most-active-days-card","Data & charts","charts","chart","Charts","Zeus","chart","data","visualization"]},{"title":"Orders Chart","href":"/components/orders-chart-card/","description":"Year-over-year bars with the same headline and delta treatment.","component":"orders-chart-card","keywords":["orders-chart-card","Data & charts","charts","chart","Charts","Zeus","chart","bar","orders","recharts","yoy"]},{"title":"Patient Info","href":"/components/patient-info-card/","description":"A profile card with identity fields and vitals.","component":"patient-info-card","keywords":["patient-info-card","Data & charts","charts","chart","Charts","Zeus","health","profile","vitals","card"]},{"title":"Progress","href":"/components/progress/","description":"Page progress is represented by a ring, linear fill, or numeric percentage that follows the document's scroll position","component":"progress","keywords":["progress","Data & charts","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Radar Chart","href":"/components/radar-chart-card/","description":"filled, dotted, lines-only, and centre-score variants with a hover-linked headline","component":"radar-chart-card","keywords":["radar-chart-card","Data & charts","charts","chart","Charts","Zeus","chart","data","visualization"]},{"title":"Radial Chart","href":"/components/radial-chart-card/","description":"concentric rings, labelled rings, grid, single-value gauges, and a stacked half gauge","component":"radial-chart-card","keywords":["radial-chart-card","Data & charts","charts","chart","Charts","Zeus","chart","data","visualization"]},{"title":"Revenue Chart","href":"/components/revenue-chart-card/","description":"Year-over-year area chart with a computed headline and delta.","component":"revenue-chart-card","keywords":["revenue-chart-card","Data & charts","charts","chart","Charts","Zeus","chart","area","revenue","recharts","yoy"]},{"title":"Sankey Chart","href":"/components/sankey-chart-card/","description":"pill nodes, target-tinted links, source and share labels, and hover isolation","component":"sankey-chart-card","keywords":["sankey-chart-card","Data & charts","charts","chart","Charts","Zeus","chart","data","visualization"]},{"title":"Scatter Chart","href":"/components/scatter-chart-card/","description":"grouped series, optional size measure, hover isolation and stat tiles","component":"scatter-chart-card","keywords":["scatter-chart-card","Data & charts","charts","chart","Charts","Zeus","chart","data","visualization"]},{"title":"Sleep Score","href":"/components/sleep-score-card/","description":"Segmented sleep score ring for React + Tailwind CSS with hover-focused sub-scores and metric rows","component":"sleep-score-card","keywords":["sleep-score-card","Data & charts","charts","chart","Charts","Zeus","chart","data","visualization"]},{"title":"Stage Bars","href":"/components/stage-bars-card/","description":"name, value and share per stage, animated widths, mono option","component":"stage-bars-card","keywords":["stage-bars-card","Data & charts","charts","chart","Charts","Zeus","chart","data","visualization"]},{"title":"Stat Cards","href":"/components/stat-cards/","description":"KPI row with gradient icon tiles, info tooltips and delta pills, in two variants.","component":"stat-cards","keywords":["stat-cards","Data & charts","blocks","block","Blocks","Zeus","dashboard","kpi","metrics","cards"]},{"title":"Steps Chart","href":"/components/steps-card/","description":"Weekly steps bar chart for React + Tailwind CSS with a week switcher, count-up headline, and hover outline","component":"steps-card","keywords":["steps-card","Data & charts","charts","chart","Charts","Zeus","chart","data","visualization"]},{"title":"Table","href":"/components/table/","description":"The semantic table primitive: sortable columns, row selection, two densities.","component":"table","keywords":["table","Data & charts","base","component","Components","Zeus","data","table","grid","rows"]}]},{"id":"components-forms","title":"Forms & inputs","kind":"component","entries":[{"title":"Auth Card","href":"/components/auth-card/","description":"Sign in, sign up and verify, with stacked, inline and grid provider layouts.","component":"auth-card","keywords":["auth-card","Forms & inputs","blocks","block","Blocks","Zeus","auth","sign-in","sign-up","oauth","otp","form"]},{"title":"Checkbox","href":"/components/checkbox/","description":"Hand-drawn tick animation, indeterminate state, two sizes and a card variant.","component":"checkbox","keywords":["checkbox","Forms & inputs","base","component","Components","Zeus","form","selection","toggle","multi-select"]},{"title":"File Upload","href":"/components/file-upload/","description":"Drag and drop with extension and size validation, and per-file progress.","component":"file-upload","keywords":["file-upload","Forms & inputs","base","component","Components","Zeus","form","upload","drag-drop","files","progress"]},{"title":"Gooey Filter Chips","href":"/components/gooey-filter-chips/","description":"Filter chips move into a shared liquid result tray with rolling counts","component":"gooey-filter-chips","keywords":["gooey-filter-chips","Forms & inputs","animations","animation","Animations","independent","animation","annnimate","gooey-filter-chips","click"]},{"title":"Input","href":"/components/input/","description":"Text field with label, hint, addons, icons and validation states.","component":"input","keywords":["input","Forms & inputs","base","component","Components","Zeus","form","text","field","validation"]},{"title":"Input OTP","href":"/components/input-otp/","description":"One-time-code boxes with paste, autofill and grouping.","component":"input-otp","keywords":["input-otp","Forms & inputs","base","component","Components","Zeus","form","otp","verification","2fa","code"]},{"title":"Morphing Select","href":"/components/morphing-select/","description":"A liquid option panel separates from a select trigger with a clipped rolling label","component":"morphing-select","keywords":["morphing-select","Forms & inputs","animations","animation","Animations","independent","animation","annnimate","morphing-select","click"]},{"title":"Multi-Step Form","href":"/components/multi-step-form/","description":"Directional form steps, floating labels, validation and cancellable asynchronous submission","component":"multi-step-form","keywords":["multi-step-form","Forms & inputs","animations","animation","Animations","independent","animation","annnimate","multi-step-form","click"]},{"title":"Radio","href":"/components/radio/","description":"Gradient dot, two sizes, and a card variant for plan pickers.","component":"radio","keywords":["radio","Forms & inputs","base","component","Components","Zeus","form","selection","single-select","group"]},{"title":"Select","href":"/components/select/","description":"Single-choice listbox whose items take free-form content — dots, avatars, icons.","component":"select","keywords":["select","Forms & inputs","base","component","Components","Zeus","form","dropdown","listbox","picker"]},{"title":"Slider","href":"/components/slider/","description":"Single value and range, with a value bubble and custom formatting.","component":"slider","keywords":["slider","Forms & inputs","base","component","Components","Zeus","form","range","numeric","filter"]},{"title":"Switch","href":"/components/switch/","description":"Pill and rectangle shapes, two sizes, plus a card variant with icon and description.","component":"switch","keywords":["switch","Forms & inputs","base","component","Components","Zeus","form","toggle","settings","boolean"]}]},{"id":"components-overlays","title":"Overlays & disclosures","kind":"component","entries":[{"title":"Animated Accordion","href":"/components/animated-accordion/","description":"Coordinated expandable panels with rotating icons and optional line-by-line text reveals","component":"animated-accordion","keywords":["animated-accordion","Overlays & disclosures","animations","animation","Animations","independent","animation","gsap","annnimate","animated-accordion"]},{"title":"Animated Tooltip","href":"/components/animated-tooltip/","description":"Delayed anchored or pointer-following captions flip at viewport edges","component":"animated-tooltip","keywords":["animated-tooltip","Overlays & disclosures","animations","animation","Animations","independent","animation","annnimate","animated-tooltip","hover"]},{"title":"Detail Drawer","href":"/components/detail-drawer/","description":"A hero image shifts and darkens as a side detail panel reveals staggered text","component":"detail-drawer","keywords":["detail-drawer","Overlays & disclosures","animations","animation","Animations","independent","animation","annnimate","detail-drawer","click"]},{"title":"Feature Dialog","href":"/components/feature-dialog/","description":"An expandable preview tile with a rising modal, gradient band and managed keyboard focus","component":"feature-dialog","keywords":["feature-dialog","Overlays & disclosures","animations","animation","Animations","independent","animation","gsap","annnimate","feature-dialog"]},{"title":"Morphing Accordion","href":"/components/morphing-accordion/","description":"Question pills and expanding answers briefly merge through a decorative liquid layer","component":"morphing-accordion","keywords":["morphing-accordion","Overlays & disclosures","animations","animation","Animations","independent","animation","annnimate","morphing-accordion","click"]},{"title":"Morphing Dialog","href":"/components/morphing-dialog/","description":"A product card expands into a native dialog with shared image and text transitions","component":"morphing-dialog","keywords":["morphing-dialog","Overlays & disclosures","animations","animation","Animations","independent","animation","annnimate","morphing-dialog","click"]},{"title":"Morphing Tooltip","href":"/components/morphing-tooltip/","description":"A shared liquid tooltip that rises from toolbar buttons, travels between them and resizes to each caption","component":"morphing-tooltip","keywords":["morphing-tooltip","Overlays & disclosures","animations","animation","Animations","independent","animation","gsap","annnimate","morphing-tooltip"]},{"title":"Settings Modal","href":"/components/settings-modal/","description":"Five-page settings dialog: profile, general, tools, storage and plan.","component":"settings-modal","keywords":["settings-modal","Overlays & disclosures","blocks","block","Blocks","Zeus","settings","modal","dialog","preferences","account"]},{"title":"Tooltip","href":"/components/tooltip/","description":"Hover and focus tooltip with the arrow and placement handled for you.","component":"tooltip","keywords":["tooltip","Overlays & disclosures","base","component","Components","Zeus","overlay","hint","hover","accessibility"]}]},{"id":"components-typography","title":"Text & typography","kind":"component","entries":[{"title":"Character Appear","href":"/components/character-appear/","description":"Scroll reveals individual glyphs by opacity, either sequentially or in a randomized order, without translating their positions","component":"character-appear","keywords":["character-appear","Text & typography","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Cinematic Text","href":"/components/cinematic-text/","description":"Headlines move into view from opposing horizontal directions, split into lines, words, or characters for staggered timing","component":"cinematic-text","keywords":["cinematic-text","Text & typography","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Dual Scramble","href":"/components/dual-scramble/","description":"Two-stage colored text scrambling on scroll, hover, focus, click or load","component":"dual-scramble","keywords":["dual-scramble","Text & typography","animations","animation","Animations","independent","animation","gsap","annnimate","dual-scramble"]},{"title":"Folding Text","href":"/components/folding-text/","description":"Individual glyphs rotate through perspective as though hinged, unfolding into legible text with scroll progress","component":"folding-text","keywords":["folding-text","Text & typography","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Kbd","href":"/components/kbd/","description":"Keyboard key caps for shortcut hints.","component":"kbd","keywords":["kbd","Text & typography","base","component","Components","Zeus","indicator","keyboard","shortcut","hint"]},{"title":"Popping Text","href":"/components/popping-text/","description":"Individual letters scale into view with overshoot and varied rotation, producing a staggered bouncing headline entrance","component":"popping-text","keywords":["popping-text","Text & typography","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Rectangle Text Reveal","href":"/components/rectangle-text-reveal/","description":"Rounded masks withdraw from successive text lines while each line moves into its readable resting position","component":"rectangle-text-reveal","keywords":["rectangle-text-reveal","Text & typography","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Text Fill Wave","href":"/components/text-fill-wave/","description":"A headline gains its foreground color progressively while a brighter accent band travels along the advancing edge across successive lines","component":"text-fill-wave","keywords":["text-fill-wave","Text & typography","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Text Morph","href":"/components/text-morph/","description":"Editing an input repositions matching characters and transitions changed text; rapid input favors responsiveness over completing every animation","component":"text-morph","keywords":["text-morph","Text & typography","animations","animation","Animations","independent","animation","annnimate","click","input"]},{"title":"Text On Path","href":"/components/text-on-path/","description":"Scrolling progressively places words along an undulating invisible path, allowing the sentence itself to reveal the curve","component":"text-on-path","keywords":["text-on-path","Text & typography","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Text Reveal","href":"/components/text-reveal/","description":"Scroll-triggered line, word and character reveals with masks and readable static content","component":"text-reveal","keywords":["text-reveal","Text & typography","animations","animation","Animations","independent","animation","gsap","annnimate","text-reveal"]},{"title":"Text Scramble","href":"/components/text-scramble/","description":"A text trigger temporarily displays changing random glyphs before resolving to the intended words, with configurable character pool and pacing","component":"text-scramble","keywords":["text-scramble","Text & typography","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Text Shimmer Wave","href":"/components/text-shimmer-wave/","description":"A light-like highlight travels over successive glyphs, either as a continuous loop or a scroll-triggered pass","component":"text-shimmer-wave","keywords":["text-shimmer-wave","Text & typography","animations","animation","Animations","independent","animation","annnimate","ambient"]},{"title":"Text Split Zoom","href":"/components/text-split-zoom/","description":"A growing image separates two portions of a headline, expanding from their shared center as the entrance or scroll animation progresses","component":"text-split-zoom","keywords":["text-split-zoom","Text & typography","animations","animation","Animations","independent","animation","annnimate","scroll"]},{"title":"Typewriter","href":"/components/typewriter/","description":"Phrases type character by character, pause, erase, and cycle with separately configurable typing and deletion speeds","component":"typewriter","keywords":["typewriter","Text & typography","animations","animation","Animations","independent","animation","annnimate","ambient"]},{"title":"Typography","href":"/components/typography/","description":"The type scale: display, title, headline, body and caption, each in four weights.","component":"typography","keywords":["typography","Text & typography","foundations","typography","Typography","Zeus","type-scale","font","text","hierarchy"]},{"title":"Velocity Type","href":"/components/velocity-type/","description":"Scroll speed and direction alter headline weight, width, slant, and lean; the type settles back when scrolling slows","component":"velocity-type","keywords":["velocity-type","Text & typography","animations","animation","Animations","independent","animation","annnimate","scroll"]}]},{"id":"components-pricing","title":"Pricing & billing","kind":"component","entries":[{"title":"Animated Pricing","href":"/components/animated-pricing/","description":"Pricing cards with digit-by-digit billing transitions, a savings chip and shimmering names","component":"animated-pricing","keywords":["animated-pricing","Pricing & billing","animations","animation","Animations","independent","animation","gsap","annnimate","animated-pricing"]}]},{"id":"components-screens","title":"Complete screens","kind":"component","entries":[{"title":"Finance Dashboard","href":"/components/finance-dashboard/","description":"Personal finance workspace with cash flow, investments, spending and transaction records","component":"finance-dashboard","keywords":["finance-dashboard","Complete screens","templates","template","Templates","Zeus","dashboard","finance","transactions"]},{"title":"Home Dashboard","href":"/components/home-dashboard/","description":"An overview workspace with revenue, earnings, recent hires and searchable records","component":"home-dashboard","keywords":["home-dashboard","Complete screens","templates","template","Templates","Zeus","dashboard","analytics","overview"]},{"title":"HR Management","href":"/components/hr-management/","description":"Hiring, engagement and employee management with editable records","component":"hr-management","keywords":["hr-management","Complete screens","templates","template","Templates","Zeus","dashboard","hr","employees"]},{"title":"Marketing Dashboard","href":"/components/marketing-dashboard/","description":"Campaign analytics with conversion funnels, channel attribution and campaign management","component":"marketing-dashboard","keywords":["marketing-dashboard","Complete screens","templates","template","Templates","Zeus","dashboard","marketing","campaigns"]},{"title":"Medical Profile","href":"/components/medical-profile/","description":"A patient workspace with health activity, sleep, alerts and patient records","component":"medical-profile","keywords":["medical-profile","Complete screens","templates","template","Templates","Zeus","dashboard","health","profile"]}]},{"id":"components-ai","title":"AI & agents","kind":"component","entries":[{"title":"Agent Chat","href":"/components/agent-chat/","description":"A working chat app: thread history, streaming messages, composer and suggestions.","component":"agent-chat","keywords":["agent-chat","AI & agents","blocks","block","Blocks","Zeus","ai","chat","agent","streaming","ai-sdk","composer"]},{"title":"Agent Limits","href":"/components/agent-limits-card/","description":"Context window usage as a segmented bar with an expandable token breakdown, plus plan quotas and their reset times.","component":"agent-limits-card","keywords":["agent-limits-card","AI & agents","blocks","block","Blocks","Zeus","ai","agent","usage","quota","tokens","context-window","progress"]},{"title":"Agent Log","href":"/components/agent-log/","description":"The primitives behind a streaming tool log: reveal masks, guides and shimmer text.","component":"agent-log","keywords":["agent-log","AI & agents","blocks","block","Blocks","Zeus","ai","agent","log","streaming","motion"]},{"title":"Agent Progress","href":"/components/agent-progress/","description":"Collapsible AI agent progress block for React and Tailwind CSS with animated task states and completion timing","component":"agent-progress","keywords":["agent-progress","AI & agents","blocks","block","Blocks","Zeus","interaction","ai","agent-progress"]},{"title":"Agent Thinking","href":"/components/agent-thinking/","description":"Four loading personalities — wave, spin, stars and infinity — with an elapsed timer.","component":"agent-thinking","keywords":["agent-thinking","AI & agents","blocks","block","Blocks","Zeus","ai","agent","loading","streaming","chat"]},{"title":"AI Chat","href":"/components/ai-chat/","description":"A coding agent workspace with progress, task playback, a code panel and a cancellable backend adapter","component":"ai-chat","keywords":["ai-chat","AI & agents","templates","template","Templates","Zeus","ai","chat","agent"]},{"title":"AI Image Generation","href":"/components/ai-image-generation/","description":"An image workspace with animated generation, a gallery, styles and a cancellable generation adapter","component":"ai-image-generation","keywords":["ai-image-generation","AI & agents","templates","template","Templates","Zeus","ai","image","generation"]},{"title":"AI Profile","href":"/components/ai-profile/","description":"An editable agent profile with contribution history, agent activity and token usage","component":"ai-profile","keywords":["ai-profile","AI & agents","templates","template","Templates","Zeus","ai","profile","contributions"]},{"title":"Composer","href":"/components/composer/","description":"The AI chat composer for React + Tailwind CSS: attachment and model menus, voice and send controls, status bar, and a liquid-glass working state","component":"composer","keywords":["composer","AI & agents","blocks","block","Blocks","Zeus","interaction","ai","composer"]},{"title":"Composer Loader","href":"/components/composer-loader/","description":"A travelling light that rims the composer while an agent is answering.","component":"composer-loader","keywords":["composer-loader","AI & agents","blocks","block","Blocks","Zeus","ai","agent","loading","composer","svg"]},{"title":"Task List","href":"/components/task-list/","description":"Streaming agent task log for React and Tailwind CSS. Tasks reveal step by step with soft height, blur, and shimmer, and drop straight into a chat thread.","component":"task-list","keywords":["task-list","AI & agents","blocks","block","Blocks","Zeus","interaction","ai","task-list"]},{"title":"Web Search","href":"/components/web-search/","description":"Streaming research trail for React and Tailwind CSS: the queries an agent ran and the sources it opened, with real site marks, drawn in as it works.","component":"web-search","keywords":["web-search","AI & agents","blocks","block","Blocks","Zeus","interaction","ai","web-search"]}]},{"id":"components-scheduling","title":"Dates & scheduling","kind":"component","entries":[{"title":"Calendar","href":"/components/calendar/","description":"event chips, details popover, month switcher, and inbox feed","component":"calendar","keywords":["calendar","Dates & scheduling","blocks","block","Blocks","Zeus","interaction","calendar","events"]},{"title":"Date Picker","href":"/components/date-picker/","description":"Popover calendar for a single date, built on @internationalized/date.","component":"date-picker","keywords":["date-picker","Dates & scheduling","base","component","Components","Zeus","form","date","calendar","picker"]},{"title":"Date Range Picker","href":"/components/date-range-picker/","description":"Two-month range picker with quick presets and an apply step.","component":"date-range-picker","keywords":["date-range-picker","Dates & scheduling","base","component","Components","Zeus","form","date","range","presets","report"]},{"title":"Meeting Scheduler","href":"/components/meeting-scheduler/","description":"A booking panel: host, duration, timezone, date and time slots.","component":"meeting-scheduler","keywords":["meeting-scheduler","Dates & scheduling","base","component","Components","Zeus","form","scheduling","booking","calendar","timezone"]}]},{"id":"components-foundations","title":"Design foundations","kind":"component","entries":[{"title":"Design tokens","href":"/components/tokens/","description":"Color primitives, semantic light/dark tokens, radii, shadows and the button gradients everything else is built on.","component":"tokens","keywords":["tokens","Design foundations","foundations","tokens","Token sets","Zeus","theme","color","dark-mode","css-variables","tailwind"]}]},{"id":"components-utilities","title":"Hooks & utilities","kind":"component","entries":[{"title":"cx","href":"/components/cx/","description":"Class merging built on tailwind-merge, plus the sortCx helper for style maps.","component":"cx","keywords":["cx","Hooks & utilities","foundations","utility","Hooks & utilities","Zeus","utility","classnames","tailwind-merge","server-safe"]},{"title":"useCountUp","href":"/components/use-count-up/","description":"Animates a number toward a target, respecting prefers-reduced-motion.","component":"use-count-up","keywords":["use-count-up","Hooks & utilities","foundations","utility","Hooks & utilities","Zeus","hook","animation","number","counter"]},{"title":"useDismissOnOutsidePress","href":"/components/use-dismiss-on-outside-press/","description":"Closes a popover or panel on an outside pointer press, without trapping focus.","component":"use-dismiss-on-outside-press","keywords":["use-dismiss-on-outside-press","Hooks & utilities","foundations","utility","Hooks & utilities","Zeus","hook","popover","dismiss","outside-click"]}]},{"id":"components-icons","title":"Icons & identity","kind":"component","entries":[{"title":"Avatar","href":"/components/avatar/","description":"Photo with initials fallback, four sizes, colour tints and presence.","component":"avatar","keywords":["avatar","Icons & identity","base","component","Components","Zeus","identity","user","image","presence"]},{"title":"Chevrons","href":"/components/chevrons/","description":"The small chevron set used by menus, selects and sortable table headers.","component":"chevrons","keywords":["chevrons","Icons & identity","foundations","icons","Icon resources","Zeus","icon","chevron","svg"]},{"title":"Logo","href":"/components/logo/","description":"The wordmark and its compact circular variant, both theme-aware.","component":"logo","keywords":["logo","Icons & identity","foundations","component","Components","Zeus","brand","logo","mark"]}]},{"id":"components-feedback","title":"Feedback & status","kind":"component","entries":[{"title":"Announcement","href":"/components/announcement/","description":"Dismissible banner for product news, with an optional action.","component":"announcement","keywords":["announcement","Feedback & status","base","component","Components","Zeus","feedback","banner","news","dismissible"]},{"title":"Badge","href":"/components/badge/","description":"Count pills for nav rows and menus, in neutral and primary.","component":"badge","keywords":["badge","Feedback & status","base","component","Components","Zeus","indicator","count","pill","nav"]},{"title":"Chip","href":"/components/chip/","description":"Bold, subtle and caption tones across nine colours, for deltas, values and tags.","component":"chip","keywords":["chip","Feedback & status","base","component","Components","Zeus","indicator","tag","status","delta","filter"]},{"title":"Notification","href":"/components/notification/","description":"Toast with status, icon or avatar, timestamp, actions and auto-dismiss.","component":"notification","keywords":["notification","Feedback & status","base","component","Components","Zeus","feedback","toast","alert","status"]},{"title":"Notification Center","href":"/components/notification-center/","description":"Grouped, filterable inbox with unread state and per-item actions.","component":"notification-center","keywords":["notification-center","Feedback & status","blocks","block","Blocks","Zeus","feedback","inbox","notifications","filtering"]},{"title":"Status Dot","href":"/components/status-dot/","description":"The small coloured dot that prefixes a status label.","component":"status-dot","keywords":["status-dot","Feedback & status","base","component","Components","Zeus","indicator","status","dot","presence"]},{"title":"Theme Toggle","href":"/components/theme-toggle/","description":"Light/dark switch that reveals the next theme from the exact click point.","component":"theme-toggle","keywords":["theme-toggle","Feedback & status","blocks","block","Blocks","Zeus","theme","dark-mode","view-transition","settings"]}]},{"id":"dashboards","title":"Dashboards","kind":"dashboard","entries":[{"title":"Revenue overview","href":"/dashboards/revenue-overview/","description":"The default commerce view: KPIs across the top, revenue against orders, then the customer table underneath.","keywords":["revenue-overview","commerce","kpi","revenue","table"]},{"title":"Agent console","href":"/dashboards/agent-console/","description":"Everything you need to put an agent in front of a user: the chat itself, the thinking states, and the composer light.","keywords":["agent-console","ai","agent","chat","streaming"]},{"title":"Account & settings","href":"/dashboards/account-settings/","description":"The screens every product needs and nobody enjoys building: auth, settings, and the notification inbox.","keywords":["account-settings","auth","settings","account","notifications"]},{"title":"Health monitor","href":"/dashboards/health-monitor/","description":"A patient view: identity and vitals beside a dated alert feed.","keywords":["health-monitor","health","profile","alerts","vitals"]},{"title":"Form kit","href":"/dashboards/form-kit/","description":"Every input the system ships, on one page — useful when you are deciding what a form should be made of.","keywords":["form-kit","form","input","validation","reference"]}]},{"id":"playgrounds","title":"Animation playgrounds","kind":"playground","entries":[{"title":"Background Fill Button playground","href":"/playground/background-fill-button/","description":"Hover or focus to watch the rounded fill rise from below.","keywords":["background-fill-button","BackgroundFillButton","animation","motion","controls"]},{"title":"Circle Fill Button playground","href":"/playground/circle-fill-button/","description":"Enter from different points. Each circle reaches the farthest corner in the same time.","keywords":["circle-fill-button","CircleFillButton","animation","motion","controls"]},{"title":"Curve Fill Button playground","href":"/playground/curve-fill-button/","description":"Enter from each side to change the direction of the curved wave.","keywords":["curve-fill-button","CurveFillButton","animation","motion","controls"]},{"title":"Animated Icon Button playground","href":"/playground/animated-icon-button/","description":"Hover or focus. The icon cycles diagonally as its circle fills the capsule.","keywords":["animated-icon-button","AnimatedIconButton","animation","motion","controls"]},{"title":"Magnetic Button playground","href":"/playground/magnetic-button/","description":"Move around the button to pull its surface and label. Leave to spring back.","keywords":["magnetic-button","MagneticButton","animation","motion","controls"]},{"title":"Rainbow Button playground","href":"/playground/rainbow-button/","description":"The rainbow flows around the dark face. Hover or focus to expand its glow.","keywords":["rainbow-button","RainbowButton","animation","motion","controls"]},{"title":"Text Underline playground","href":"/playground/text-underline/","description":"The line draws from the left, then leaves toward the right.","keywords":["text-underline","TextUnderline","animation","motion","controls"]},{"title":"Grid Chapters playground","href":"/playground/grid-chapters/","description":"Scroll through the chapters. The heading stays pinned while image columns pass through the grid.","keywords":["grid-chapters","GridChapters","animation","motion","controls"]},{"title":"Network Globe playground","href":"/playground/network-globe/","description":"Drag the dotted globe or choose the next location. Routes follow the selected destination.","keywords":["network-globe","NetworkGlobe","animation","motion","controls"]},{"title":"Dock Footer playground","href":"/playground/dock-footer/","description":"Scroll to uncover the footer. The dock opens at the bottom; Menu and Escape control it at any time.","keywords":["dock-footer","DockFooter","animation","motion","controls"]},{"title":"Flip Filter playground","href":"/playground/flip-filter/","description":"Filter products and switch between grid and list. Interrupt a transition to see retained cards retarget continuously.","keywords":["flip-filter","FlipFilter","animation","motion","controls"]},{"title":"Capsule Hero playground","href":"/playground/capsule-hero/","description":"Activate the rounded aperture to expand it, then activate it again or press Escape. This sample uses a poster; the SDK also accepts a video source.","keywords":["capsule-hero","CapsuleHero","animation","motion","controls"]},{"title":"Wipe Slider playground","href":"/playground/wipe-slider/","description":"Use the arrows, keyboard or swipe to change slides. The image, masked copy and rolling index respond to direction.","keywords":["wipe-slider","WipeSlider","animation","motion","controls"]},{"title":"Step Wipe playground","href":"/playground/step-wipe/","description":"Scroll through the product stages. Each background, foreground frame and caption has its own wipe timing.","keywords":["step-wipe","StepWipe","animation","motion","controls"]},{"title":"Gradient Footer playground","href":"/playground/gradient-footer/","description":"Scroll to reveal the footer glow and brand. Hover or focus links to draw their underlines.","keywords":["gradient-footer","GradientFooter","animation","motion","controls"]},{"title":"Testimonial Globe playground","href":"/playground/testimonial-globe/","description":"Drag the portrait sphere and navigate the quotes. On small screens, swipe the sphere to change the single quote.","keywords":["testimonial-globe","TestimonialGlobe","animation","motion","controls"]},{"title":"Drift Gallery playground","href":"/playground/drift-gallery/","description":"Grab the cards to separate them, then release to carry momentum. Velocity adds grain and shifts the color treatment.","keywords":["drift-gallery","DriftGallery","animation","motion","controls"]},{"title":"Morphing Pill Menu playground","href":"/playground/morphing-pill-menu/","description":"Open the pill, hover or focus its rolling links, then press Escape to close.","keywords":["morphing-pill-menu","MorphingPillMenu","animation","motion","controls"]},{"title":"Gooey Menu playground","href":"/playground/gooey-menu/","description":"Open the action control and choose an action. Change the arrangement to compare the liquid connections.","keywords":["gooey-menu","GooeyMenu","animation","motion","controls"]},{"title":"Mega Menu playground","href":"/playground/mega-menu/","description":"Move between navigation groups. On mobile, open the menu and expand a group.","keywords":["mega-menu","MegaMenu","animation","motion","controls"]},{"title":"Fullscreen Slide Menu playground","href":"/playground/fullscreen-slide-menu/","description":"Open navigation to turn the page away. Hover or focus a link to change its image.","keywords":["fullscreen-slide-menu","FullscreenSlideMenu","animation","motion","controls"]},{"title":"Scatter Cluster playground","href":"/playground/scatter-cluster/","description":"Scroll inside the scene or focus it and use Page Down. Advance moves to the next position.","keywords":["scatter-cluster","ScatterCluster","animation","motion","controls"]},{"title":"Multi Flip playground","href":"/playground/multi-flip/","description":"Scroll inside the scene or focus it and use Page Down. Advance moves to the next position.","keywords":["multi-flip","MultiFlip","animation","motion","controls"]},{"title":"Velocity Clip playground","href":"/playground/velocity-clip/","description":"Scroll inside the scene or focus it and use Page Down. Advance moves to the next position.","keywords":["velocity-clip","VelocityClip","animation","motion","controls"]},{"title":"SVG Draw Path playground","href":"/playground/svg-draw-path/","description":"Scroll inside the scene or focus it and use Page Down. Advance moves to the next position.","keywords":["svg-draw-path","SvgDrawPath","animation","motion","controls"]},{"title":"Progress playground","href":"/playground/progress/","description":"Scroll inside the scene or focus it and use Page Down. Advance moves to the next position.","keywords":["progress","Progress","animation","motion","controls"]},{"title":"Parallax playground","href":"/playground/parallax/","description":"Scroll inside the scene or focus it and use Page Down. Advance moves to the next position.","keywords":["parallax","Parallax","animation","motion","controls"]},{"title":"Mask Reveal playground","href":"/playground/mask-reveal/","description":"Scroll inside the scene or focus it and use Page Down. Advance moves to the next position.","keywords":["mask-reveal","MaskReveal","animation","motion","controls"]},{"title":"Hide Header playground","href":"/playground/hide-header/","description":"Scroll inside the scene or focus it and use Page Down. Advance moves to the next position.","keywords":["hide-header","HideHeader","animation","motion","controls"]},{"title":"Flip Zone playground","href":"/playground/flip-zone/","description":"Scroll inside the scene or focus it and use Page Down. Advance moves to the next position.","keywords":["flip-zone","FlipZone","animation","motion","controls"]},{"title":"Element Reveal playground","href":"/playground/element-reveal/","description":"Scroll inside the scene or focus it and use Page Down. Advance moves to the next position.","keywords":["element-reveal","ElementReveal","animation","motion","controls"]},{"title":"Counter playground","href":"/playground/counter/","description":"Scroll inside the scene or focus it and use Page Down. Advance moves to the next position.","keywords":["counter","Counter","animation","motion","controls"]},{"title":"Background Color playground","href":"/playground/background-color/","description":"Scroll inside the scene or focus it and use Page Down. Advance moves to the next position.","keywords":["background-color","BackgroundColor","animation","motion","controls"]},{"title":"Animated Grid playground","href":"/playground/animated-grid/","description":"Scroll inside the scene or focus it and use Page Down. Advance moves to the next position.","keywords":["animated-grid","AnimatedGrid","animation","motion","controls"]},{"title":"Random Rotate playground","href":"/playground/random-rotate/","description":"Scroll inside the scene or focus it and use Page Down. Advance moves to the next position.","keywords":["random-rotate","RandomRotate","animation","motion","controls"]},{"title":"Image Trail playground","href":"/playground/image-trail/","description":"Move across the scene to leave image instances behind. Touch and reduced motion keep the content static.","keywords":["image-trail","ImageTrail","animation","motion","controls"]},{"title":"Custom Cursor playground","href":"/playground/custom-cursor/","description":"Move across the cards to see the cursor change labels. Links remain usable by keyboard and touch.","keywords":["custom-cursor","CustomCursor","animation","motion","controls"]},{"title":"3D Card Flip playground","href":"/playground/3d-card-flip/","description":"Move around the card to change its perspective, glare and shadow.","keywords":["3d-card-flip","CardFlip3D","animation","motion","controls"]},{"title":"Photo Stack Gallery playground","href":"/playground/photo-stack-gallery/","description":"Open the stack, choose a thumbnail, then close. The same images exchange places.","keywords":["photo-stack-gallery","PhotoStackGallery","animation","motion","controls"]},{"title":"Spiral Gallery playground","href":"/playground/spiral-gallery/","description":"Drag the 3D helix and release to preserve momentum. Arrow keys also turn it.","keywords":["spiral-gallery","SpiralGallery","animation","motion","controls"]},{"title":"Lens Rail playground","href":"/playground/lens-rail/","description":"Drag or use the arrows. The focal lens expands nearby cards and compresses distant ones.","keywords":["lens-rail","LensRail","animation","motion","controls"]},{"title":"Expanding Strip playground","href":"/playground/expanding-strip/","description":"Hover, focus or tap a column to expand its image and caption.","keywords":["expanding-strip","ExpandingStrip","animation","motion","controls"]},{"title":"Index Wipe playground","href":"/playground/index-wipe/","description":"Move through the chapter names. Background wipes follow the direction of your selection.","keywords":["index-wipe","IndexWipe","animation","motion","controls"]},{"title":"Card Fan playground","href":"/playground/card-fan/","description":"Enter the stack to spread the fan, then target a card to lift it. Focus or tap works too.","keywords":["card-fan","CardFan","animation","motion","controls"]},{"title":"Radial Gallery playground","href":"/playground/radial-gallery/","description":"Images rotate around a circle. Drag or use the arrows to turn the ring.","keywords":["radial-gallery","RadialGallery","animation","motion","controls"]},{"title":"Circular Slider playground","href":"/playground/circular-slider/","description":"Drag the cards along the circular arc. Release to snap the nearest card into place.","keywords":["circular-slider","CircularSlider","animation","motion","controls"]},{"title":"Infinite Parallax Slider playground","href":"/playground/infinite-parallax-slider/","description":"Drag a looping carousel with independent image and caption movement. Hover pauses playback.","keywords":["infinite-parallax-slider","InfiniteParallaxSlider","animation","motion","controls"]},{"title":"Image Follow List playground","href":"/playground/image-follow-list/","description":"Move through the rows to reveal a preview that follows your pointer. Focus and tap reveal it too.","keywords":["image-follow-list","ImageFollowList","animation","motion","controls"]},{"title":"Hoverable List playground","href":"/playground/hoverable-list/","description":"Enter a row from above and below to change the bend of its curved fill.","keywords":["hoverable-list","HoverableList","animation","motion","controls"]},{"title":"Infinite Draggable Grid playground","href":"/playground/infinite-draggable-grid/","description":"Drag the staggered rows in either direction. They wrap seamlessly and carry your momentum.","keywords":["infinite-draggable-grid","InfiniteDraggableGrid","animation","motion","controls"]},{"title":"Infinite Marquee playground","href":"/playground/infinite-marquee/","description":"Drag to scrub the repeated text and images. Choose an axis and direction; playback resumes after release.","keywords":["infinite-marquee","InfiniteMarquee","animation","motion","controls"]},{"title":"Mesh Gradient playground","href":"/playground/mesh-gradient/","description":"Move across the color field to disturb its flow.","keywords":["mesh-gradient","MeshGradient","animation","motion","controls"]},{"title":"Dithering playground","href":"/playground/dithering/","description":"Move over the image to reveal its original colors through the lens.","keywords":["dithering","Dithering","animation","motion","controls"]},{"title":"Image Glitch playground","href":"/playground/image-glitch/","description":"Hover over the image to break the signal; move away to let it recover.","keywords":["image-glitch","ImageGlitch","animation","motion","controls"]},{"title":"Gooey Hover Reveal playground","href":"/playground/gooey-hover-reveal/","description":"Draw across the surface. Keyboard focus, touch and reduced motion show the content directly.","keywords":["gooey-hover-reveal","GooeyHoverReveal","animation","motion","controls"]},{"title":"Dither Cursor Trail playground","href":"/playground/dither-cursor-trail/","description":"Move across the surface to leave a trail of fading dots.","keywords":["dither-cursor-trail","DitherCursorTrail","animation","motion","controls"]},{"title":"Image Dissolve Scroll playground","href":"/playground/image-dissolve-scroll/","description":"Scroll down through the pinned image to dissolve it, then scroll back to restore it.","keywords":["image-dissolve-scroll","ImageDissolveScroll","animation","motion","controls"]},{"title":"Gooey Filter Chips playground","href":"/playground/gooey-filter-chips/","description":"Select several chips, then remove them from the result tray.","keywords":["gooey-filter-chips","GooeyFilterChips","animation","motion","controls"]},{"title":"Morphing Island playground","href":"/playground/morphing-island/","description":"Open Sections, choose a summary, then scroll to its section.","keywords":["morphing-island","MorphingIsland","animation","motion","controls"]},{"title":"Multi-Step Form playground","href":"/playground/multi-step-form/","description":"Complete each step. The final submit reports a local completion state.","keywords":["multi-step-form","MultiStepForm","animation","motion","controls"]},{"title":"Morphing Tabs playground","href":"/playground/morphing-tabs/","description":"Select tabs or use arrow keys to move the connected surface.","keywords":["morphing-tabs","MorphingTabs","animation","motion","controls"]},{"title":"Morphing Accordion playground","href":"/playground/morphing-accordion/","description":"Open and close questions while the liquid answer panels separate.","keywords":["morphing-accordion","MorphingAccordion","animation","motion","controls"]},{"title":"Morphing Select playground","href":"/playground/morphing-select/","description":"Open the select and choose an option, or use the keyboard.","keywords":["morphing-select","MorphingSelect","animation","motion","controls"]},{"title":"Detail Drawer playground","href":"/playground/detail-drawer/","description":"Open the hero to reveal details, then close or press Escape.","keywords":["detail-drawer","DetailDrawer","animation","motion","controls"]},{"title":"Animated Tooltip playground","href":"/playground/animated-tooltip/","description":"Hover, focus, or tap a control. Try pointer following and edge placement.","keywords":["animated-tooltip","AnimatedTooltip","animation","motion","controls"]},{"title":"Morphing Dialog playground","href":"/playground/morphing-dialog/","description":"Expand the card and follow its image and title into the dialog.","keywords":["morphing-dialog","MorphingDialog","animation","motion","controls"]},{"title":"Text On Path playground","href":"/playground/text-on-path/","description":"Scroll inside the scene or use Advance to follow the motion.","keywords":["text-on-path","TextOnPath","animation","motion","controls"]},{"title":"Text Fill Wave playground","href":"/playground/text-fill-wave/","description":"Scroll inside the scene or use Advance to follow the motion.","keywords":["text-fill-wave","TextFillWave","animation","motion","controls"]},{"title":"Velocity Type playground","href":"/playground/velocity-type/","description":"Scroll inside the scene or use Advance to follow the motion.","keywords":["velocity-type","VelocityType","animation","motion","controls"]},{"title":"Typewriter playground","href":"/playground/typewriter/","description":"Phrases type, pause and erase at independently controlled speeds.","keywords":["typewriter","Typewriter","animation","motion","controls"]},{"title":"Character Appear playground","href":"/playground/character-appear/","description":"Scroll inside the scene or use Advance to follow the motion.","keywords":["character-appear","CharacterAppear","animation","motion","controls"]},{"title":"Text Morph playground","href":"/playground/text-morph/","description":"Edit the input to move matching letters into their new positions.","keywords":["text-morph","TextMorph","animation","motion","controls"]},{"title":"Text Split Zoom playground","href":"/playground/text-split-zoom/","description":"Scroll inside the scene or use Advance to follow the motion.","keywords":["text-split-zoom","TextSplitZoom","animation","motion","controls"]},{"title":"Text Scramble playground","href":"/playground/text-scramble/","description":"Scroll inside the scene or use Advance to follow the motion.","keywords":["text-scramble","TextScramble","animation","motion","controls"]},{"title":"Rectangle Text Reveal playground","href":"/playground/rectangle-text-reveal/","description":"Scroll inside the scene or use Advance to follow the motion.","keywords":["rectangle-text-reveal","RectangleTextReveal","animation","motion","controls"]},{"title":"Popping Text playground","href":"/playground/popping-text/","description":"Scroll inside the scene or use Advance to follow the motion.","keywords":["popping-text","PoppingText","animation","motion","controls"]},{"title":"Folding Text playground","href":"/playground/folding-text/","description":"Scroll inside the scene or use Advance to follow the motion.","keywords":["folding-text","FoldingText","animation","motion","controls"]},{"title":"Cinematic Text playground","href":"/playground/cinematic-text/","description":"Scroll inside the scene or use Advance to follow the motion.","keywords":["cinematic-text","CinematicText","animation","motion","controls"]},{"title":"Text Shimmer Wave playground","href":"/playground/text-shimmer-wave/","description":"Watch the light travel across the letters.","keywords":["text-shimmer-wave","TextShimmerWave","animation","motion","controls"]},{"title":"Morphing Tooltip playground","href":"/playground/morphing-tooltip/","description":"Hover or focus the buttons. The same tooltip follows you.","keywords":["morphing-tooltip","MorphingTooltip","animation","motion","controls"]},{"title":"Text Reveal playground","href":"/playground/text-reveal/","description":"Watch the text arrive. Replay to compare your settings.","keywords":["text-reveal","TextReveal","animation","motion","controls"]},{"title":"Animated Accordion playground","href":"/playground/animated-accordion/","description":"Open a question to see the panel and text move together.","keywords":["animated-accordion","AnimatedAccordion","animation","motion","controls"]},{"title":"Dual Scramble playground","href":"/playground/dual-scramble/","description":"Choose a trigger, then hover, click or replay the text.","keywords":["dual-scramble","DualScramble","animation","motion","controls"]},{"title":"Multi-Level Drawer Menu playground","href":"/playground/multi-level-drawer-menu/","description":"Open the menu and explore its nested navigation.","keywords":["multi-level-drawer-menu","MultiLevelDrawerMenu","animation","motion","controls"]},{"title":"Image Fly-In playground","href":"/playground/image-fly-in/","description":"Move your pointer over the scene to shift the image field.","keywords":["image-fly-in","ImageFlyIn","animation","motion","controls"]},{"title":"Scale Slider playground","href":"/playground/scale-slider/","description":"Drag the images or let the slider move on its own.","keywords":["scale-slider","ScaleSlider","animation","motion","controls"]},{"title":"Curtain Slider playground","href":"/playground/curtain-slider/","description":"Drag across the image or use the next and previous controls.","keywords":["curtain-slider","CurtainSlider","animation","motion","controls"]},{"title":"Feature Dialog playground","href":"/playground/feature-dialog/","description":"Open the feature card to explore the expanding detail sheet.","keywords":["feature-dialog","FeatureDialog","animation","motion","controls"]},{"title":"ASCII Overlay playground","href":"/playground/ascii-overlay/","description":"Move over the portrait to explore its live character texture.","keywords":["ascii-overlay","AsciiOverlay","animation","motion","controls"]},{"title":"Animated Pricing playground","href":"/playground/animated-pricing/","description":"Switch the billing period to watch the prices roll into place.","keywords":["animated-pricing","AnimatedPricing","animation","motion","controls"]}]}],"children":"$L4"}]]}]}]]}],{"children":["$L5",{"children":["$L6",{"children":["$L7",{},null,false,null]},null,false,"$8"]},null,false,"$8"]},null,false,null],"$L9",false]],"m":"$undefined","G":["$a",["$Lb","$Lc"]],"S":true,"h":null,"r":"$undefined","s":"$undefined","a":"$undefined","l":"$undefined","p":"$undefined","d":"$undefined","b":"UfJyyO88iN0C3HvvrH7WP"}
d:I[337280,["/_next/static/chunks/0ekqw6yi_zy68.js","/_next/static/chunks/1l1ztpmvvr6un.js"],"default"]
e:I[475701,["/_next/static/chunks/0ekqw6yi_zy68.js","/_next/static/chunks/1l1ztpmvvr6un.js"],"default"]
10:I[579014,["/_next/static/chunks/0ekqw6yi_zy68.js","/_next/static/chunks/1l1ztpmvvr6un.js"],"OutletBoundary"]
11:"$Sreact.suspense"
13:I[579014,["/_next/static/chunks/0ekqw6yi_zy68.js","/_next/static/chunks/1l1ztpmvvr6un.js"],"ViewportBoundary"]
15:I[579014,["/_next/static/chunks/0ekqw6yi_zy68.js","/_next/static/chunks/1l1ztpmvvr6un.js"],"MetadataBoundary"]
17:I[392511,["/_next/static/chunks/0ekqw6yi_zy68.js","/_next/static/chunks/1l1ztpmvvr6un.js","/_next/static/chunks/0lh826sok5gy8.js","/_next/static/chunks/3_1pfg1675we5.js","/_next/static/chunks/3b-j35phbz363.js","/_next/static/chunks/2v6dx3y3pgbg2.js","/_next/static/chunks/3znghnjm2oz_l.js","/_next/static/chunks/3458ylrm-tu9f.js","/_next/static/chunks/1h9znjrekhrnu.js","/_next/static/chunks/052khci2jbxel.js","/_next/static/chunks/3fuakxesa5sfw.js","/_next/static/chunks/34kutq8p1wnfe.js","/_next/static/chunks/0y5abjvkezh1o.js"],"DocsLayout"]
19:I[125016,["/_next/static/chunks/0ekqw6yi_zy68.js","/_next/static/chunks/1l1ztpmvvr6un.js","/_next/static/chunks/0lh826sok5gy8.js","/_next/static/chunks/3_1pfg1675we5.js","/_next/static/chunks/3b-j35phbz363.js","/_next/static/chunks/2v6dx3y3pgbg2.js","/_next/static/chunks/3znghnjm2oz_l.js","/_next/static/chunks/3458ylrm-tu9f.js","/_next/static/chunks/1h9znjrekhrnu.js","/_next/static/chunks/052khci2jbxel.js","/_next/static/chunks/3fuakxesa5sfw.js","/_next/static/chunks/34kutq8p1wnfe.js","/_next/static/chunks/0y5abjvkezh1o.js"],"ComponentPage"]
4:["$","$Ld",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$Le",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":[[["$","title",null,{"children":"404: This page could not be found."}],["$","div",null,{"style":{"fontFamily":"system-ui,\"Segoe UI\",Roboto,Helvetica,Arial,sans-serif,\"Apple Color Emoji\",\"Segoe UI Emoji\"","height":"100vh","textAlign":"center","display":"flex","flexDirection":"column","alignItems":"center","justifyContent":"center"},"children":["$","div",null,{"children":[["$","style",null,{"dangerouslySetInnerHTML":{"__html":"body{color:#000;background:#fff;margin:0}.next-error-h1{border-right:1px solid rgba(0,0,0,.3)}@media (prefers-color-scheme:dark){body{color:#fff;background:#000}.next-error-h1{border-right:1px solid rgba(255,255,255,.3)}}"}}],["$","h1",null,{"className":"next-error-h1","style":{"display":"inline-block","margin":"0 20px 0 0","padding":"0 23px 0 0","fontSize":24,"fontWeight":500,"verticalAlign":"top","lineHeight":"49px"},"children":404}],["$","div",null,{"style":{"display":"inline-block"},"children":["$","h2",null,{"style":{"fontSize":14,"fontWeight":400,"lineHeight":"49px","margin":0},"children":"This page could not be found."}]}]]}]}]],[]],"forbidden":"$undefined","unauthorized":"$undefined"}]
5:["$","$1","c",{"children":[null,["$","$Ld",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$Le",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","forbidden":"$undefined","unauthorized":"$undefined"}]]}]
6:["$","$1","c",{"children":[null,["$","$Ld",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$Le",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","forbidden":"$undefined","unauthorized":"$undefined"}]]}]
7:["$","$1","c",{"children":["$Lf",[["$","script","script-0",{"src":"/_next/static/chunks/0lh826sok5gy8.js","async":true,"nonce":"$undefined"}],["$","script","script-1",{"src":"/_next/static/chunks/3_1pfg1675we5.js","async":true,"nonce":"$undefined"}],["$","script","script-2",{"src":"/_next/static/chunks/3b-j35phbz363.js","async":true,"nonce":"$undefined"}],["$","script","script-3",{"src":"/_next/static/chunks/2v6dx3y3pgbg2.js","async":true,"nonce":"$undefined"}],["$","script","script-4",{"src":"/_next/static/chunks/3znghnjm2oz_l.js","async":true,"nonce":"$undefined"}],["$","script","script-5",{"src":"/_next/static/chunks/3458ylrm-tu9f.js","async":true,"nonce":"$undefined"}],["$","script","script-6",{"src":"/_next/static/chunks/1h9znjrekhrnu.js","async":true,"nonce":"$undefined"}],["$","script","script-7",{"src":"/_next/static/chunks/052khci2jbxel.js","async":true,"nonce":"$undefined"}],["$","script","script-8",{"src":"/_next/static/chunks/3fuakxesa5sfw.js","async":true,"nonce":"$undefined"}],["$","script","script-9",{"src":"/_next/static/chunks/34kutq8p1wnfe.js","async":true,"nonce":"$undefined"}],["$","script","script-10",{"src":"/_next/static/chunks/0y5abjvkezh1o.js","async":true,"nonce":"$undefined"}]],["$","$L10",null,{"children":["$","$11",null,{"name":"Next.MetadataOutlet","children":"$@12"}]}]]}]
9:["$","$1","h",{"children":[null,["$","$L13",null,{"children":"$L14"}],["$","div",null,{"hidden":true,"children":["$","$L15",null,{"children":["$","$11",null,{"name":"Next.Metadata","children":"$L16"}]}]}],["$","meta",null,{"name":"next-size-adjust","content":""}]]}]
b:["$","link","0",{"rel":"stylesheet","href":"/_next/static/chunks/00d9be-48vpo2.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}]
c:["$","link","1",{"rel":"stylesheet","href":"/_next/static/chunks/2jq731gt7y9oq.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}]
8:C
f:["$","$L17",null,{"title":"Agent Chat","heading":"Agent Chat","icon":"components","groups":[{"id":"buttons","title":"Buttons","items":[{"name":"background-fill-button","title":"Background Fill Button","isNew":true},{"name":"circle-fill-button","title":"Circle Fill Button","isNew":true},{"name":"curve-fill-button","title":"Curve Fill Button","isNew":true},{"name":"animated-icon-button","title":"Animated Icon Button","isNew":true},{"name":"magnetic-button","title":"Magnetic Button","isNew":true},{"name":"rainbow-button","title":"Rainbow Button","isNew":true},{"name":"text-underline","title":"Text Underline","isNew":true},{"name":"button","title":"Button","isNew":"$undefined"},{"name":"icon-button","title":"Icon Button","isNew":"$undefined"},{"name":"link-button","title":"Link Button","isNew":"$undefined"},{"name":"button-group","title":"Button Group","isNew":"$undefined"},{"name":"close-button","title":"Close Button","isNew":"$undefined"},{"name":"social-button","title":"Social Button","isNew":"$undefined"}]},{"id":"forms","title":"Forms & inputs","items":[{"name":"gooey-filter-chips","title":"Gooey Filter Chips","isNew":true},{"name":"multi-step-form","title":"Multi-Step Form","isNew":true},{"name":"morphing-select","title":"Morphing Select","isNew":true},{"name":"input","title":"Input","isNew":"$undefined"},{"name":"input-otp","title":"Input OTP","isNew":"$undefined"},{"name":"checkbox","title":"Checkbox","isNew":"$undefined"},{"name":"radio","title":"Radio","isNew":"$undefined"},{"name":"switch","title":"Switch","isNew":"$undefined"},{"name":"select","title":"Select","isNew":"$undefined"},{"name":"slider","title":"Slider","isNew":"$undefined"},{"name":"file-upload","title":"File Upload","isNew":"$undefined"},{"name":"auth-card","title":"Auth Card","isNew":"$undefined"}]},{"id":"navigation","title":"Navigation","items":[{"name":"morphing-pill-menu","title":"Morphing Pill Menu","isNew":true},{"name":"gooey-menu","title":"Gooey Menu","isNew":true},{"name":"mega-menu","title":"Mega Menu","isNew":true},{"name":"fullscreen-slide-menu","title":"Fullscreen Slide Menu","isNew":true},{"name":"hide-header","title":"Hide Header","isNew":true},{"name":"custom-cursor","title":"Custom Cursor","isNew":true},{"name":"morphing-island","title":"Morphing Island","isNew":true},{"name":"morphing-tabs","title":"Morphing Tabs","isNew":true},{"name":"multi-level-drawer-menu","title":"Multi-Level Drawer Menu","isNew":true},{"name":"dropdown","title":"Dropdown","isNew":"$undefined"},{"name":"tabs","title":"Tabs","isNew":"$undefined"},{"name":"segmented-control","title":"Segmented Control","isNew":"$undefined"},{"name":"breadcrumb","title":"Breadcrumb","isNew":"$undefined"},{"name":"pagination","title":"Pagination","isNew":"$undefined"},{"name":"sidebar","title":"Sidebar","isNew":"$undefined"}]},{"id":"feedback","title":"Feedback & status","items":[{"name":"badge","title":"Badge","isNew":"$undefined"},{"name":"chip","title":"Chip","isNew":"$undefined"},{"name":"status-dot","title":"Status Dot","isNew":"$undefined"},{"name":"announcement","title":"Announcement","isNew":"$undefined"},{"name":"notification","title":"Notification","isNew":"$undefined"},{"name":"theme-toggle","title":"Theme Toggle","isNew":"$undefined"},{"name":"notification-center","title":"Notification Center","isNew":"$undefined"}]},{"id":"overlays","title":"Overlays & disclosures","items":[{"name":"morphing-accordion","title":"Morphing Accordion","isNew":true},{"name":"detail-drawer","title":"Detail Drawer","isNew":true},{"name":"animated-tooltip","title":"Animated Tooltip","isNew":true},{"name":"morphing-dialog","title":"Morphing Dialog","isNew":true},{"name":"morphing-tooltip","title":"Morphing Tooltip","isNew":true},{"name":"animated-accordion","title":"Animated Accordion","isNew":true},{"name":"feature-dialog","title":"Feature Dialog","isNew":true},{"name":"tooltip","title":"Tooltip","isNew":"$undefined"},{"name":"settings-modal","title":"Settings Modal","isNew":"$undefined"}]},{"id":"typography","title":"Text & typography","items":[{"name":"text-on-path","title":"Text On Path","isNew":true},{"name":"text-fill-wave","title":"Text Fill Wave","isNew":true},{"name":"velocity-type","title":"Velocity Type","isNew":true},{"name":"typewriter","title":"Typewriter","isNew":true},{"name":"character-appear","title":"Character Appear","isNew":true},{"name":"text-morph","title":"Text Morph","isNew":true},{"name":"text-split-zoom","title":"Text Split Zoom","isNew":true},{"name":"text-scramble","title":"Text Scramble","isNew":true},{"name":"rectangle-text-reveal","title":"Rectangle Text Reveal","isNew":true},{"name":"popping-text","title":"Popping Text","isNew":true},{"name":"folding-text","title":"Folding Text","isNew":true},{"name":"cinematic-text","title":"Cinematic Text","isNew":true},{"name":"text-shimmer-wave","title":"Text Shimmer Wave","isNew":true},{"name":"text-reveal","title":"Text Reveal","isNew":true},{"name":"dual-scramble","title":"Dual Scramble","isNew":true},{"name":"typography","title":"Typography","isNew":"$undefined"},{"name":"kbd","title":"Kbd","isNew":"$undefined"}]},{"id":"icons","title":"Icons & identity","items":[{"name":"chevrons","title":"Chevrons","isNew":"$undefined"},{"name":"logo","title":"Logo","isNew":"$undefined"},{"name":"avatar","title":"Avatar","isNew":"$undefined"}]},{"id":"layout","title":"Layout & structure","items":[{"name":"grid-chapters","title":"Grid Chapters","isNew":true},{"name":"network-globe","title":"Network Globe","isNew":true},{"name":"dock-footer","title":"Dock Footer","isNew":true},{"name":"flip-filter","title":"Flip Filter","isNew":true},{"name":"capsule-hero","title":"Capsule Hero","isNew":true},{"name":"wipe-slider","title":"Wipe Slider","isNew":true},{"name":"step-wipe","title":"Step Wipe","isNew":true},{"name":"gradient-footer","title":"Gradient Footer","isNew":true},{"name":"testimonial-globe","title":"Testimonial Globe","isNew":true},{"name":"drift-gallery","title":"Drift Gallery","isNew":true},{"name":"divider","title":"Divider","isNew":"$undefined"},{"name":"app-shell","title":"App Shell","isNew":"$undefined"}]},{"id":"data","title":"Data & charts","items":[{"name":"progress","title":"Progress","isNew":true},{"name":"counter","title":"Counter","isNew":true},{"name":"combo-chart-card","title":"Combo Chart","isNew":true},{"name":"contributions-card","title":"Contributions","isNew":true},{"name":"earnings-chart-card","title":"Earnings Chart","isNew":true},{"name":"funnel-chart-card","title":"Funnel Chart","isNew":true},{"name":"heatmap-chart-card","title":"Heatmap Chart","isNew":true},{"name":"most-active-days-card","title":"Most Active Days","isNew":true},{"name":"radar-chart-card","title":"Radar Chart","isNew":true},{"name":"radial-chart-card","title":"Radial Chart","isNew":true},{"name":"sankey-chart-card","title":"Sankey Chart","isNew":true},{"name":"scatter-chart-card","title":"Scatter Chart","isNew":true},{"name":"sleep-score-card","title":"Sleep Score","isNew":true},{"name":"stage-bars-card","title":"Stage Bars","isNew":true},{"name":"steps-card","title":"Steps Chart","isNew":true},{"name":"table","title":"Table","isNew":"$undefined"},{"name":"stat-cards","title":"Stat Cards","isNew":"$undefined"},{"name":"data-table","title":"Data Table","isNew":"$undefined"},{"name":"area-chart-card","title":"Area Chart","isNew":true},{"name":"line-chart-card","title":"Line Chart","isNew":true},{"name":"bar-list-card","title":"Bar List","isNew":true},{"name":"activity-rings-card","title":"Activity Rings","isNew":true},{"name":"chart-card","title":"Chart Card","isNew":true},{"name":"revenue-chart-card","title":"Revenue Chart","isNew":"$undefined"},{"name":"orders-chart-card","title":"Orders Chart","isNew":"$undefined"},{"name":"important-alerts-card","title":"Important Alerts","isNew":"$undefined"},{"name":"patient-info-card","title":"Patient Info","isNew":"$undefined"}]},{"id":"media","title":"Images & media","items":[{"name":"mesh-gradient","title":"Mesh Gradient","isNew":true},{"name":"dithering","title":"Dithering","isNew":true},{"name":"image-glitch","title":"Image Glitch","isNew":true},{"name":"gooey-hover-reveal","title":"Gooey Hover Reveal","isNew":true},{"name":"dither-cursor-trail","title":"Dither Cursor Trail","isNew":true},{"name":"image-dissolve-scroll","title":"Image Dissolve Scroll","isNew":true},{"name":"scatter-cluster","title":"Scatter Cluster","isNew":true},{"name":"multi-flip","title":"Multi Flip","isNew":true},{"name":"velocity-clip","title":"Velocity Clip","isNew":true},{"name":"svg-draw-path","title":"SVG Draw Path","isNew":true},{"name":"parallax","title":"Parallax","isNew":true},{"name":"mask-reveal","title":"Mask Reveal","isNew":true},{"name":"flip-zone","title":"Flip Zone","isNew":true},{"name":"element-reveal","title":"Element Reveal","isNew":true},{"name":"background-color","title":"Background Color","isNew":true},{"name":"animated-grid","title":"Animated Grid","isNew":true},{"name":"random-rotate","title":"Random Rotate","isNew":true},{"name":"photo-stack-gallery","title":"Photo Stack Gallery","isNew":true},{"name":"spiral-gallery","title":"Spiral Gallery","isNew":true},{"name":"lens-rail","title":"Lens Rail","isNew":true},{"name":"expanding-strip","title":"Expanding Strip","isNew":true},{"name":"index-wipe","title":"Index Wipe","isNew":true},{"name":"card-fan","title":"Card Fan","isNew":true},{"name":"radial-gallery","title":"Radial Gallery","isNew":true},{"name":"circular-slider","title":"Circular Slider","isNew":true},{"name":"infinite-parallax-slider","title":"Infinite Parallax Slider","isNew":true},{"name":"image-follow-list","title":"Image Follow List","isNew":true},{"name":"hoverable-list","title":"Hoverable List","isNew":true},{"name":"infinite-draggable-grid","title":"Infinite Draggable Grid","isNew":true},{"name":"infinite-marquee","title":"Infinite Marquee","isNew":true},{"name":"image-trail","title":"Image Trail","isNew":true},{"name":"3d-card-flip","title":"3D Card Flip","isNew":true},{"name":"image-fly-in","title":"Image Fly-In","isNew":true},{"name":"scale-slider","title":"Scale Slider","isNew":true},{"name":"curtain-slider","title":"Curtain Slider","isNew":true},{"name":"ascii-overlay","title":"ASCII Overlay","isNew":true},{"name":"carousel","title":"Carousel","isNew":"$undefined"}]},{"id":"scheduling","title":"Dates & scheduling","items":[{"name":"calendar","title":"Calendar","isNew":true},{"name":"date-picker","title":"Date Picker","isNew":"$undefined"},{"name":"date-range-picker","title":"Date Range Picker","isNew":"$undefined"},{"name":"meeting-scheduler","title":"Meeting Scheduler","isNew":"$undefined"}]},{"id":"ai","title":"AI & agents","items":[{"name":"ai-profile","title":"AI Profile","isNew":true},{"name":"ai-chat","title":"AI Chat","isNew":true},{"name":"ai-image-generation","title":"AI Image Generation","isNew":true},{"name":"agent-progress","title":"Agent Progress","isNew":true},{"name":"composer","title":"Composer","isNew":true},{"name":"task-list","title":"Task List","isNew":true},{"name":"web-search","title":"Web Search","isNew":true},{"name":"agent-limits-card","title":"Agent Limits","isNew":true},{"name":"agent-thinking","title":"Agent Thinking","isNew":"$undefined"},{"name":"composer-loader","title":"Composer Loader","isNew":"$undefined"},{"name":"agent-log","title":"Agent Log","isNew":"$undefined"},{"name":"agent-chat","title":"Agent Chat","isNew":"$undefined"}]},{"id":"pricing","title":"Pricing & billing","items":[{"name":"animated-pricing","title":"Animated Pricing","isNew":true}]},{"id":"screens","title":"Complete screens","items":[{"name":"home-dashboard","title":"Home Dashboard","isNew":true},{"name":"finance-dashboard","title":"Finance Dashboard","isNew":true},{"name":"marketing-dashboard","title":"Marketing Dashboard","isNew":true},{"name":"hr-management","title":"HR Management","isNew":true},{"name":"medical-profile","title":"Medical Profile","isNew":true}]},{"id":"foundations","title":"Design foundations","items":[{"name":"tokens","title":"Design tokens","isNew":"$undefined"}]},{"id":"utilities","title":"Hooks & utilities","items":[{"name":"cx","title":"cx","isNew":"$undefined"},{"name":"use-count-up","title":"useCountUp","isNew":"$undefined"},{"name":"use-dismiss-on-outside-press","title":"useDismissOnOutsidePress","isNew":"$undefined"}]}],"description":"A working chat app: thread history, streaming messages, composer and suggestions.","columnClassName":"$undefined","detail":true,"children":"$L18"}]
1a:T1177,"use client";

import { RiCheckLine, RiMoreFill, RiUploadLine } from "@remixicon/react";
import { useEffect, useState } from "react";

import {
  Dropdown,
  DropdownGroup,
  DropdownItem,
  DropdownPopover,
  DropdownTrigger,
} from "../../base/dropdown/dropdown";
import { cx } from "../../utils/cx";

/**
 * The controls in the top right of the chat surface: share the open chat, and
 * a menu of the actions that apply to it.
 *
 * Every entry does something the starter can actually do — copy, download,
 * mark, delete. Nothing here is a placeholder waiting on a backend, because a
 * control that looks live and does nothing is worse than no control.
 */

export interface AgentChatActionsProps {
  /** Plain-text transcript of the open chat, for share and copy. */
  transcript: string;
  onExport: () => void;
  onToggleUnread: () => void;
  onDelete: () => void;
  /** No chat open yet, so there is nothing to act on. */
  disabled?: boolean;
  className?: string;
}

export function AgentChatActions({
  transcript,
  onExport,
  onToggleUnread,
  onDelete,
  disabled = false,
  className,
}: AgentChatActionsProps) {
  const [menuOpen, setMenuOpen] = useState(false);
  const [shared, setShared] = useState(false);

  useEffect(() => {
    if (!shared) return;
    const timer = setTimeout(() => setShared(false), 1600);
    return () => clearTimeout(timer);
  }, [shared]);

  const share = async () => {
    if (!transcript) return;
    // The Web Share sheet where the browser has one, the clipboard everywhere
    // else. Both leave the transcript on the device.
    try {
      if (navigator.share) {
        await navigator.share({ text: transcript });
        return;
      }
      await navigator.clipboard.writeText(transcript);
      setShared(true);
    } catch {
      // A dismissed share sheet and a refused clipboard both land here, and
      // neither is worth interrupting the reader over.
    }
  };

  const choose = (action: () => void) => () => {
    setMenuOpen(false);
    action();
  };

  return (
    <div className={cx("flex shrink-0 items-center gap-0.5", className)}>
      <button
        type="button"
        onClick={share}
        disabled={disabled}
        aria-label={shared ? "Transcript copied" : "Share chat"}
        title={shared ? "Transcript copied" : "Share chat"}
        className={ACTION_BUTTON}
      >
        {shared ? (
          <RiCheckLine className="size-[18px] shrink-0" aria-hidden />
        ) : (
          <RiUploadLine className="size-[18px] shrink-0" aria-hidden />
        )}
      </button>

      <Dropdown isOpen={menuOpen} onOpenChange={setMenuOpen}>
        <DropdownTrigger
          aria-label="More actions for this chat"
          isDisabled={disabled}
          className={cx(ACTION_BUTTON, menuOpen && "bg-background-primary-hover text-text-primary")}
        >
          <RiMoreFill className="size-[18px] shrink-0" aria-hidden />
        </DropdownTrigger>

        <DropdownPopover
          aria-label="More actions for this chat"
          placement="bottom end"
          className="w-[190px] p-2"
        >
          <DropdownGroup>
            <DropdownItem onSelect={choose(onExport)} className="px-2 py-1.5">
              <span className="truncate text-body-medium whitespace-nowrap text-text-primary">
                Export chats
              </span>
            </DropdownItem>
            <DropdownItem onSelect={choose(onToggleUnread)} className="px-2 py-1.5">
              <span className="truncate text-body-medium whitespace-nowrap text-text-primary">
                Mark as unread
              </span>
            </DropdownItem>
            <DropdownItem onSelect={choose(onDelete)} className="px-2 py-1.5">
              <span className="truncate text-body-medium whitespace-nowrap text-text-error-primary">
                Delete chat
              </span>
            </DropdownItem>
          </DropdownGroup>
        </DropdownPopover>
      </Dropdown>
    </div>
  );
}

/** DropdownTrigger is itself a button, so both controls share plain classes
 *  rather than wrapping an IconButton (which would nest one button in another). */
const ACTION_BUTTON = cx(
  "inline-flex size-7 shrink-0 cursor-pointer items-center justify-center rounded-md",
  "text-foreground-icon-secondary transition-colors duration-150 ease",
  "hover:bg-background-primary-hover hover:text-foreground-icon-primary",
  "disabled:cursor-not-allowed disabled:opacity-40",
);
1b:T4909,"use client";


import { useStarterBase } from "../app-shell/app-shell";
import {
  RiAddLine,
  RiDownloadLine,
  RiGlobalLine,
  RiLogoutBoxRLine,
  RiMore2Fill,
  RiSendPlaneLine,
  RiSettings3Line,
  RiSpeedUpLine,
} from "@remixicon/react";
import { useEffect, useRef, useState, type KeyboardEvent } from "react";

import { Avatar } from "../../base/avatar/avatar";
import {
  Dropdown,
  DropdownGroup,
  DropdownDivider,
  DropdownItem,
  DropdownPopover,
  DropdownTrigger,
} from "../../base/dropdown/dropdown";
import { ChevronDownSmall } from "../../foundations/icons/chevrons";
import { cx } from "../../utils/cx";

/**
 * The chat-history rail, a card of its own beside the chat.
 *
 * It mirrors the structure of the app sidebar — an action at the top, a
 * section label, then a list of items with a relative-time badge — so the two
 * rails read as the same system from opposite sides of the workspace.
 *
 * Threads are held by the parent and kept in the visitor's own browser. That
 * is the honest ceiling for a starter with no database: hosted history across
 * devices is what a backend buys you, and this is the free version of it.
 */

export interface ChatThreadSummary {
  id: string;
  title: string;
  updatedAt: number;
  unread?: boolean;
}

export interface AgentChatHistoryProps {
  threads: ChatThreadSummary[];
  activeId: string;
  onSelect: (id: string) => void;
  onNewChat: () => void;
  onRename: (id: string, title: string) => void;
  onToggleUnread: (id: string) => void;
  onDelete: (id: string) => void;
  /** Downloads the stored chats as JSON. */
  onExport: () => void;
  /** Disables switching mid-stream, which would strand the running response. */
  disabled?: boolean;
  className?: string;
}

export function AgentChatHistory({
  threads,
  activeId,
  onSelect,
  onNewChat,
  onRename,
  onToggleUnread,
  onDelete,
  onExport,
  disabled = false,
  className,
}: AgentChatHistoryProps) {
  return (
    <aside
      aria-label="Chat history"
      className={cx(
        // A card of its own beside the chat rather than a panel within it, the
        // way the Pro template seats its code panel: same surface and radius as
        // the chat card, separated by the workspace gap. Full height, and its
        // own padding, since it no longer borrows the chat card's.
        "flex h-full w-[260px] shrink-0 flex-col gap-6 overflow-hidden rounded-3xl bg-background-secondary-default p-3",
        className,
      )}
    >
      <button
        type="button"
        onClick={onNewChat}
        disabled={disabled}
        className="flex w-full cursor-pointer items-center gap-2 rounded-2lg px-2 py-2 text-body-medium text-text-primary transition-colors hover:bg-background-secondary-hover disabled:cursor-not-allowed disabled:opacity-50"
      >
        <RiAddLine className="size-5 shrink-0 text-foreground-icon-secondary" aria-hidden />
        New chat
      </button>

      <div className="flex min-h-0 flex-1 flex-col gap-1 overflow-y-auto [scrollbar-width:none]">
        <p className="px-2 pb-1 text-body-2-medium text-text-tertiary">Recent</p>

        {threads.length === 0 ? (
          <p className="px-2 text-body-2-regular text-text-tertiary">
            Chats you start show up here.
          </p>
        ) : (
          threads.map((thread) => (
            <ThreadRow
              key={thread.id}
              thread={thread}
              active={thread.id === activeId}
              disabled={disabled}
              onSelect={onSelect}
              onRename={onRename}
              onToggleUnread={onToggleUnread}
              onDelete={onDelete}
            />
          ))
        )}
      </div>

      <RailFooter threads={threads} onExport={onExport} />
    </aside>
  );
}

/**
 * The account strip along the bottom of the rail.
 *
 * The trailing control downloads the stored chats as JSON. It does something
 * real on purpose — this rail's whole premise is that history lives in the
 * visitor's own browser, and the one thing that premise owes them is a way to
 * take it with them.
 */
function RailFooter({
  threads,
  onExport,
}: {
  threads: ChatThreadSummary[];
  onExport: () => void;
}) {
  const count = threads.length;
  const label = count === 0 ? "No chats to export" : `Export ${count} chats`;
  return (
    <div className="mt-auto flex items-center gap-1 border-t border-separator-border pt-3 pr-1">
      <AccountMenu threads={threads} />
      <button
        type="button"
        onClick={onExport}
        disabled={count === 0}
        aria-label={label}
        title={label}
        className="flex size-6 shrink-0 cursor-pointer items-center justify-center rounded-full bg-button-primary text-white transition-opacity hover:opacity-90 disabled:cursor-not-allowed disabled:opacity-40"
      >
        <RiDownloadLine className="size-3.5 shrink-0" aria-hidden />
      </button>
    </div>
  );
}

/** The rail keeps at most this many chats, which is what makes "chats" a real
 *  quota rather than a decorative percentage. Mirrors MAX_THREADS in the shell. */
const THREAD_CAPACITY = 30;
/** localStorage is about 5MB in practice; enough to make the number mean something. */
const STORAGE_BUDGET_BYTES = 5 * 1024 * 1024;

/**
 * The account switcher at the foot of the rail: avatar, name, chevron, and a
 * grouped menu built from the same pieces as the app sidebar's team card, so
 * the two read as one system.
 *
 * "Usage left" expands in place. Its numbers are the starter's actual
 * limits — how many of the retained chats are used, and how much of the
 * browser's storage they occupy — rather than invented percentages, so the row
 * still means something once this ships inside someone else's app.
 */
function AccountMenu({ threads }: { threads: ChatThreadSummary[] }) {
  const [isOpen, setIsOpen] = useState(false);
  const [usageOpen, setUsageOpen] = useState(false);
  const [storedBytes, setStoredBytes] = useState(0);
  const close = () => setIsOpen(false);
  const base = useStarterBase();
  const logOut = () => {
    close();
    window.location.assign(`${base}/login`);
  };

  const chatPercent = Math.min(100, Math.round((threads.length / THREAD_CAPACITY) * 100));
  const storagePercent = Math.min(100, Math.round((storedBytes / STORAGE_BUDGET_BYTES) * 100));

  const toggleUsage = () => {
    // Measured when the section opens rather than on every render: it reads
    // localStorage, which is outside React and not free.
    if (!usageOpen) setStoredBytes(measureStoredBytes());
    setUsageOpen((open) => !open);
  };

  return (
    <Dropdown
      isOpen={isOpen}
      onOpenChange={(open) => {
        setIsOpen(open);
        if (!open) setUsageOpen(false);
      }}
    >
      <DropdownTrigger
        aria-label="Mertcan Esmergul account menu"
        className={cx(
          "flex min-w-0 flex-1 items-center gap-2 rounded-2lg px-1 py-1 text-left",
          "transition-colors hover:bg-background-secondary-hover",
          isOpen && "bg-background-secondary-hover",
        )}
      >
        <Avatar size="sm" initials="M" color="neutral" alt="Mertcan Esmergul" />
        <span className="min-w-0 flex-1 truncate text-body-2-medium text-text-primary">
          Mertcan Esmergul
        </span>
      </DropdownTrigger>

      <DropdownPopover
        aria-label="Account menu"
        placement="top start"
        className="w-[248px] p-2.5"
        dialogClassName="gap-[7px]"
      >
        <DropdownGroup>
          <button
            type="button"
            onClick={toggleUsage}
            aria-expanded={usageOpen}
            className="flex w-full cursor-pointer items-center gap-2 rounded-2lg px-2 py-1.5 text-left transition-colors hover:bg-background-secondary-default"
          >
            <RiSpeedUpLine className="size-[18px] shrink-0 text-foreground-icon-secondary" aria-hidden />
            <span className="flex-1 truncate text-body-medium whitespace-nowrap text-text-primary">
              Usage left
            </span>
            <ChevronDownSmall
              className={cx(
                "size-4 shrink-0 text-foreground-icon-secondary transition-transform duration-150",
                usageOpen && "rotate-180",
              )}
              aria-hidden
            />
          </button>

          {usageOpen && (
            <div className="flex flex-col gap-1 pb-1">
              <UsageRow label="Chats" value={`${threads.length} of ${THREAD_CAPACITY}`} percent={chatPercent} />
              <UsageRow label="Storage" value={formatBytes(storedBytes)} percent={storagePercent} />
              <button
                type="button"
                onClick={close}
                className="flex w-full cursor-pointer items-center gap-2 rounded-2lg py-1 pr-2 pl-9 text-left transition-colors hover:bg-background-secondary-default"
              >
                <span className="flex-1 truncate text-body-2-medium whitespace-nowrap text-text-primary">
                  Upgrade to Max
                </span>
                <RiGlobalLine className="size-4 shrink-0 text-foreground-icon-tertiary" aria-hidden />
              </button>
            </div>
          )}

          <DropdownItem onSelect={close} className="px-2 py-1.5">
            <RiSendPlaneLine className="size-[18px] shrink-0 text-foreground-icon-secondary" aria-hidden />
            <span className="truncate text-body-medium whitespace-nowrap text-text-primary">
              Invite a friend
            </span>
          </DropdownItem>
          <DropdownItem onSelect={close} className="px-2 py-1.5">
            <RiSettings3Line className="size-[18px] shrink-0 text-foreground-icon-secondary" aria-hidden />
            <span className="truncate text-body-medium whitespace-nowrap text-text-primary">
              Settings
            </span>
          </DropdownItem>
        </DropdownGroup>

        <DropdownDivider />

        <DropdownGroup>
          <DropdownItem onSelect={logOut} className="px-2 py-1.5">
            <RiLogoutBoxRLine className="size-[18px] shrink-0 text-foreground-icon-secondary" aria-hidden />
            <span className="truncate text-body-medium whitespace-nowrap text-text-primary">
              Log out
            </span>
          </DropdownItem>
        </DropdownGroup>
      </DropdownPopover>
    </Dropdown>
  );
}

/** A measured line under "Usage left": what it is, how much, how full. */
function UsageRow({ label, value, percent }: { label: string; value: string; percent: number }) {
  return (
    <div className="flex items-center gap-2 py-0.5 pr-2 pl-9">
      <span className="flex-1 truncate text-body-2-medium text-text-primary">{label}</span>
      <span className="shrink-0 text-body-2-regular text-text-secondary">{value}</span>
      <span className="w-9 shrink-0 text-right text-body-2-regular text-text-tertiary">{percent}%</span>
    </div>
  );
}

/** Bytes the stored chats occupy. UTF-16 in storage, so two bytes a character. */
function measureStoredBytes() {
  try {
    return (window.localStorage.getItem("boardui:agent-chat-threads")?.length ?? 0) * 2;
  } catch {
    return 0;
  }
}

function formatBytes(bytes: number) {
  if (bytes < 1024) return `${bytes} B`;
  if (bytes < 1024 * 1024) return `${Math.round(bytes / 1024)} KB`;
  return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
}

function ThreadRow({
  thread,
  active,
  disabled,
  onSelect,
  onRename,
  onToggleUnread,
  onDelete,
}: {
  thread: ChatThreadSummary;
  active: boolean;
  disabled: boolean;
  onSelect: (id: string) => void;
  onRename: (id: string, title: string) => void;
  onToggleUnread: (id: string) => void;
  onDelete: (id: string) => void;
}) {
  const [menuOpen, setMenuOpen] = useState(false);
  const [renaming, setRenaming] = useState(false);
  const [draft, setDraft] = useState(thread.title);
  const inputRef = useRef<HTMLInputElement>(null);

  useEffect(() => {
    if (!renaming) return;
    inputRef.current?.focus();
    inputRef.current?.select();
  }, [renaming]);

  const commit = () => {
    const next = draft.trim();
    if (next && next !== thread.title) onRename(thread.id, next);
    setRenaming(false);
  };

  const onKeyDown = (event: KeyboardEvent<HTMLInputElement>) => {
    if (event.key === "Enter") {
      event.preventDefault();
      commit();
    }
    if (event.key === "Escape") {
      event.preventDefault();
      setDraft(thread.title);
      setRenaming(false);
    }
  };

  if (renaming) {
    return (
      <div className="rounded-2lg bg-background-secondary-hover px-2 py-1.5">
        <label className="sr-only" htmlFor={`rename-${thread.id}`}>
          Rename chat
        </label>
        <input
          id={`rename-${thread.id}`}
          ref={inputRef}
          value={draft}
          onChange={(event) => setDraft(event.target.value)}
          onKeyDown={onKeyDown}
          onBlur={commit}
          className="w-full bg-transparent text-body-2-regular text-text-primary outline-none"
        />
      </div>
    );
  }

  return (
    <div
      className={cx(
        "group/row relative flex items-center rounded-2lg transition-colors",
        // Stepped off the rail's own surface. The rail moved to `secondary`
        // when it became a card of its own, and `primary-hover` resolves to
        // exactly that colour in light — selection would have been invisible.
        // Selection and hover share the fill, so hovering previews selecting.
        active || menuOpen
          ? "bg-background-secondary-hover"
          : "hover:bg-background-secondary-hover",
      )}
    >
      <button
        type="button"
        onClick={() => onSelect(thread.id)}
        disabled={disabled}
        aria-current={active ? "true" : undefined}
        className="flex min-w-0 flex-1 cursor-pointer items-center gap-1.5 py-1.5 pl-2 text-left disabled:cursor-not-allowed disabled:opacity-50"
      >
        {thread.unread && (
          <span
            aria-label="Unread"
            className="size-1.5 shrink-0 rounded-full bg-button-primary"
          />
        )}
        <span
          className={cx(
            "min-w-0 flex-1 truncate text-body-2-regular",
            thread.unread ? "text-text-primary" : "text-text-secondary",
          )}
        >
          {thread.title}
        </span>
      </button>

      {/* The age and the menu share one slot: the age steps aside on hover so
          the control appears in place rather than shifting the title.
          The two must never be visible together, so they are driven by one
          condition each way. `focus-visible` rather than `focus-within` is the
          load-bearing part: closing the menu with the pointer hands focus back
          to the trigger, and `focus-within` would keep the icon lit on top of
          the age that has already returned. */}
      <span className="group/slot relative flex size-7 shrink-0 items-center justify-center pr-1">
        <span
          aria-hidden={menuOpen}
          className={cx(
            "text-caption-1-regular text-text-tertiary transition-opacity",
            menuOpen
              ? "opacity-0"
              : "opacity-100 group-hover/row:opacity-0 group-has-[:focus-visible]/slot:opacity-0",
          )}
        >
          {relativeTime(thread.updatedAt)}
        </span>

        <span
          className={cx(
            // Nudged 1px left of the age badge's centre: the glyph reads as
            // sitting slightly right in its own box, so the optical centre and
            // the geometric one do not agree.
            "absolute inset-0 flex -translate-x-px items-center justify-center transition-opacity",
            menuOpen
              ? "opacity-100"
              : "opacity-0 group-hover/row:opacity-100 group-has-[:focus-visible]/slot:opacity-100",
          )}
        >
          <RowMenu
            title={thread.title}
            isOpen={menuOpen}
            onOpenChange={setMenuOpen}
            onRename={() => {
              setDraft(thread.title);
              setRenaming(true);
            }}
            onToggleUnread={() => onToggleUnread(thread.id)}
            onDelete={() => onDelete(thread.id)}
          />
        </span>
      </span>
    </div>
  );
}

/** DropdownTrigger is itself the button, so it is styled directly — putting an
 *  IconButton inside it would nest one button in another. */
function RowMenu({
  title,
  isOpen,
  onOpenChange,
  onRename,
  onToggleUnread,
  onDelete,
}: {
  title: string;
  isOpen: boolean;
  onOpenChange: (open: boolean) => void;
  onRename: () => void;
  onToggleUnread: () => void;
  onDelete: () => void;
}) {
  const choose = (action: () => void) => () => {
    onOpenChange(false);
    action();
  };

  return (
    <Dropdown isOpen={isOpen} onOpenChange={onOpenChange}>
      <DropdownTrigger
        aria-label={`More actions for ${title}`}
        className={cx(
          "inline-flex size-6 shrink-0 items-center justify-center rounded-md",
          "text-foreground-icon-secondary transition-colors duration-150 ease",
          "hover:bg-background-tertiary-default hover:text-foreground-icon-primary",
          isOpen && "bg-background-tertiary-default text-foreground-icon-primary",
        )}
      >
        <RiMore2Fill className="size-4 shrink-0" aria-hidden />
      </DropdownTrigger>

      <DropdownPopover
        aria-label={`More actions for ${title}`}
        placement="bottom end"
        className="w-[190px] p-2"
      >
        <DropdownGroup>
          <DropdownItem onSelect={choose(onRename)} className="px-2 py-1.5">
            <span className="truncate text-body-medium whitespace-nowrap text-text-primary">Rename</span>
          </DropdownItem>
          <DropdownItem onSelect={choose(onToggleUnread)} className="px-2 py-1.5">
            <span className="truncate text-body-medium whitespace-nowrap text-text-primary">
              Mark as unread
            </span>
          </DropdownItem>
          <DropdownItem onSelect={choose(onDelete)} className="px-2 py-1.5">
            <span className="truncate text-body-medium whitespace-nowrap text-text-error-primary">Delete</span>
          </DropdownItem>
        </DropdownGroup>
      </DropdownPopover>
    </Dropdown>
  );
}

/** Compact ages for a narrow badge: now, 34m, 5h, 18h, 3d. */
function relativeTime(at: number) {
  const seconds = Math.max(0, Math.round((Date.now() - at) / 1000));
  if (seconds < 60) return "now";
  const minutes = Math.round(seconds / 60);
  if (minutes < 60) return `${minutes}m`;
  const hours = Math.round(minutes / 60);
  if (hours < 24) return `${hours}h`;
  return `${Math.round(hours / 24)}d`;
}
1c:T1d58,"use client";

import { RiCheckLine, RiFileCopyLine, RiVolumeMuteLine, RiVolumeUpLine } from "@remixicon/react";
import { motion, useReducedMotion } from "motion/react";
import { memo, useEffect, useMemo, useState, type ReactNode } from "react";

import { cx } from "../../utils/cx";

/**
 * One turn in the transcript.
 *
 * Assistant replies arrive a paragraph at a time, each softening in out of a
 * blur as it first appears and then only growing, so nothing already on screen
 * ever re-animates.
 *
 * The action row is deliberately mounted at all times and revealed on hover or
 * focus. Rendering it only on hover would make it unreachable by keyboard and
 * would shift the layout as the pointer moves across the transcript.
 */

export interface AgentMessageProps {
  role: string;
  text: string;
  /** True while this message is still being streamed. */
  streaming?: boolean;
  /** When the message first appeared, for the hover timestamp. */
  at?: number;
}

export function AgentMessage({ role, text, streaming = false, at }: AgentMessageProps) {
  if (!text) return null;

  if (role === "user") {
    return (
      <p className="ml-auto flex w-fit max-w-[75%] flex-col rounded-2xl bg-background-primary-default px-3 py-[11px] text-left text-body-regular break-words whitespace-pre-wrap text-text-primary shadow-card">
        {text}
      </p>
    );
  }

  return (
    <div className="group/message flex flex-col gap-1 px-1">
      <StreamedText text={text} />
      <MessageActions text={text} at={at} hidden={streaming} />
    </div>
  );
}

/**
 * Paragraphs, each softening in once as it first appears.
 *
 * The animation deliberately lives at the line level rather than the word
 * level. Animating words looks worse in practice: a token arrives as a
 * fragment, the blur-in plays on that fragment, and then the fragment mutates
 * to the finished word with no transition — which reads as a stutter. It also
 * forces a swap from animated spans back to plain text when the stream ends,
 * and everything mid-flight snaps at once.
 *
 * A line mounts exactly once and then only grows, so nothing ever restarts or
 * snaps. Evenness of the text itself comes from the server, which releases
 * whole words on a steady tick (see `smoothStream` in the route).
 */
function StreamedText({ text }: { text: string }) {
  const reduceMotion = useReducedMotion();
  const lines = useMemo(() => text.split("\n").filter((line) => line.trim() !== ""), [text]);

  return (
    <div className="flex flex-col gap-3">
      {lines.map((line, index) => (
        <Line key={index} text={line} animate={!reduceMotion} />
      ))}
    </div>
  );
}

/**
 * At module scope so their identity is stable. A line re-renders on every
 * token of the reply — dozens of times a second — and handing Motion a fresh
 * object literal each time invites it to re-evaluate a target that has not
 * actually changed.
 */
const LINE_HIDDEN = { opacity: 0, filter: "blur(5px)", y: 4 };
const LINE_SHOWN = { opacity: 1, filter: "blur(0px)", y: 0 };
const LINE_TRANSITION = { duration: 0.42, ease: [0.22, 0.61, 0.36, 1] } as const;

/**
 * A settled line keeps `filter: blur(0px)` rather than losing the filter
 * altogether — Motion owns that property once it animates it, and a `style`
 * override does not win it back. At one filtered element per paragraph the
 * cost is invisible; it was only a problem when every word carried its own.
 *
 * memo matters here: without it every paragraph re-renders on each token of
 * the reply, which is wasted work for the lines that are already finished.
 */
const Line = memo(function Line({ text, animate }: { text: string; animate: boolean }) {
  return (
    <motion.p
      initial={animate ? LINE_HIDDEN : false}
      animate={LINE_SHOWN}
      transition={LINE_TRANSITION}
      className="text-body-regular break-words text-text-primary"
    >
      {text}
    </motion.p>
  );
});

function MessageActions({ text, at, hidden }: { text: string; at?: number; hidden: boolean }) {
  const [copied, setCopied] = useState(false);
  const [speaking, setSpeaking] = useState(false);

  useEffect(() => {
    if (!copied) return;
    const timer = setTimeout(() => setCopied(false), 1600);
    return () => clearTimeout(timer);
  }, [copied]);

  // Speech keeps running if the component goes away mid-sentence, so it is
  // cancelled on unmount rather than left talking over the next screen.
  useEffect(() => {
    return () => {
      if (typeof window !== "undefined") window.speechSynthesis?.cancel();
    };
  }, []);

  const copy = async () => {
    try {
      await navigator.clipboard.writeText(text);
      setCopied(true);
    } catch {
      // Clipboard access can be refused (insecure origin, denied permission).
      // Silently leaving the icon unchanged is better than a thrown error.
    }
  };

  const toggleSpeech = () => {
    const synth = typeof window === "undefined" ? undefined : window.speechSynthesis;
    if (!synth) return;
    if (speaking) {
      synth.cancel();
      setSpeaking(false);
      return;
    }
    const utterance = new SpeechSynthesisUtterance(text);
    utterance.onend = () => setSpeaking(false);
    utterance.onerror = () => setSpeaking(false);
    synth.cancel();
    synth.speak(utterance);
    setSpeaking(true);
  };

  return (
    <div
      className={cx(
        "flex items-center gap-1 transition-opacity duration-150",
        // Revealed on hover, and whenever a control inside has keyboard focus.
        hidden
          ? "pointer-events-none opacity-0"
          : "opacity-0 group-hover/message:opacity-100 focus-within:opacity-100",
      )}
    >
      <ActionButton label={copied ? "Copied" : "Copy message"} onClick={copy}>
        {copied ? <RiCheckLine className="size-4" aria-hidden /> : <RiFileCopyLine className="size-4" aria-hidden />}
      </ActionButton>

      <ActionButton label={speaking ? "Stop reading aloud" : "Read aloud"} onClick={toggleSpeech}>
        {speaking ? (
          <RiVolumeMuteLine className="size-4" aria-hidden />
        ) : (
          <RiVolumeUpLine className="size-4" aria-hidden />
        )}
      </ActionButton>

      {at ? (
        <time dateTime={new Date(at).toISOString()} className="ml-1 text-caption-1-regular text-text-tertiary">
          {formatAgo(at)}
        </time>
      ) : null}
    </div>
  );
}

function ActionButton({
  label,
  onClick,
  children,
}: {
  label: string;
  onClick: () => void;
  children: ReactNode;
}) {
  return (
    <button
      type="button"
      onClick={onClick}
      aria-label={label}
      title={label}
      className="flex size-7 cursor-pointer items-center justify-center rounded-md text-foreground-icon-tertiary transition-colors hover:bg-background-primary-default hover:text-foreground-icon-secondary focus-visible:ring-2 focus-visible:ring-border-focus-ring focus-visible:outline-none"
    >
      {children}
    </button>
  );
}

/** "just now", "3 minutes ago", "2 hours ago" — matching how the rail reads. */
function formatAgo(at: number) {
  const seconds = Math.max(0, Math.round((Date.now() - at) / 1000));
  if (seconds < 45) return "just now";
  const minutes = Math.round(seconds / 60);
  if (minutes < 60) return `${minutes} minute${minutes === 1 ? "" : "s"} ago`;
  const hours = Math.round(minutes / 60);
  if (hours < 24) return `${hours} hour${hours === 1 ? "" : "s"} ago`;
  const days = Math.round(hours / 24);
  return `${days} day${days === 1 ? "" : "s"} ago`;
}
1d:T674e,"use client";

import { useChat } from "@ai-sdk/react";
import { RiCloseLine, RiMenuLine } from "@remixicon/react";
import { DefaultChatTransport, type UIMessage } from "ai";

import { Button } from "../../base/buttons/button";
import { DemoTransport, SwitchingTransport } from "./demo-transport";
import { useCallback, useEffect, useMemo, useRef, useState } from "react";

import { AgentChatActions } from "./agent-chat-actions";
import {
  AgentChatHistory,
  type ChatThreadSummary,
} from "./agent-chat-history";
import { AgentMessage } from "./agent-chat-message";
import { AgentComposer } from "./agent-composer";
import { AgentThinking } from "../agent-thinking/agent-thinking";
import { starterNav, useStarterBase } from "../app-shell/app-shell";
import { DashboardSidebar } from "../dashboard/dashboard-sidebar";
import { IconButton } from "../../base/buttons/icon-button";
import { cx } from "../../utils/cx";

/**
 * A working chat app, wired to the `/api/chat` runtime.
 *
 * Built only from free components — the free `sidebar`, AgentThinking,
 * ComposerLoader, IconButton — so the whole screen can ship in a public
 * starter repo without giving away anything sold as Pro. It follows the Pro AI
 * chat template's geometry (12px page frame, 16px gap, the 260px sidebar card,
 * a rounded-3xl chat surface) so a starter and a purchased template read as
 * the same product.
 *
 * Any later chat screen that streams from a model should read the same
 * message shape from the same endpoint, so it can replace this one without
 * touching the backend.
 */

const ENDPOINT = "/api/chat";
const STORAGE_KEY = "boardui:agent-chat-threads";
/** Remembered so a reload of a keyless deploy lands back in the demo, not on the notice. */
const DEMO_KEY = "boardui:agent-chat-demo";
/** Enough history to be useful without letting localStorage grow unbounded. */
const MAX_THREADS = 30;

const SUGGESTIONS = [
  "Explain what this starter does",
  "Write a product update in three sentences",
  "Give me five names for a scheduling app",
];

interface StoredThread extends ChatThreadSummary {
  messages: UIMessage[];
  /** Message id -> when it arrived, so a restored chat shows real times
   *  rather than reporting every message as having just landed. */
  messageAt: Record<string, number>;
}

type Probe = {
  status: "checking" | "ready" | "unconfigured";
  provider: string | null;
  /** Display name from the runtime ("Anthropic"), so the UI needs no list of its own. */
  label: string | null;
  model: string | null;
};

export function AgentChat({
  className,
  contained = false,
}: {
  className?: string;
  /** Docs preview mode: fill the preview frame's height instead of the
   *  viewport, and keep the phone nav drawer inside the frame. */
  contained?: boolean;
}) {
  const [probe, setProbe] = useState<Probe>({ status: "checking", provider: null, label: null, model: null });
  const [input, setInput] = useState("");
  const [navOpen, setNavOpen] = useState(false);
  // The sidebar links only to pages the starter has; see app-shell.
  const navItems = starterNav(useStarterBase());
  const [threads, setThreads] = useState<StoredThread[]>([]);
  // Empty until the mount effect assigns one: an id generated during render
  // would differ between server and client and break hydration.
  const [activeId, setActiveId] = useState("");
  /** Stamped on send, so render never calls Date.now(). */
  const [activeUpdatedAt, setActiveUpdatedAt] = useState(0);
  /** Set when the open thread is renamed. Without it the derived title
   *  (built from the first message) would immediately overwrite the name. */
  const [activeTitle, setActiveTitle] = useState<string | null>(null);
  const scrollRef = useRef<HTMLDivElement>(null);
  const titleRef = useRef<HTMLSpanElement>(null);
  const columnRef = useRef<HTMLDivElement>(null);
  /** When each message arrived, for the hover timestamp. */
  const [messageAt, setMessageAt] = useState<Record<string, number>>({});
  /** True once the transcript is scrolled at all. */
  const [scrolledUnder, setScrolledUnder] = useState(false);
  /** Whether the title and the message column actually overlap horizontally.
   *  On a wide screen the column is centred well clear of a short title, so
   *  text passing behind the header never touches it and a rule would be
   *  drawing a line under nothing. */
  const [canClash, setCanClash] = useState(false);
  /** Guards the writer: before the restore below runs, `threads` is empty,
   *  and persisting that would erase stored history on every page load. */
  const [restored, setRestored] = useState(false);

  // Demo mode: the visitor skipped the key notice. Only meaningful while the
  // runtime reports no key; the moment a key exists, the real route answers.
  const [demo, setDemo] = useState(false);
  const demoActive = demo && probe.status === "unconfigured";
  const [transport] = useState(
    () => new SwitchingTransport(new DefaultChatTransport({ api: ENDPOINT }), new DemoTransport()),
  );
  useEffect(() => {
    try {
      // eslint-disable-next-line react-hooks/set-state-in-effect -- restoring from localStorage after mount; there is no render-time source for it
      setDemo(window.localStorage.getItem(DEMO_KEY) === "on");
    } catch {
      // Storage can be blocked; the notice simply shows again.
    }
  }, []);
  const enterDemo = () => {
    setDemo(true);
    try {
      window.localStorage.setItem(DEMO_KEY, "on");
    } catch {
      // Nothing to remember without storage.
    }
  };

  const { messages, sendMessage, setMessages, status, stop, error } = useChat({
    transport,
  });

  const busy = status === "submitted" || status === "streaming";

  // Reasoning models stream their thinking before any text, and the SDK flips
  // status to "streaming" on that first non-text chunk. Gating the indicator on
  // "submitted" alone therefore hides it seconds before a word appears, leaving
  // an empty transcript. It stays up until real text exists.
  const streamedText = useMemo(() => {
    const last = messages[messages.length - 1];
    if (!last || last.role !== "assistant") return "";
    return last.parts
      .filter((part) => part.type === "text")
      .map((part) => part.text)
      .join("");
  }, [messages]);
  const showThinking = busy && streamedText.length === 0;

  /** The open chat as plain text, for the share control. */
  const transcript = useMemo(
    () =>
      messages
        .map((message) => `${message.role === "user" ? "You" : "Assistant"}: ${messageText(message)}`)
        .filter((line) => !line.endsWith(": "))
        .join("\n\n"),
    [messages],
  );

  // Ask the runtime what it has before the first message, so a deployment with
  // no key renders setup steps instead of letting someone type into a dead box.
  useEffect(() => {
    let cancelled = false;
    fetch(ENDPOINT, { method: "GET" })
      .then((res) => (res.ok ? res.json() : null))
      .then((data: { configured?: boolean; provider?: string; providerLabel?: string; model?: string } | null) => {
        if (cancelled) return;
        // Only an explicit `configured: false` means "no key". A failed or
        // unreadable probe says nothing about the key, and treating it as
        // missing would hide the composer behind setup steps for a fault that
        // has nothing to do with configuration.
        setProbe({
          status: data && data.configured === false ? "unconfigured" : "ready",
          provider: data?.provider ?? null,
          label: data?.providerLabel ?? null,
          model: data?.model ?? null,
        });
      })
      .catch(() => {
        // A failed probe shouldn't lock the composer — let the send attempt
        // surface the real error rather than guessing the cause here.
        if (!cancelled) setProbe({ status: "ready", provider: null, label: null, model: null });
      });
    return () => {
      cancelled = true;
    };
  }, []);

  // Restore history, then open on a fresh thread rather than reviving the last
  // one — landing mid-conversation after a reload is more surprising than
  // useful, and the rail is right there. localStorage cannot be read during
  // render (it does not exist while server-rendering), so this is the one
  // place state has to be set from an effect.
  useEffect(() => {
    // eslint-disable-next-line react-hooks/set-state-in-effect -- restoring from localStorage after mount; there is no render-time source for it
    setActiveId(newThreadId());
    setThreads(orderThreads(readThreads()));
    setRestored(true);
  }, []);

  // The thread being typed into is derived rather than stored, so its title
  // appears as soon as the first message lands without a state round-trip.
  // The timestamp is stamped when a message is sent rather than read during
  // render, so rendering stays pure and the badge does not drift each frame.
  const liveThread: StoredThread | null = useMemo(
    () =>
      activeId && messages.length > 0
        ? {
            id: activeId,
            title: activeTitle ?? deriveTitle(messages),
            updatedAt: activeUpdatedAt,
            messages,
            messageAt,
          }
        : null,
    [activeId, messages, activeUpdatedAt, messageAt, activeTitle],
  );

  // Ordered by last message, never by what is open: selecting a thread must
  // not move it, or the list reshuffles under the pointer as you browse it.
  const allThreads = useMemo(() => {
    const rest = threads.filter((thread) => thread.id !== activeId);
    return orderThreads(liveThread ? [liveThread, ...rest] : rest);
  }, [liveThread, threads, activeId]);

  // Persist whenever the list settles. Held back during a stream so it is not
  // rewritten on every token, and until the restore above has run so an empty
  // first render cannot clobber stored history.
  useEffect(() => {
    if (!restored || busy) return;
    writeThreads(allThreads);
  }, [restored, busy, allThreads]);

  // Arrival times come from outside React (they are a property of when the
  // stream delivered each message), so they are recorded rather than derived.
  useEffect(() => {
    const unseen = messages.filter((message) => messageAt[message.id] === undefined);
    if (unseen.length === 0) return;
    const now = Date.now();
    // eslint-disable-next-line react-hooks/set-state-in-effect -- restoring from localStorage after mount; there is no render-time source for it
    setMessageAt((prev) => {
      const next = { ...prev };
      for (const message of unseen) next[message.id] = now;
      return next;
    });
  }, [messages, messageAt]);

  // Geometry, not a breakpoint: the title is as wide as its text, so whether
  // it reaches the column depends on both and has to be measured.
  useEffect(() => {
    const measure = () => {
      const title = titleRef.current?.getBoundingClientRect();
      const column = columnRef.current?.getBoundingClientRect();
      if (!title || !column) return;
      // A 16px buffer so text never slides right up against the title.
      setCanClash(title.right + 16 > column.left);
    };
    measure();
    const observer = new ResizeObserver(measure);
    if (scrollRef.current) observer.observe(scrollRef.current);
    return () => observer.disconnect();
  }, [messages.length, activeId]);

  useEffect(() => {
    scrollRef.current?.scrollTo({ top: scrollRef.current.scrollHeight, behavior: "smooth" });
  }, [messages, busy]);

  const submit = (text: string) => {
    const trimmed = text.trim();
    if (!trimmed || busy) return;
    setActiveUpdatedAt(Date.now());
    sendMessage({ text: trimmed }, { body: { demo: demoActive } });
    setInput("");
  };

  /** Folds the thread being left back into the stored list before switching.
   *  Its timestamp is carried over untouched, so leaving a thread never
   *  changes where it sits in the list. */
  const keepLiveThread = useCallback(() => {
    if (!liveThread) return;
    setThreads((prev) =>
      orderThreads([liveThread, ...prev.filter((thread) => thread.id !== liveThread.id)]),
    );
  }, [liveThread]);

  const startNewChat = useCallback(() => {
    if (busy) return;
    keepLiveThread();
    setActiveId(newThreadId());
    setActiveUpdatedAt(0);
    setActiveTitle(null);
    setMessageAt({});
    setMessages([]);
    setInput("");
  }, [busy, keepLiveThread, setMessages]);

  const renameThread = useCallback(
    (id: string, title: string) => {
      if (id === activeId) setActiveTitle(title);
      setThreads((prev) =>
        prev.map((thread) => (thread.id === id ? { ...thread, title } : thread)),
      );
    },
    [activeId],
  );

  const toggleUnread = useCallback((id: string) => {
    setThreads((prev) =>
      prev.map((thread) => (thread.id === id ? { ...thread, unread: !thread.unread } : thread)),
    );
  }, []);

  const deleteThread = useCallback(
    (id: string) => {
      const remaining = threads.filter((thread) => thread.id !== id);
      setThreads(remaining);
      // Deleting the thread you are reading leaves nothing to read, so the
      // pane opens a fresh one rather than showing a transcript with no row.
      if (id === activeId) {
        setActiveId(newThreadId());
        setActiveUpdatedAt(0);
        setActiveTitle(null);
        setMessageAt({});
        setMessages([]);
        setInput("");
      }
    },
    [threads, activeId, setMessages],
  );

  /** Hands the stored chats back as a file. Nothing leaves the browser: the
   *  blob is built locally and revoked straight after the click. */
  const exportThreads = useCallback(() => {
    if (allThreads.length === 0) return;
    const blob = new Blob([JSON.stringify(allThreads, null, 2)], { type: "application/json" });
    const url = URL.createObjectURL(blob);
    const link = document.createElement("a");
    link.href = url;
    link.download = "boardui-chats.json";
    // In the document because some browsers ignore click() on a detached
    // anchor, and revoked a tick later rather than immediately: revoking in the
    // same turn can kill the URL before the browser has read the blob, which
    // saves an empty file or nothing at all.
    document.body.appendChild(link);
    link.click();
    link.remove();
    setTimeout(() => URL.revokeObjectURL(url), 0);
  }, [allThreads]);

  const selectThread = useCallback(
    (id: string) => {
      if (busy || id === activeId) return;
      const thread = allThreads.find((candidate) => candidate.id === id);
      if (!thread) return;
      keepLiveThread();
      // Reading a chat is what marks it read again.
      if (thread.unread) toggleUnread(id);
      setActiveId(id);
      setActiveUpdatedAt(thread.updatedAt);
      setActiveTitle(thread.title);
      setMessageAt(thread.messageAt ?? {});
      setMessages(thread.messages);
      setInput("");
    },
    [busy, activeId, allThreads, keepLiveThread, toggleUnread, setMessages],
  );

  return (
    <div
      className={cx(
        "relative flex w-full gap-4 overflow-hidden bg-background-full p-3",
        contained ? "h-[var(--template-preview-height)]" : "h-dvh",
        className,
      )}
    >
      <DashboardSidebar items={navItems} selected="chat" className="hidden lg:flex" />

      {/* Below lg the sidebar rides in as an overlay drawer. The Pro template
          pushes the workspace across instead; this is the plain version. */}
      {navOpen && (
        <div className={cx(contained ? "absolute" : "fixed", "inset-0 z-50 flex lg:hidden")}>
          <button
            type="button"
            aria-label="Close navigation"
            onClick={() => setNavOpen(false)}
            className="absolute inset-0 cursor-pointer bg-black/40"
          />
          <div className="relative flex h-full p-3">
            <DashboardSidebar
              mobile
              items={navItems}
              selected="chat"
              onClose={() => setNavOpen(false)}
              className="flex"
            />
          </div>
        </div>
      )}

      <div className="relative flex min-h-0 min-w-0 flex-1 gap-3 overflow-hidden">
        <div className="relative flex h-full min-w-0 flex-1 flex-col overflow-hidden rounded-3xl bg-background-secondary-default">
            <header
              className={cx(
                "absolute inset-x-0 top-0 z-10 flex h-12 items-center gap-2 border-b px-4 pt-[7px] transition-colors",
                // Overlaid rather than stacked above the transcript. Stacked,
                // the scroll area began below it and text was cut off at the
                // header's edge; overlaid, it runs to the container's own
                // rounded top and is clipped by that instead.
                // A rule only earns its place when the text both passes under
                // the header AND the title is wide enough to be in its way.
                // The border is always present but transparent, so appearing
                // costs no height and nothing shifts.
                // When it does clash the band frosts over, so the text passing
                // behind it is obscured instead of running through the title.
                // Tied to the same condition as the rule: with no clash the
                // header stays fully transparent and the transcript reads all
                // the way up to the card's edge.
                scrolledUnder && canClash
                  ? "border-separator-border bg-white/20 backdrop-blur-[20px] dark:bg-black/20"
                  : "border-transparent",
              )}
            >
              <IconButton
                icon={navOpen ? RiCloseLine : RiMenuLine}
                size="small"
                aria-label="Open navigation"
                onClick={() => setNavOpen((open) => !open)}
                className="lg:hidden"
              />
              <span
                ref={titleRef}
                className="min-w-0 shrink truncate text-headline-medium text-text-primary"
              >
                {headerTitle(allThreads, activeId)}
              </span>

              <AgentChatActions
                className="ml-auto"
                transcript={transcript}
                onExport={exportThreads}
                onToggleUnread={() => toggleUnread(activeId)}
                onDelete={() => deleteThread(activeId)}
                disabled={messages.length === 0}
              />
            </header>

            {probe.status === "unconfigured" && messages.length === 0 && !demo ? (
              <SetupNotice onSkip={enterDemo} />
            ) : (
              <>
                <div
                  ref={scrollRef}
                  onScroll={(event) => setScrolledUnder(event.currentTarget.scrollTop > 0)}
                  className="min-h-0 flex-1 overflow-y-auto scroll-smooth"
                >
                  <div
                    ref={columnRef}
                    className={cx(
                      "mx-auto flex w-full max-w-3xl flex-col gap-5 px-4 pt-[72px] pb-6",
                      // With nothing to read, the prompt sits in the middle of
                      // the empty space rather than pinned under the header.
                      messages.length === 0 && "min-h-full justify-center",
                    )}
                  >
                    {messages.length === 0 ? (
                      <EmptyState onPick={submit} demo={demoActive} />
                    ) : (
                      messages.map((message, index) => (
                        <AgentMessage
                          key={message.id}
                          role={message.role}
                          text={messageText(message)}
                          streaming={busy && index === messages.length - 1}
                          at={messageAt[message.id]}
                        />
                      ))
                    )}

                    {showThinking && (
                      <AgentThinking variant="wave" label="Thinking" className="px-1" />
                    )}

                    {error && (
                      <p role="alert" className="px-1 text-body-regular text-text-tertiary">
                        Something went wrong. Check the server logs, then try again.
                      </p>
                    )}
                  </div>
                </div>

                <div className="shrink-0 px-3 pb-3">
                  <div className="mx-auto w-full max-w-3xl">
                    <AgentComposer
                      value={input}
                      onValueChange={setInput}
                      onSubmit={() => submit(input)}
                      onStop={stop}
                      busy={busy}
                      model={probe.model}
                      provider={demoActive ? "Demo mode" : (probe.label ?? probe.provider)}
                      messageCount={messages.length}
                    />
                  </div>
                </div>
              </>
            )}
        </div>

        {(probe.status !== "unconfigured" || messages.length > 0 || demo) && (
            <AgentChatHistory
              threads={allThreads}
              activeId={activeId}
              onSelect={selectThread}
              onNewChat={startNewChat}
              onRename={renameThread}
              onToggleUnread={toggleUnread}
              onDelete={deleteThread}
              onExport={exportThreads}
            disabled={busy}
            className="hidden xl:flex"
          />
        )}
      </div>
      {/* Fixed to the viewport, so not inside the docs preview frame. */}
    </div>
  );
}

function EmptyState({ onPick, demo = false }: { onPick: (text: string) => void; demo?: boolean }) {
  return (
    <div className="flex flex-col items-center gap-4 text-center">
      <div className="flex flex-col gap-1">
        <h2 className="text-title-2-medium text-text-primary">What can I help with?</h2>
        <p className="text-body-regular text-text-secondary">
          {demo
            ? "Demo mode: the answers are scripted. Add AI_API_KEY for a real model."
            : "This chat runs against your own API key. History stays in this browser."}
        </p>
      </div>
      <div className="flex flex-wrap justify-center gap-2">
        {SUGGESTIONS.map((suggestion) => (
          <button
            key={suggestion}
            type="button"
            onClick={() => onPick(suggestion)}
            className="cursor-pointer rounded-full bg-background-primary-default px-3.5 py-2 text-body-regular text-text-secondary shadow-xs transition-colors hover:bg-background-primary-hover"
          >
            {suggestion}
          </button>
        ))}
      </div>
    </div>
  );
}

/**
 * Shown when the runtime reports no provider key. Deliberately specific about
 * where the key goes, because the person seeing this has usually just clicked
 * Deploy and has no context for where that setting lives.
 */
function SetupNotice({ onSkip }: { onSkip: () => void }) {
  return (
    <div className="flex min-h-0 flex-1 items-center justify-center p-6">
      <div className="flex max-w-md flex-col gap-3 rounded-2xl bg-background-primary-default p-6 shadow-card">
        <h2 className="text-headline-medium text-text-primary">Add an API key to start</h2>
        <p className="text-body-regular text-text-secondary">
          The chat is wired up and ready. It needs a model provider key before it can answer.
        </p>
        <ol className="flex list-decimal flex-col gap-1.5 pl-5 text-body-regular text-text-secondary">
          <li>Open your project on Vercel, then Settings, then Environment Variables.</li>
          <li>
            Add <code className="text-text-primary">AI_API_KEY</code> with a key from OpenAI,
            Anthropic, Google, OpenRouter, or Vercel AI Gateway.
          </li>
          <li>Redeploy.</li>
        </ol>
        <p className="text-caption-1-regular text-text-tertiary">
          Running locally? Put the same variable in <code>.env.local</code> and restart the dev
          server.
        </p>
        <Button variant="primary" size="medium" className="w-full" onClick={onSkip}>
          Skip, show me the demo
        </Button>
      </div>
    </div>
  );
}

/** Newest message first, and trimmed from the oldest end. */
function orderThreads(threads: StoredThread[]) {
  return [...threads].sort((a, b) => b.updatedAt - a.updatedAt).slice(0, MAX_THREADS);
}

/** The text parts of a message, joined. Reasoning and tool parts ride the
 *  same array and are ignored here; a richer screen would render them. */
function messageText(message: UIMessage) {
  return message.parts
    .filter((part) => part.type === "text")
    .map((part) => part.text)
    .join("");
}

function newThreadId() {
  // randomUUID needs a secure context; a deployed starter always has one, but
  // plain-http previews and older browsers do not.
  return globalThis.crypto?.randomUUID?.() ?? `t-${Date.now()}-${Math.random().toString(16).slice(2)}`;
}

function deriveTitle(messages: UIMessage[]) {
  const firstUser = messages.find((message) => message.role === "user");
  const text = firstUser?.parts
    .filter((part) => part.type === "text")
    .map((part) => part.text)
    .join("")
    .trim();
  if (!text) return "New chat";
  return text.length > 48 ? `${text.slice(0, 48)}...` : text;
}

function headerTitle(threads: ChatThreadSummary[], activeId: string) {
  return threads.find((thread) => thread.id === activeId)?.title ?? "New chat";
}

/** Storage is a convenience, never a source of truth — every access can throw
 *  (private browsing, disabled site data) and must not take the chat with it. */
function readThreads(): StoredThread[] {
  try {
    const raw = window.localStorage.getItem(STORAGE_KEY);
    if (!raw) return [];
    const parsed: unknown = JSON.parse(raw);
    if (!Array.isArray(parsed)) return [];
    return parsed.filter(
      (thread): thread is StoredThread =>
        typeof thread === "object" &&
        thread !== null &&
        typeof (thread as StoredThread).id === "string" &&
        Array.isArray((thread as StoredThread).messages),
    );
  } catch {
    return [];
  }
}

function writeThreads(threads: StoredThread[]) {
  try {
    window.localStorage.setItem(STORAGE_KEY, JSON.stringify(threads));
  } catch {
    // Quota or blocked storage: history is best-effort, the chat still works.
  }
}
1e:T1c40,"use client";

import {
  RiArrowUpLine,
  RiAttachment2,
  RiInfinityLine,
  RiSparklingLine,
  RiStopFill,
} from "@remixicon/react";
import { useRef, type FormEvent, type KeyboardEvent } from "react";

import { ComposerLoader } from "../composer-loader/composer-loader";
import { cx } from "../../utils/cx";

/**
 * The free composer: a 52px pill with a text field and circular controls,
 * plus the thin status row beneath it.
 *
 * It matches the proportions of the Pro composer on purpose, so a starter
 * looks like it belongs to the same product — the fixed-height pill, 8px
 * padding with 36px circles sitting flush inside it, and the 10px gap rhythm
 * across the row. What it deliberately does NOT carry is the paid work in
 * `composer`: the liquid-glass surfaces behind the controls, the model and
 * effort menus, the attachment plugin panel, and the voice equalizer. Those
 * are the reason someone buys that component, so this one leaves the slots
 * present but plain.
 *
 * The status row shows what is actually true of the running app — which
 * provider answered, which model, how many messages are in the thread —
 * rather than decorative chrome, so it stays honest in a deployed starter.
 */

export interface AgentComposerProps {
  value: string;
  onValueChange: (value: string) => void;
  onSubmit: () => void;
  onStop: () => void;
  busy: boolean;
  /** Model id reported by the runtime, e.g. "openai/gpt-5-nano". */
  model?: string | null;
  provider?: string | null;
  messageCount: number;
  className?: string;
}

export function AgentComposer({
  value,
  onValueChange,
  onSubmit,
  onStop,
  busy,
  model,
  provider,
  messageCount,
  className,
}: AgentComposerProps) {
  const inputRef = useRef<HTMLInputElement>(null);

  const submit = (event: FormEvent) => {
    event.preventDefault();
    onSubmit();
  };

  const onKeyDown = (event: KeyboardEvent<HTMLInputElement>) => {
    if (event.key === "Enter" && !event.shiftKey) {
      event.preventDefault();
      onSubmit();
    }
  };

  return (
    <div className={cx("flex w-full flex-col gap-2.5", className)}>
      {/* radius omitted: ComposerLoader defaults to a full pill, matching the form. */}
      <ComposerLoader active={busy}>
        {/* The pill sits one step lighter than the chat surface behind it in
            both themes (white on neutral-100 in light, neutral-800 on
            neutral-900 in dark), so a single token works without a dark
            override. Its controls then step back down to secondary. */}
        <form
          onSubmit={submit}
          className={cx(
            "flex h-[52px] w-full items-center gap-2.5 rounded-full p-2",
            // ComposerLoader paints its own pill surface and runs the light
            // behind its children, so an opaque form would cover the light
            // entirely. While busy the form steps aside and lets it show.
            busy ? "bg-transparent" : "bg-background-primary-default shadow-xs",
          )}
        >
          {/* Attachment slot. Plain on purpose — the plugin panel behind this
              button in the Pro composer is part of what that component sells. */}
          {/* Shares the Pro composer's add-button tokens so the two read as the
              same control. They resolve one step lighter than the pill in dark
              and one step darker in light, which is why a single pair works for
              both themes without a dark: override. */}
          <button
            type="button"
            aria-label="Add attachment"
            className="flex size-9 shrink-0 cursor-pointer items-center justify-center rounded-full bg-ai-chat-composer-add-background text-foreground-icon-primary transition-colors duration-150 ease hover:bg-ai-chat-composer-add-hover-background"
          >
            <RiAttachment2 className="size-5" aria-hidden />
          </button>

          <label className="sr-only" htmlFor="agent-composer-input">
            Message
          </label>
          <input
            id="agent-composer-input"
            ref={inputRef}
            type="text"
            value={value}
            onChange={(event) => onValueChange(event.target.value)}
            onKeyDown={onKeyDown}
            placeholder="Ask me anything"
            autoComplete="off"
            className="h-5 min-w-0 flex-1 bg-transparent text-body-regular text-text-primary caret-text-primary outline-none placeholder:text-text-tertiary"
          />

          {model ? (
            <span
              // Static where the Pro composer puts its model picker: the model
              // is set by an env var, so there is nothing to switch at runtime.
              className="flex h-8 shrink-0 items-center gap-1 rounded-xl py-1.5 pr-2 pl-2 text-body-2-medium text-text-secondary"
              title={`Answering with ${model}`}
            >
              <RiSparklingLine className="size-4 shrink-0 text-foreground-icon-secondary" aria-hidden />
              <span className="hidden max-w-[13ch] truncate sm:inline">{shortModel(model)}</span>
            </span>
          ) : null}

          <div className="flex shrink-0 items-center gap-2 pl-1.5">
            {busy ? (
              <button
                type="button"
                onClick={onStop}
                aria-label="Stop generating"
                className="flex size-9 cursor-pointer items-center justify-center rounded-full bg-background-secondary-default text-foreground-icon-secondary transition-colors hover:bg-background-secondary-hover"
              >
                <RiStopFill className="size-5" aria-hidden />
              </button>
            ) : (
              <button
                type="submit"
                aria-label="Send message"
                disabled={value.trim().length === 0}
                className="flex size-9 cursor-pointer items-center justify-center rounded-full bg-button-primary text-white transition-opacity disabled:cursor-not-allowed disabled:opacity-40"
              >
                <RiArrowUpLine className="size-5" aria-hidden />
              </button>
            )}
          </div>
        </form>
      </ComposerLoader>

      <div className="flex h-[26px] w-full items-center justify-between">
        <div className="flex items-center gap-3">
          <StatusItem icon={RiInfinityLine} label={provider ?? "Not configured"} />
        </div>
        <div className="flex items-center gap-3">
          <StatusItem
            icon={RiSparklingLine}
            label={messageCount === 0 ? "New chat" : `${messageCount} messages`}
          />
        </div>
      </div>
    </div>
  );
}

function StatusItem({
  icon: Icon,
  label,
}: {
  icon: typeof RiInfinityLine;
  label: string;
}) {
  return (
    <span className="flex items-center gap-1">
      <Icon className="size-4 shrink-0 text-foreground-icon-secondary" aria-hidden />
      <span className="whitespace-nowrap text-body-2-medium text-text-secondary">{label}</span>
    </span>
  );
}

/** "openai/gpt-5-nano" reads better in a 13ch slot as "gpt-5-nano". */
function shortModel(model: string) {
  const slash = model.lastIndexOf("/");
  return slash === -1 ? model : model.slice(slash + 1);
}
1f:Tb49,/**
 * What the chat says when there is no model behind it.
 *
 * Demo mode exists so a fresh deploy can be tried before a key is added: the
 * screen, the streaming, the history all behave as they do with a model, and
 * only the words are canned. Each answer says so, quietly, so nobody mistakes
 * it for a model's.
 */

type DemoAnswer = { match: RegExp; reply: string };

const ANSWERS: DemoAnswer[] = [
  {
    match: /starter|what (is|does) this|about this app|boardui/i,
    reply:
      "This is the BoardUI chat starter: a Next.js app with a streaming chat, a dashboard, an inbox and sign-in screens, all built from BoardUI's free components and installed as source you own. Right now I'm in demo mode, so this answer is canned. Add AI_API_KEY in your Vercel project settings, redeploy, and the same chat talks to a real model.",
  },
  {
    match: /product update|announcement|release notes|changelog/i,
    reply:
      "Here's a product update in three sentences. This release adds a dashboard with revenue and orders charts, an inbox, and sign-in and sign-up screens, all built from the same free components as the chat. Every page installs as source, so nothing is locked behind a package. It deploys to Vercel in a click and runs on your own model key. (Demo mode: canned answer. Add AI_API_KEY for a real one.)",
  },
  {
    match: /names?\b.*\b(app|product|scheduling|startup|company)|name ideas|suggest.*names/i,
    reply:
      "Five names for a scheduling app: Slotwise, Tidemark, Cadence, Dayline, and Meridian. Slotwise says what it does, Cadence and Meridian carry a rhythm, Tidemark and Dayline feel like calendars without saying so. (Demo mode: canned answer. With a key, I'd tailor these to your product.)",
  },
  {
    match: /key|api|openrouter|openai|anthropic|provider|model/i,
    reply:
      "To connect a real model, add one environment variable, AI_API_KEY, in your Vercel project (Settings, then Environment Variables) and redeploy. The provider is read from the key itself: OpenAI, Anthropic, Google, OpenRouter, Groq, xAI or Vercel AI Gateway all work. Set CHAT_MODEL if you want a specific model. Until then I'm in demo mode, answering from a short script.",
  },
  {
    match: /hello|hi\b|hey|good (morning|afternoon|evening)/i,
    reply:
      "Hello. I'm the starter's demo assistant, answering from a short script until a model key is added. Try asking what this starter does, or how to connect a key.",
  },
];

const FALLBACK =
  "I'm in demo mode, so I can only answer from a short script, and that question isn't in it. Add AI_API_KEY in your Vercel project settings and redeploy, and this same chat will answer properly. Meanwhile, try: what does this starter do, or how do I connect a key.";

export function pickDemoAnswer(prompt: string): string {
  return ANSWERS.find((answer) => answer.match.test(prompt))?.reply ?? FALLBACK;
}
20:Tcc0,import type { ChatTransport, UIMessage, UIMessageChunk } from "ai";

import { pickDemoAnswer } from "./demo-answers";

/** A beat before the first word, so the thinking indicator reads as thinking. */
const THINK_MS = 700;
/** Per word. Slightly faster than a model, since there is nothing to wait for. */
const WORD_MS = 28;

const wait = (ms: number, signal?: AbortSignal) =>
  new Promise<void>((resolve, reject) => {
    const timer = setTimeout(resolve, ms);
    signal?.addEventListener("abort", () => {
      clearTimeout(timer);
      reject(new DOMException("Aborted", "AbortError"));
    });
  });

function lastUserText(messages: UIMessage[]): string {
  const last = [...messages].reverse().find((message) => message.role === "user");
  return (
    last?.parts
      .filter((part): part is { type: "text"; text: string } => part.type === "text")
      .map((part) => part.text)
      .join("\n") ?? ""
  );
}

/**
 * The chat transport for a deploy that has no model key yet. It speaks the
 * same UI-message stream the real route does, so `useChat` cannot tell the
 * difference: the words arrive one at a time, Stop works, and the thread is
 * saved like any other. Only the words are scripted.
 */
export class DemoTransport implements ChatTransport<UIMessage> {
  async sendMessages({
    messages,
    abortSignal,
  }: {
    messages: UIMessage[];
    abortSignal: AbortSignal | undefined;
  }): Promise<ReadableStream<UIMessageChunk>> {
    const reply = pickDemoAnswer(lastUserText(messages));
    const id = `demo-${Date.now()}`;
    return new ReadableStream<UIMessageChunk>({
      async start(controller) {
        try {
          controller.enqueue({ type: "start" });
          controller.enqueue({ type: "start-step" });
          await wait(THINK_MS, abortSignal);
          controller.enqueue({ type: "text-start", id });
          const words = reply.split(/(?<=\s)/);
          for (const word of words) {
            await wait(WORD_MS, abortSignal);
            controller.enqueue({ type: "text-delta", id, delta: word });
          }
          controller.enqueue({ type: "text-end", id });
          controller.enqueue({ type: "finish-step" });
          controller.enqueue({ type: "finish" });
        } catch {
          controller.enqueue({ type: "abort" });
        } finally {
          controller.close();
        }
      },
    });
  }

  async reconnectToStream(): Promise<ReadableStream<UIMessageChunk> | null> {
    return null;
  }
}

/**
 * Holds both transports and answers per request: a send carrying
 * `body.demo` goes to the script, anything else to the real route. Nothing is
 * mutated after construction, so `useChat` keeps one instance for good.
 */
export class SwitchingTransport implements ChatTransport<UIMessage> {
  constructor(
    private readonly live: ChatTransport<UIMessage>,
    private readonly canned: ChatTransport<UIMessage>,
  ) {}

  sendMessages: ChatTransport<UIMessage>["sendMessages"] = (options) => {
    const demo = Boolean((options.body as { demo?: boolean } | undefined)?.demo);
    return (demo ? this.canned : this.live).sendMessages(options);
  };

  reconnectToStream: ChatTransport<UIMessage>["reconnectToStream"] = (options) =>
    this.live.reconnectToStream(options);
}
18:["$","$L19",null,{"item":{"$schema":"https://myzeusui.com/schema/registry-item.json","name":"agent-chat","title":"Agent Chat","description":"A working chat app: thread history, streaming messages, composer and suggestions.","category":"blocks","family":"ai","kind":"block","designSystem":"zeus","foundationDependencies":["tokens"],"iconLibraries":["remix"],"tags":["ai","chat","agent","streaming","ai-sdk","composer"],"status":"stable","tier":"free","dependencies":["@ai-sdk/react","@internationalized/date","@remixicon/react","ai","motion","react-aria-components","tailwind-merge"],"registryDependencies":["agent-thinking","app-shell","avatar","badge","breadcrumb","button","checkbox","chevrons","chip","close-button","composer-loader","cx","date-picker","dropdown","file-upload","icon-button","input","kbd","notification-center","pagination","segmented-control","select","settings-modal","sidebar","switch","tabs","theme-toggle","tokens","tooltip","use-dismiss-on-outside-press"],"files":[{"path":"application/agent-chat/agent-chat-actions.tsx","target":"components/application/agent-chat/agent-chat-actions.tsx","type":"component","content":"$1a"},{"path":"application/agent-chat/agent-chat-history.tsx","target":"components/application/agent-chat/agent-chat-history.tsx","type":"component","content":"$1b"},{"path":"application/agent-chat/agent-chat-message.tsx","target":"components/application/agent-chat/agent-chat-message.tsx","type":"component","content":"$1c"},{"path":"application/agent-chat/agent-chat.tsx","target":"components/application/agent-chat/agent-chat.tsx","type":"component","content":"$1d"},{"path":"application/agent-chat/agent-composer.tsx","target":"components/application/agent-chat/agent-composer.tsx","type":"component","content":"$1e"},{"path":"application/agent-chat/demo-answers.ts","target":"components/application/agent-chat/demo-answers.ts","type":"component","content":"$1f"},{"path":"application/agent-chat/demo-transport.ts","target":"components/application/agent-chat/demo-transport.ts","type":"component","content":"$20"},{"path":"hooks/use-pathname.ts","target":"hooks/use-pathname.ts","type":"hook","content":"\"use client\";\n\nimport { useSyncExternalStore } from \"react\";\n\nfunction subscribe(listener: () => void) {\n  window.addEventListener(\"popstate\", listener);\n  return () => window.removeEventListener(\"popstate\", listener);\n}\n\n/** Browser navigation without requiring a framework router provider. */\nexport function usePathname() {\n  return useSyncExternalStore(subscribe, () => window.location.pathname, () => \"\");\n}\n"}],"hash":"9b47c0340744"},"demoSource":"\"use client\";\n\nimport { AgentChat } from \"@/components/application/agent-chat/agent-chat\";\n\nexport default function AgentChatDemo() {\n  // `contained` fills the preview frame instead of the viewport.\n  return (\n    <div className=\"h-[640px] w-full overflow-hidden rounded-2lg border border-border-button-white\">\n      <AgentChat contained />\n    </div>\n  );\n}","previous":{"name":"agent-log","title":"Agent Log"},"next":{"name":"area-chart-card","title":"Area Chart"}}]
14:[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]
12:null
16:[["$","title","0",{"children":"Agent Chat — Zeus UI"}],["$","meta","1",{"name":"description","content":"A working chat app: thread history, streaming messages, composer and suggestions."}],["$","meta","2",{"name":"keywords","content":"ai,chat,agent,streaming,ai-sdk,composer"}]]
