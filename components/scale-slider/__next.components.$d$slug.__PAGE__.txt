1:"$Sreact.fragment"
2:I[99454,["/_next/static/chunks/0by1pam5w7cuf.js","/_next/static/chunks/0hdjbswbbz8h2.js","/_next/static/chunks/06es_ecb9le4q.js","/_next/static/chunks/0jocqdh7qodi4.js","/_next/static/chunks/0iw0to4qmrq4l.js","/_next/static/chunks/1ncyn83jriw3_.js","/_next/static/chunks/0-afrvt74ak7l.js","/_next/static/chunks/0337601gswv_s.js","/_next/static/chunks/34zl6f3dq0u0q.js","/_next/static/chunks/1wypk9kc4p_mt.js","/_next/static/chunks/3qu3p8mmx45cn.js","/_next/static/chunks/06kkgaxc-u2z8.js","/_next/static/chunks/2az_x4uxpy3sm.js"],"DocsLayout"]
12:X
17:X
17:C
0:{"buildId":"Up4wXEsPAulfj7Bbm1_DB","data":[{"rsc":["$","$1","c",{"children":[["$","$L2",null,{"title":"Scale Slider","heading":"Scale Slider","icon":"components","groups":[{"id":"buttons","title":"Buttons","items":[{"name":"button","title":"Button"},{"name":"icon-button","title":"Icon Button"},{"name":"link-button","title":"Link Button"},{"name":"button-group","title":"Button Group"},{"name":"close-button","title":"Close Button"},{"name":"social-button","title":"Social Button"}]},{"id":"forms","title":"Forms & inputs","items":[{"name":"input","title":"Input"},{"name":"input-otp","title":"Input OTP"},{"name":"checkbox","title":"Checkbox"},{"name":"radio","title":"Radio"},{"name":"switch","title":"Switch"},{"name":"select","title":"Select"},{"name":"slider","title":"Slider"},{"name":"file-upload","title":"File Upload"},{"name":"auth-card","title":"Auth Card"}]},{"id":"navigation","title":"Navigation","items":[{"name":"multi-level-drawer-menu","title":"Multi-Level Drawer Menu","isNew":true},{"name":"dropdown","title":"Dropdown"},{"name":"tabs","title":"Tabs"},{"name":"segmented-control","title":"Segmented Control"},{"name":"breadcrumb","title":"Breadcrumb"},{"name":"pagination","title":"Pagination"},{"name":"sidebar","title":"Sidebar"}]},{"id":"feedback","title":"Feedback & status","items":[{"name":"badge","title":"Badge"},{"name":"chip","title":"Chip"},{"name":"status-dot","title":"Status Dot"},{"name":"announcement","title":"Announcement"},{"name":"notification","title":"Notification"},{"name":"theme-toggle","title":"Theme Toggle"},{"name":"notification-center","title":"Notification Center"}]},{"id":"overlays","title":"Overlays & disclosures","items":[{"name":"morphing-tooltip","title":"Morphing Tooltip","isNew":true},{"name":"animated-accordion","title":"Animated Accordion","isNew":true},{"name":"feature-dialog","title":"Feature Dialog","isNew":true},{"name":"tooltip","title":"Tooltip"},{"name":"settings-modal","title":"Settings Modal"}]},{"id":"typography","title":"Text & typography","items":[{"name":"text-reveal","title":"Text Reveal","isNew":true},{"name":"dual-scramble","title":"Dual Scramble","isNew":true},{"name":"typography","title":"Typography"},{"name":"kbd","title":"Kbd"}]},{"id":"icons","title":"Icons & identity","items":[{"name":"chevrons","title":"Chevrons"},{"name":"logo","title":"Logo"},{"name":"avatar","title":"Avatar"}]},{"id":"layout","title":"Layout & structure","items":[{"name":"divider","title":"Divider"},{"name":"app-shell","title":"App Shell"}]},{"id":"data","title":"Data & charts","items":[{"name":"combo-chart-card","title":"Combo Chart","isNew":true},{"name":"contributions-card","title":"Contributions","isNew":true},{"name":"earnings-chart-card","title":"Earnings Chart","isNew":true},{"name":"funnel-chart-card","title":"Funnel Chart","isNew":true},{"name":"heatmap-chart-card","title":"Heatmap Chart","isNew":true},{"name":"most-active-days-card","title":"Most Active Days","isNew":true},{"name":"radar-chart-card","title":"Radar Chart","isNew":true},{"name":"radial-chart-card","title":"Radial Chart","isNew":true},{"name":"sankey-chart-card","title":"Sankey Chart","isNew":true},{"name":"scatter-chart-card","title":"Scatter Chart","isNew":true},{"name":"sleep-score-card","title":"Sleep Score","isNew":true},{"name":"stage-bars-card","title":"Stage Bars","isNew":true},{"name":"steps-card","title":"Steps Chart","isNew":true},{"name":"table","title":"Table"},{"name":"stat-cards","title":"Stat Cards"},{"name":"data-table","title":"Data Table"},{"name":"area-chart-card","title":"Area Chart","isNew":true},{"name":"line-chart-card","title":"Line Chart","isNew":true},{"name":"bar-list-card","title":"Bar List","isNew":true},{"name":"activity-rings-card","title":"Activity Rings","isNew":true},{"name":"chart-card","title":"Chart Card","isNew":true},{"name":"revenue-chart-card","title":"Revenue Chart"},{"name":"orders-chart-card","title":"Orders Chart"},{"name":"important-alerts-card","title":"Important Alerts"},{"name":"patient-info-card","title":"Patient Info"}]},{"id":"media","title":"Images & media","items":[{"name":"image-fly-in","title":"Image Fly-In","isNew":true},{"name":"scale-slider","title":"Scale Slider","isNew":true},{"name":"curtain-slider","title":"Curtain Slider","isNew":true},{"name":"ascii-overlay","title":"ASCII Overlay","isNew":true},{"name":"carousel","title":"Carousel"}]},{"id":"scheduling","title":"Dates & scheduling","items":[{"name":"calendar","title":"Calendar","isNew":true},{"name":"date-picker","title":"Date Picker"},{"name":"date-range-picker","title":"Date Range Picker"},{"name":"meeting-scheduler","title":"Meeting Scheduler"}]},{"id":"ai","title":"AI & agents","items":[{"name":"ai-profile","title":"AI Profile","isNew":true},{"name":"ai-chat","title":"AI Chat","isNew":true},{"name":"ai-image-generation","title":"AI Image Generation","isNew":true},{"name":"agent-progress","title":"Agent Progress","isNew":true},{"name":"composer","title":"Composer","isNew":true},{"name":"task-list","title":"Task List","isNew":true},{"name":"web-search","title":"Web Search","isNew":true},{"name":"agent-limits-card","title":"Agent Limits","isNew":true},{"name":"agent-thinking","title":"Agent Thinking"},{"name":"composer-loader","title":"Composer Loader"},{"name":"agent-log","title":"Agent Log"},{"name":"agent-chat","title":"Agent Chat"}]},{"id":"pricing","title":"Pricing & billing","items":[{"name":"animated-pricing","title":"Animated Pricing","isNew":true}]},{"id":"screens","title":"Complete screens","items":[{"name":"home-dashboard","title":"Home Dashboard","isNew":true},{"name":"finance-dashboard","title":"Finance Dashboard","isNew":true},{"name":"marketing-dashboard","title":"Marketing Dashboard","isNew":true},{"name":"hr-management","title":"HR Management","isNew":true},{"name":"medical-profile","title":"Medical Profile","isNew":true}]},{"id":"foundations","title":"Design foundations","items":[{"name":"tokens","title":"Design tokens"}]},{"id":"utilities","title":"Hooks & utilities","items":[{"name":"cx","title":"cx"},{"name":"use-count-up","title":"useCountUp"},{"name":"use-dismiss-on-outside-press","title":"useDismissOnOutsidePress"}]}],"description":"An exponential image conveyor with drag inertia, keyboard navigation and optional drift","detail":true,"children":"$L3"}],["$L4","$L5","$L6","$L7","$L8","$L9","$La","$Lb","$Lc","$Ld","$Le","$Lf"],"$L10"]}],"isPartial":"$@11","staleTime":"$12","varyParams":null},{"rsc":"$L13","isPartial":"$@14","staleTime":"$12","varyParams":null},{"rsc":"$L15","isPartial":"$@16","staleTime":"$12","varyParams":"$17"},{"rsc":"$L18","isPartial":"$@19","staleTime":"$12","varyParams":"$17"},{"rsc":"$L1a","isPartial":"$@1b","staleTime":"$12","varyParams":null}],"isUpgradeableISRFallback":false,"a":"$@1c","rootVaryParams":null,"needsRuntimeRequest":"$@1d"}
1e:I[68462,["/_next/static/chunks/0by1pam5w7cuf.js","/_next/static/chunks/0hdjbswbbz8h2.js","/_next/static/chunks/06es_ecb9le4q.js","/_next/static/chunks/0jocqdh7qodi4.js","/_next/static/chunks/0iw0to4qmrq4l.js","/_next/static/chunks/1ncyn83jriw3_.js","/_next/static/chunks/0-afrvt74ak7l.js","/_next/static/chunks/0337601gswv_s.js","/_next/static/chunks/34zl6f3dq0u0q.js","/_next/static/chunks/1wypk9kc4p_mt.js","/_next/static/chunks/3qu3p8mmx45cn.js","/_next/static/chunks/06kkgaxc-u2z8.js","/_next/static/chunks/2az_x4uxpy3sm.js"],"ComponentPage"]
21:I[79014,["/_next/static/chunks/0by1pam5w7cuf.js"],"OutletBoundary"]
22:"$Sreact.suspense"
24:I[79014,["/_next/static/chunks/0by1pam5w7cuf.js"],"ViewportBoundary"]
25:I[79014,["/_next/static/chunks/0by1pam5w7cuf.js"],"MetadataBoundary"]
26:I[37280,["/_next/static/chunks/0by1pam5w7cuf.js"],"default"]
27:I[75701,["/_next/static/chunks/0by1pam5w7cuf.js"],"default"]
28:I[72844,["/_next/static/chunks/0by1pam5w7cuf.js"],""]
:HL["/_next/static/chunks/16_ml9va511u1.css","style"]
:HL["/_next/static/chunks/37crpmwj58985.css","style"]
1f:T46e0,"use client";

// Adapted from the user-supplied Annnimate Starter Pack by Good Fella.
// Zeus integration repairs documented in docs/ANNNIMATE.md.

import React, { forwardRef, useImperativeHandle, useRef } from "react";
import { gsap } from "gsap";
import { useGSAP } from "@gsap/react";
import { Draggable } from "gsap/dist/Draggable";
import { InertiaPlugin } from "gsap/dist/InertiaPlugin";
import { CustomEase } from "gsap/dist/CustomEase";

if (typeof window !== "undefined") {
  gsap.registerPlugin(useGSAP, Draggable, InertiaPlugin, CustomEase);
  CustomEase.create("zeusAnnimate", "M0,0 C0.3,0.9 0.1,1 1,1");
  CustomEase.create("zeusAnnimateInOut", "M0,0 C0.7,0 0.16,1 1,1");
}

const DEFAULT_IMAGES = [
  "/assets/annnimate/roster_profile_06.jpg",
  "/assets/annnimate/roster_profile_01.jpg",
  "/assets/annnimate/roster_cap-profile_02.jpg",
  "/assets/annnimate/roster_cap-profile_01.jpg",
  "/assets/annnimate/roster_profile_09.jpg",
  "/assets/annnimate/roster_profile_07.jpg",
  "/assets/annnimate/silhouette_eyewear-silhouette_01.jpg",
  "/assets/annnimate/roster_profile_02.jpg",
  "/assets/annnimate/roster_profile_10.jpg",
  "/assets/annnimate/roster_eyewear-profile_01.jpg",
  "/assets/annnimate/roster_silhouette_01.jpg",
  "/assets/annnimate/roster_cap-silhouette_01.jpg",
];

const EASE_KEY = "zeusAnnimateInOut";
const OVERLAP = 1.5; // px - cards overlap their tail neighbour to hide sub-pixel seams
const SCROLL_DAMP = 0.9; // per-frame decay of the scroll boost - higher = longer glide

/**
 * ScaleSlider Component
 * Square image cards on one bottom baseline, sized as a pure function of
 * x-position. Card boundaries sit on an exponential map whose vanishing
 * point is off-screen past the tail edge, so cards scale down continuously
 * and proportionally as they travel tailward and exit the edge cropped at
 * minScale * heroSize (never collapsing to zero). ALL cards are always
 * rendered - off-stage ones park off-screen via transform and are clipped,
 * never display-toggled, so the recycle jump is invisible in both travel
 * directions. Driven by drag + inertia, autoplay drift, page-scroll,
 * keyboard, and optional prev/next arrows.
 *
 * @param {string} [className=''] - Additional CSS classes for the wrapper
 * @param {Array<string>} [images] - Image URLs, one per card (min 3)
 * @param {'rtl'|'ltr'} [direction='rtl'] - Which edge the hero card is anchored to
 * @param {'auto'|number} [count='auto'] - How many cards tile the viewport
 * @param {number} [minScale=0.3] - Smallest card size as a fraction of the biggest
 * @param {number} [autoplay=0.0016] - Continuous ramp drift speed. 0 = off
 * @param {number} [drag=0.0012] - Drag-to-travel sensitivity
 * @param {number} [scroll=0.0002] - How strongly page scroll accelerates the slider
 * @param {number} [focusY=0.22] - Vertical cover-crop anchor bias
 * @param {number} [radius=0] - Card corner radius in pixels
 * @param {number} [parallax=0] - Scale parallax depth (0 = off)
 */
const ScaleSlider = forwardRef(function ScaleSlider(
  {
    className = "",
    images = DEFAULT_IMAGES,
    direction = "rtl",
    count = "auto",
    minScale = 0.3,
    autoplay = 0.0016,
    drag = 0.0012,
    scroll = 0.0002,
    focusY = 0.22,
    radius = 0,
    parallax = 0,
  },
  ref,
) {
  const wrapRef = useRef(null);
  const sceneRef = useRef(null);
  const cardRefs = useRef([]);
  const prevBtnRef = useRef(null);
  const nextBtnRef = useRef(null);
  const apiRef = useRef({});
  cardRefs.current = [];

  function setCardRef(el) {
    if (el && !cardRefs.current.includes(el)) cardRefs.current.push(el);
  }

  useImperativeHandle(
    ref,
    () => ({
      next: () => apiRef.current.next?.(),
      prev: () => apiRef.current.prev?.(),
      goTo: (j) => apiRef.current.goTo?.(j),
    }),
    [],
  );

  useGSAP(
    () => {
      const scene = sceneRef.current;
      const wrap = wrapRef.current;
      if (!scene || !wrap) return;

      const cards = cardRefs.current
        .map(function (el) {
          return { el: el, img: el.querySelector("img") };
        })
        .filter(function (c) {
          return c.img;
        });
      const M = cards.length;
      if (M < 3) return;
      const prevBtn = prevBtnRef.current;
      const nextBtn = nextBtnRef.current;

      // --- Config ---
      const DIRECTION = direction === "ltr" ? "ltr" : "rtl";
      const COUNT_ATTR = count || "auto";
      const MIN_SCALE = Math.min(0.8, Math.max(0.05, parseFloat(minScale)));
      const AUTOPLAY = parseFloat(autoplay);
      const DRAG = parseFloat(drag);
      const SCROLL = parseFloat(scroll);
      const FOCUS_Y = parseFloat(focusY);
      const RADIUS = parseFloat(radius);
      const PARALLAX = Math.max(0, parseFloat(parallax));

      // --- Constants ---
      // drag: content follows the finger. rtl head is on the right, so a
      // rightward drag (+dx) advances travel toward the head; ltr mirrors.
      const DRAG_SIGN = DIRECTION === "ltr" ? -1 : 1;
      const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

      // --- State ---
      let viewW = 1,
        viewH = 1;
      let base = 100; // fixed px size of every card frame; size is driven by transform scale
      let visCount = 5;
      let rampR = 1.35,
        rampK = 1,
        rampXv = 0; // derived in computeRamp()
      let travel = 0;
      let dragX = 0;
      let scrollY = window.scrollY || 0;
      let scrollBoost = 0; // decaying extra drift speed from page scroll
      let running = true;
      let dragInstance = null;
      let navTween = null; // active tween when an arrow is animating to a card
      let resizeTimer = null;

      // --- Helpers ---
      function phaseNow() {
        return travel - Math.floor(travel);
      }

      // How many cards tile the stage. "auto" scales with viewport shape - more
      // cards on wider screens keeps the head card from exceeding the stage
      // height (a card taller than the stage would be clipped at the top and
      // read as non-square).
      function computeCount() {
        if (COUNT_ATTR !== "auto") {
          return Math.max(3, Math.min(M - 1, parseInt(COUNT_ATTR, 10) || 5));
        }
        const aspect = viewW / viewH;
        return aspect >= 2.2 ? 6 : aspect >= 1.5 ? 5 : aspect >= 1.0 ? 4 : 3;
      }

      // Solve the ramp from the two knobs so exactly visCount cards tile
      // [0, viewW] edge-to-edge at EVERY phase - head cropped past the right
      // edge (boundary(0) >= viewW), tail cropped past the left edge at
      // MIN_SCALE (boundary(visCount) <= 0). rampR is the neighbour size ratio
      // (each card R x its tail neighbour) set so the head:tail span == MIN_SCALE
      // across visCount cards. Anchoring the vanishing point at -MIN_SCALE * K
      // (not -MIN_SCALE * K / R) is what keeps the tail pinned to the left edge
      // through the whole cycle instead of drifting inward and leaving a gap.
      function computeRamp() {
        rampR = Math.pow(1 / MIN_SCALE, 1 / (visCount - 1));
        rampK = viewW / (1 - MIN_SCALE);
        rampXv = -MIN_SCALE * rampK;
      }

      // Distance of boundary k from the tail edge. Boundary 0 is the head card's
      // outer edge - always at or past the head viewport edge.
      function boundary(k) {
        return rampXv + rampK * Math.pow(rampR, phaseNow() - k);
      }

      // The nearest travel value where image j lands on the head at phase 0
      // (a "landed" state is any whole integer; image = integer mod M).
      function landedForImage(j) {
        const n = ((j % M) + M) % M;
        return n + M * Math.round((travel - n) / M);
      }

      // --- Animation routines ---
      // Every card is ALWAYS rendered. Card i is at slot k = (i - floor(travel))
      // mod M; slots >= visCount sit off-screen (boundary <= 0) and are clipped.
      // Never display-toggled, so the recycle - a card jumping from off one edge
      // to off the other - is an invisible off-screen transform jump either way.
      function positionCards() {
        const shift = Math.floor(travel);
        for (let i = 0; i < M; i++) {
          const card = cards[i];
          const el = card.el;
          const k = (((i - shift) % M) + M) % M;
          const outer = boundary(k);
          const inner = boundary(k + 1);
          const size = Math.max(outer - inner, 1);
          const w = size + OVERLAP;
          // rtl: left edge at inner (distance from the left/tail edge), overlap
          // extends tailward (left). ltr mirrors: left edge at viewW - outer,
          // overlap extends tailward (right, via the width).
          const left = DIRECTION === "ltr" ? viewW - outer : inner - OVERLAP;
          // Size is driven by transform SCALE off a fixed base frame (never by
          // width/height), so nothing re-rasterizes per frame - the image is
          // rasterized once at `base` px and only composited. transform-origin
          // is the frame's bottom-left, so scale keeps the card on the baseline
          // and grows it rightward from `left`.
          const fscale = w / base;
          el.style.zIndex = String(visCount - k); // head (small k) on top
          el.style.transform =
            "translate3d(" + left.toFixed(1) + "px,0,0) scale(" + fscale.toFixed(5) + ")";

          // Scale parallax (scale-OUT): the image is largest at the head and
          // eases back to 1x toward the tail. Skip it for off-stage cards.
          if (PARALLAX > 0 && k < visCount) {
            const cx = left + w / 2;
            let tailDist = DIRECTION === "ltr" ? (viewW - cx) / viewW : cx / viewW;
            tailDist = tailDist < 0 ? 0 : tailDist > 1 ? 1 : tailDist;
            const zoom = 1 + PARALLAX * tailDist;
            card.img.style.transform = "scale(" + zoom.toFixed(4) + ")";
          }
        }
      }

      // Animate travel to a target (an arrow step or a goTo target). While it
      // runs, frame() hands travel over to this tween so autoplay/drag/scroll
      // don't fight it, and it lands exactly on a whole card.
      function animateTo(target) {
        if (navTween) navTween.kill();
        const proxy = { t: travel };
        const dur = Math.min(1.1, 0.45 + 0.12 * Math.abs(target - travel));
        navTween = gsap.to(proxy, {
          t: target,
          duration: dur,
          ease: typeof CustomEase !== "undefined" ? EASE_KEY : "power3.inOut",
          onUpdate: function () {
            travel = proxy.t;
            positionCards();
          },
          onComplete: function () {
            navTween = null;
          },
        });
      }
      function goForward() {
        animateTo(Math.round(travel) - 1);
      } // autoplay direction
      function goBackward() {
        animateTo(Math.round(travel) + 1);
      }
      function goToImage(j) {
        animateTo(landedForImage(j));
      }

      function frame() {
        if (!running) return;
        const sy = window.scrollY || 0;
        const scrollDelta = sy - scrollY;
        scrollY = sy;
        if (navTween) {
          dragX = 0;
          return;
        } // nav owns travel; its tween renders
        // Scroll feeds a decaying velocity BOOST rather than moving travel 1:1,
        // so the conveyor accelerates on scroll and glides to a stop instead of
        // freezing the instant scrolling stops. Scrolling either direction adds
        // speed the same way (magnitude only), matching autoplay - never reverses.
        scrollBoost = scrollBoost * SCROLL_DAMP + Math.abs(scrollDelta) * SCROLL;
        travel += dragX * DRAG * DRAG_SIGN - (reducedMotion ? 0 : AUTOPLAY) - scrollBoost;
        dragX = 0;
        positionCards();
      }

      function resize() {
        const w = scene.clientWidth,
          h = scene.clientHeight;
        if (!w || !h) return;
        viewW = w;
        viewH = h;
        visCount = computeCount();
        computeRamp();
        // Fixed base frame size = the largest a card ever gets, plus parallax
        // headroom so the zoomed image stays sampled from enough pixels. Set on
        // the frames once here (not per frame) so per-frame updates are pure
        // GPU transforms - this is the fix for the autoplay stutter (animating
        // width/height re-rasterized the cover image every frame).
        const maxHero = rampK * (rampR - 1);
        base = (maxHero + OVERLAP) * (1 + PARALLAX);
        const px = base.toFixed(1) + "px";
        for (let i = 0; i < M; i++) {
          cards[i].el.style.width = px;
          cards[i].el.style.height = px;
        }
        positionCards();
      }

      function initDrag() {
        if (typeof Draggable === "undefined") return;
        const proxy = document.createElement("div");
        dragInstance = Draggable.create(proxy, {
          type: "x",
          trigger: scene,
          inertia: typeof InertiaPlugin !== "undefined",
          allowContextMenu: true,
          dragClickables: true,
          onDragStart: function () {
            if (navTween) {
              navTween.kill();
              navTween = null;
            }
          },
          onDrag: function () {
            dragX += this.deltaX;
          },
          onThrowUpdate: function () {
            dragX += this.deltaX;
          },
        })[0];
      }

      // --- Event listeners ---
      function onKeydown(e) {
        if (e.key === "ArrowLeft") {
          e.preventDefault();
          (DIRECTION === "ltr" ? goBackward : goForward)();
        } else if (e.key === "ArrowRight") {
          e.preventDefault();
          (DIRECTION === "ltr" ? goForward : goBackward)();
        }
      }
      wrap.addEventListener("keydown", onKeydown);

      function onPrev() {
        goBackward();
      }
      function onNext() {
        goForward();
      }
      if (prevBtn) prevBtn.addEventListener("click", onPrev);
      if (nextBtn) nextBtn.addEventListener("click", onNext);

      const ro = new ResizeObserver(function () {
        clearTimeout(resizeTimer);
        resizeTimer = setTimeout(resize, 150);
      });
      ro.observe(scene);

      let inView = true;
      function onVis() {
        running = inView && !document.hidden;
      }
      const visibilityObserver = new IntersectionObserver(([entry]) => {
        inView = entry.isIntersecting;
        onVis();
      });
      visibilityObserver.observe(wrap);
      document.addEventListener("visibilitychange", onVis);

      // --- Initial DOM state ---
      const objPos = "50% " + (FOCUS_Y * 100).toFixed(1) + "%";
      for (let i = 0; i < M; i++) {
        cards[i].img.style.objectPosition = objPos;
        if (RADIUS > 0) cards[i].el.style.borderRadius = RADIUS + "px";
      }
      resize(); // positions every card (transforms) while still display:none
      for (let i = 0; i < M; i++) cards[i].el.style.display = "block"; // reveal, already placed
      initDrag();

      // --- Reduced motion ---
      // The ticker still runs so drag + scroll + nav respond; only autoplay is
      // gated above (reducedMotion -> AUTOPLAY contributes 0).
      gsap.ticker.add(frame);

      // --- Public API ---
      // next/prev advance the conveyor one card (next = the autoplay direction);
      // goTo animates a specific image index to the featured head.
      apiRef.current = {
        next: function () {
          goForward();
        },
        prev: function () {
          goBackward();
        },
        goTo: function (j) {
          goToImage(j);
        },
      };

      return function cleanup() {
        running = false;
        gsap.ticker.remove(frame);
        clearTimeout(resizeTimer);
        if (navTween) navTween.kill();
        if (dragInstance) dragInstance.kill();
        ro.disconnect();
        visibilityObserver.disconnect();
        document.removeEventListener("visibilitychange", onVis);
        wrap.removeEventListener("keydown", onKeydown);
        if (prevBtn) prevBtn.removeEventListener("click", onPrev);
        if (nextBtn) nextBtn.removeEventListener("click", onNext);
      };
    },
    {
      scope: wrapRef,
      revertOnUpdate: true,
      dependencies: [
        images,
        direction,
        count,
        minScale,
        autoplay,
        drag,
        scroll,
        focusY,
        radius,
        parallax,
      ],
    },
  );

  return (
    <div
      ref={wrapRef}
      className={"scs_wrap" + (className ? " " + className : "")}
      data-anm-scale-slider
      role="group"
      aria-label="Image slider"
      aria-roledescription="carousel"
      tabIndex={0}
    >
      <div className="scs_scene" ref={sceneRef} data-anm-scale-slider-scene>
        {images.map(function (src, i) {
          return (
            <div className="scs_card" key={i} ref={setCardRef} data-anm-scale-slider-card>
              <img className="scs_img" src={src} alt="" draggable="false" />
            </div>
          );
        })}
      </div>

      <div className="scs_pill" role="group" aria-label="Slide controls">
        <button
          className="scs_arrow"
          ref={prevBtnRef}
          data-anm-scale-slider-prev
          type="button"
          aria-label="Previous"
        >
          <svg className="scs_arrow_icon" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <path
              d="M10 3L5 8L10 13"
              stroke="currentColor"
              strokeWidth="1.5"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          </svg>
        </button>
        <button
          className="scs_arrow"
          ref={nextBtnRef}
          data-anm-scale-slider-next
          type="button"
          aria-label="Next"
        >
          <svg className="scs_arrow_icon" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <path
              d="M6 3L11 8L6 13"
              stroke="currentColor"
              strokeWidth="1.5"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          </svg>
        </button>
      </div>
    </div>
  );
});

export default ScaleSlider;
20:T830,// Styles adapted from the supplied Annnimate Starter Pack.
const css = `.scs_wrap {
  position: relative;
  width: 100%;
  height: var(--zeus-animation-height, 640px);
  background-color: #ebebe9;
  overflow: hidden;
  touch-action: pan-y;
  outline: none;
  cursor: grab;
}

.scs_wrap:active {
  cursor: grabbing;
}

.scs_wrap:focus-visible {
  outline: 2px solid #fd551d;
  outline-offset: -2px;
}

.scs_scene {
  position: absolute;
  inset: 0;
}

.scs_card {
  position: absolute;
  left: 0;
  bottom: 0;
  display: none;
  aspect-ratio: 1 / 1;
  overflow: hidden;
  transform-origin: 0 100%;
  will-change: transform;
  backface-visibility: hidden;
}

.scs_img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: 50% 22%;
  transform: scale(1);
  transform-origin: center;
  will-change: transform;
  backface-visibility: hidden;
  user-select: none;
  -webkit-user-select: none;
  -webkit-user-drag: none;
}

/* Optional navigation - a horizontally-centered control pill at the bottom,
   matching the wipe-slider control (translucent + blur, circular prev/next
   arrows). */
.scs_pill {
  position: absolute;
  left: 50%;
  bottom: 40px;
  z-index: 20;
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 6px;
  border-radius: 999px;
  background: rgba(18, 16, 14, 0.35);
  -webkit-backdrop-filter: blur(14px);
  backdrop-filter: blur(14px);
  transform: translateX(-50%);
}

.scs_arrow {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  flex: 0 0 auto;
  padding: 0;
  border: none;
  border-radius: 999px;
  background: rgba(18, 16, 14, 0.5);
  color: rgba(255, 250, 242, 0.92);
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.scs_arrow:hover {
  background: rgba(18, 16, 14, 0.68);
}

.scs_arrow:focus-visible {
  outline: 2px solid rgba(255, 255, 255, 0.8);
  outline-offset: 2px;
}

.scs_arrow_icon {
  display: block;
  width: 16px;
  height: 16px;
}

@media (max-width: 767px) {
  .scs_wrap {
    height: 100svh;
  }

  .scs_pill {
    bottom: 24px;
  }
}
`;
export default css;
3:["$","$L1e",null,{"item":{"$schema":"https://myzeusui.com/schema/registry-item.json","name":"scale-slider","title":"Scale Slider","description":"An exponential image conveyor with drag inertia, keyboard navigation and optional drift","category":"animations","family":"media","kind":"animation","designSystem":null,"foundationDependencies":["tokens"],"iconLibraries":[],"tags":["animation","gsap","annnimate","scale-slider"],"status":"beta","tier":"free","isNew":true,"dependencies":["@gsap/react","gsap"],"registryDependencies":["tokens"],"files":[{"path":"application/animations/scale-slider-engine.d.ts","target":"components/application/animations/scale-slider-engine.d.ts","type":"component","content":"import type { ScaleSliderProps, ScaleSliderHandle } from \"./scale-slider\";\nimport type { ForwardRefExoticComponent, RefAttributes } from \"react\";\ndeclare const Engine: ForwardRefExoticComponent<\n  ScaleSliderProps & RefAttributes<ScaleSliderHandle>\n>;\nexport default Engine;\n"},{"path":"application/animations/scale-slider-engine.jsx","target":"components/application/animations/scale-slider-engine.jsx","type":"component","content":"$1f"},{"path":"application/animations/scale-slider-styles.ts","target":"components/application/animations/scale-slider-styles.ts","type":"component","content":"$20"},{"path":"application/animations/scale-slider.tsx","target":"components/application/animations/scale-slider.tsx","type":"component","content":"\"use client\";\n\n// Annnimate Starter Pack adaptation. See docs/ANNNIMATE.md for provenance.\nimport { forwardRef } from \"react\";\nimport Engine from \"./scale-slider-engine.jsx\";\nimport css from \"./scale-slider-styles\";\n\nexport interface ScaleSliderHandle {\n  next: () => void;\n  prev: () => void;\n  goTo: (index: number) => void;\n}\nexport interface ScaleSliderProps {\n  className?: string;\n  images?: string[];\n  direction?: \"rtl\" | \"ltr\";\n  count?: \"auto\" | number;\n  minScale?: number;\n  autoplay?: number;\n  drag?: number;\n  scroll?: number;\n  focusY?: number;\n  radius?: number;\n  parallax?: number;\n}\n\nexport const ScaleSlider = forwardRef<ScaleSliderHandle, ScaleSliderProps>(\n  function ScaleSlider(props, ref) {\n    return (\n      <>\n        <style>{css}</style>\n        <Engine {...props} ref={ref} />\n      </>\n    );\n  },\n);\n"},{"path":"public/assets/annnimate/roster_cap-profile_01.jpg","target":"public/assets/annnimate/roster_cap-profile_01.jpg","type":"asset","encoding":"base64"},{"path":"public/assets/annnimate/roster_cap-profile_02.jpg","target":"public/assets/annnimate/roster_cap-profile_02.jpg","type":"asset","encoding":"base64"},{"path":"public/assets/annnimate/roster_cap-silhouette_01.jpg","target":"public/assets/annnimate/roster_cap-silhouette_01.jpg","type":"asset","encoding":"base64"},{"path":"public/assets/annnimate/roster_eyewear-profile_01.jpg","target":"public/assets/annnimate/roster_eyewear-profile_01.jpg","type":"asset","encoding":"base64"},{"path":"public/assets/annnimate/roster_profile_01.jpg","target":"public/assets/annnimate/roster_profile_01.jpg","type":"asset","encoding":"base64"},{"path":"public/assets/annnimate/roster_profile_02.jpg","target":"public/assets/annnimate/roster_profile_02.jpg","type":"asset","encoding":"base64"},{"path":"public/assets/annnimate/roster_profile_06.jpg","target":"public/assets/annnimate/roster_profile_06.jpg","type":"asset","encoding":"base64"},{"path":"public/assets/annnimate/roster_profile_07.jpg","target":"public/assets/annnimate/roster_profile_07.jpg","type":"asset","encoding":"base64"},{"path":"public/assets/annnimate/roster_profile_09.jpg","target":"public/assets/annnimate/roster_profile_09.jpg","type":"asset","encoding":"base64"},{"path":"public/assets/annnimate/roster_profile_10.jpg","target":"public/assets/annnimate/roster_profile_10.jpg","type":"asset","encoding":"base64"},{"path":"public/assets/annnimate/roster_silhouette_01.jpg","target":"public/assets/annnimate/roster_silhouette_01.jpg","type":"asset","encoding":"base64"},{"path":"public/assets/annnimate/silhouette_eyewear-silhouette_01.jpg","target":"public/assets/annnimate/silhouette_eyewear-silhouette_01.jpg","type":"asset","encoding":"base64"}],"hash":"beb8b37e5688"},"demoSource":"\"use client\";\n\nimport { ScaleSlider } from \"@/components/application/animations/scale-slider\";\n\nexport default function Demo() {\n  return (\n    <div className=\"w-full overflow-hidden rounded-2xl\">\n      <ScaleSlider minScale={0.3} autoplay={0.0016} radius={0} />\n    </div>\n  );\n}","previous":{"name":"image-fly-in","title":"Image Fly-In"},"next":{"name":"curtain-slider","title":"Curtain Slider"}}]
4:["$","script","script-0",{"src":"/_next/static/chunks/0hdjbswbbz8h2.js","async":true}]
5:["$","script","script-1",{"src":"/_next/static/chunks/06es_ecb9le4q.js","async":true}]
6:["$","script","script-2",{"src":"/_next/static/chunks/0jocqdh7qodi4.js","async":true}]
7:["$","script","script-3",{"src":"/_next/static/chunks/0iw0to4qmrq4l.js","async":true}]
8:["$","script","script-4",{"src":"/_next/static/chunks/1ncyn83jriw3_.js","async":true}]
9:["$","script","script-5",{"src":"/_next/static/chunks/0-afrvt74ak7l.js","async":true}]
a:["$","script","script-6",{"src":"/_next/static/chunks/0337601gswv_s.js","async":true}]
b:["$","script","script-7",{"src":"/_next/static/chunks/34zl6f3dq0u0q.js","async":true}]
c:["$","script","script-8",{"src":"/_next/static/chunks/1wypk9kc4p_mt.js","async":true}]
d:["$","script","script-9",{"src":"/_next/static/chunks/3qu3p8mmx45cn.js","async":true}]
e:["$","script","script-10",{"src":"/_next/static/chunks/06kkgaxc-u2z8.js","async":true}]
f:["$","script","script-11",{"src":"/_next/static/chunks/2az_x4uxpy3sm.js","async":true}]
10:["$","$L21",null,{"children":["$","$22",null,{"name":"Next.MetadataOutlet","children":"$@23"}]}]
13:["$","$1","h",{"children":[null,["$","$L24",null,{"children":[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]}],["$","div",null,{"hidden":true,"children":["$","$L25",null,{"children":["$","$22",null,{"name":"Next.Metadata","children":[["$","title","0",{"children":"Scale Slider — Zeus UI"}],["$","meta","1",{"name":"description","content":"An exponential image conveyor with drag inertia, keyboard navigation and optional drift"}],["$","meta","2",{"name":"keywords","content":"animation,gsap,annnimate,scale-slider"}]]}]}]}],["$","meta",null,{"name":"next-size-adjust","content":""}]]}]
15:["$","$1","c",{"children":[null,["$","$L26",null,{"parallelRouterKey":"children","template":["$","$L27",null,{}]}]]}]
18:["$","$1","c",{"children":[null,["$","$L26",null,{"parallelRouterKey":"children","template":["$","$L27",null,{}]}]]}]
1a:["$","$1","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/_next/static/chunks/16_ml9va511u1.css","precedence":"next"}],["$","link","1",{"rel":"stylesheet","href":"/_next/static/chunks/37crpmwj58985.css","precedence":"next"}],["$","script","script-0",{"src":"/_next/static/chunks/0by1pam5w7cuf.js","async":true}]],["$","html",null,{"lang":"en","suppressHydrationWarning":true,"className":"inter_9cfd2fd2-module__mkxXHa__variable jetbrains_mono_32f24d5e-module__zeawOW__variable h-full antialiased","children":["$","body",null,{"className":"min-h-full flex flex-col bg-background-full","children":[["$","$L28",null,{"id":"zeus-theme","strategy":"beforeInteractive","children":"(function(){try{var dark=localStorage.getItem(\"zeus:theme\")===\"dark\";document.documentElement.classList.toggle(\"dark\",dark)}catch(e){document.documentElement.classList.remove(\"dark\")}})();"}],["$","$L26",null,{"parallelRouterKey":"children","template":["$","$L27",null,{}],"notFound":[[["$","title",null,{"children":"404: This page could not be found."}],["$","div",null,{"style":{"fontFamily":"system-ui,\"Segoe UI\",Roboto,Helvetica,Arial,sans-serif,\"Apple Color Emoji\",\"Segoe UI Emoji\"","height":"100vh","textAlign":"center","display":"flex","flexDirection":"column","alignItems":"center","justifyContent":"center"},"children":["$","div",null,{"children":[["$","style",null,{"dangerouslySetInnerHTML":{"__html":"body{color:#000;background:#fff;margin:0}.next-error-h1{border-right:1px solid rgba(0,0,0,.3)}@media (prefers-color-scheme:dark){body{color:#fff;background:#000}.next-error-h1{border-right:1px solid rgba(255,255,255,.3)}}"}}],["$","h1",null,{"className":"next-error-h1","style":{"display":"inline-block","margin":"0 20px 0 0","padding":"0 23px 0 0","fontSize":24,"fontWeight":500,"verticalAlign":"top","lineHeight":"49px"},"children":404}],["$","div",null,{"style":{"display":"inline-block"},"children":["$","h2",null,{"style":{"fontSize":14,"fontWeight":400,"lineHeight":"49px","margin":0},"children":"This page could not be found."}]}]]}]}]],[]]}]]}]}]]}]
23:null
1d:true
12:300
12:C
1c:0
14:"$undefined"
16:"$undefined"
19:"$undefined"
1b:"$undefined"
11:"$undefined"
