1:"$Sreact.fragment"
2:I[216224,["/_next/static/chunks/1-kkngoowwvks.js","/_next/static/chunks/1l1ztpmvvr6un.js","/_next/static/chunks/3akuivfsf40p7.js","/_next/static/chunks/3_1pfg1675we5.js","/_next/static/chunks/3b-j35phbz363.js","/_next/static/chunks/2v6dx3y3pgbg2.js","/_next/static/chunks/3znghnjm2oz_l.js","/_next/static/chunks/3458ylrm-tu9f.js","/_next/static/chunks/1h9znjrekhrnu.js","/_next/static/chunks/052khci2jbxel.js","/_next/static/chunks/3fuakxesa5sfw.js","/_next/static/chunks/34kutq8p1wnfe.js","/_next/static/chunks/19s02zkpk1224.js"],"DocsLayout"]
11:X
16:X
16:C
0:{"buildId":"HmSgPc6hu9Z-EFDBHGy7K","data":[{"rsc":["$","$1","c",{"children":[["$","$L2",null,{"title":"AI Image Generation","heading":"AI Image Generation","icon":"components","groups":[{"id":"buttons","title":"Buttons","items":[{"name":"background-fill-button","title":"Background Fill Button","isNew":true},{"name":"circle-fill-button","title":"Circle Fill Button","isNew":true},{"name":"curve-fill-button","title":"Curve Fill Button","isNew":true},{"name":"animated-icon-button","title":"Animated Icon Button","isNew":true},{"name":"magnetic-button","title":"Magnetic Button","isNew":true},{"name":"rainbow-button","title":"Rainbow Button","isNew":true},{"name":"text-underline","title":"Text Underline","isNew":true},{"name":"button","title":"Button"},{"name":"icon-button","title":"Icon Button"},{"name":"link-button","title":"Link Button"},{"name":"button-group","title":"Button Group"},{"name":"close-button","title":"Close Button"},{"name":"social-button","title":"Social Button"}]},{"id":"forms","title":"Forms & inputs","items":[{"name":"gooey-filter-chips","title":"Gooey Filter Chips","isNew":true},{"name":"multi-step-form","title":"Multi-Step Form","isNew":true},{"name":"morphing-select","title":"Morphing Select","isNew":true},{"name":"input","title":"Input"},{"name":"input-otp","title":"Input OTP"},{"name":"checkbox","title":"Checkbox"},{"name":"radio","title":"Radio"},{"name":"switch","title":"Switch"},{"name":"select","title":"Select"},{"name":"slider","title":"Slider"},{"name":"file-upload","title":"File Upload"},{"name":"auth-card","title":"Auth Card"}]},{"id":"navigation","title":"Navigation","items":[{"name":"morphing-pill-menu","title":"Morphing Pill Menu","isNew":true},{"name":"gooey-menu","title":"Gooey Menu","isNew":true},{"name":"mega-menu","title":"Mega Menu","isNew":true},{"name":"fullscreen-slide-menu","title":"Fullscreen Slide Menu","isNew":true},{"name":"hide-header","title":"Hide Header","isNew":true},{"name":"custom-cursor","title":"Custom Cursor","isNew":true},{"name":"morphing-island","title":"Morphing Island","isNew":true},{"name":"morphing-tabs","title":"Morphing Tabs","isNew":true},{"name":"multi-level-drawer-menu","title":"Multi-Level Drawer Menu","isNew":true},{"name":"dropdown","title":"Dropdown"},{"name":"tabs","title":"Tabs"},{"name":"segmented-control","title":"Segmented Control"},{"name":"breadcrumb","title":"Breadcrumb"},{"name":"pagination","title":"Pagination"},{"name":"sidebar","title":"Sidebar"}]},{"id":"feedback","title":"Feedback & status","items":[{"name":"badge","title":"Badge"},{"name":"chip","title":"Chip"},{"name":"status-dot","title":"Status Dot"},{"name":"announcement","title":"Announcement"},{"name":"notification","title":"Notification"},{"name":"theme-toggle","title":"Theme Toggle"},{"name":"notification-center","title":"Notification Center"}]},{"id":"overlays","title":"Overlays & disclosures","items":[{"name":"morphing-accordion","title":"Morphing Accordion","isNew":true},{"name":"detail-drawer","title":"Detail Drawer","isNew":true},{"name":"animated-tooltip","title":"Animated Tooltip","isNew":true},{"name":"morphing-dialog","title":"Morphing Dialog","isNew":true},{"name":"morphing-tooltip","title":"Morphing Tooltip","isNew":true},{"name":"animated-accordion","title":"Animated Accordion","isNew":true},{"name":"feature-dialog","title":"Feature Dialog","isNew":true},{"name":"tooltip","title":"Tooltip"},{"name":"settings-modal","title":"Settings Modal"}]},{"id":"typography","title":"Text & typography","items":[{"name":"text-on-path","title":"Text On Path","isNew":true},{"name":"text-fill-wave","title":"Text Fill Wave","isNew":true},{"name":"velocity-type","title":"Velocity Type","isNew":true},{"name":"typewriter","title":"Typewriter","isNew":true},{"name":"character-appear","title":"Character Appear","isNew":true},{"name":"text-morph","title":"Text Morph","isNew":true},{"name":"text-split-zoom","title":"Text Split Zoom","isNew":true},{"name":"text-scramble","title":"Text Scramble","isNew":true},{"name":"rectangle-text-reveal","title":"Rectangle Text Reveal","isNew":true},{"name":"popping-text","title":"Popping Text","isNew":true},{"name":"folding-text","title":"Folding Text","isNew":true},{"name":"cinematic-text","title":"Cinematic Text","isNew":true},{"name":"text-shimmer-wave","title":"Text Shimmer Wave","isNew":true},{"name":"text-reveal","title":"Text Reveal","isNew":true},{"name":"dual-scramble","title":"Dual Scramble","isNew":true},{"name":"typography","title":"Typography"},{"name":"kbd","title":"Kbd"}]},{"id":"icons","title":"Icons & identity","items":[{"name":"chevrons","title":"Chevrons"},{"name":"logo","title":"Logo"},{"name":"avatar","title":"Avatar"}]},{"id":"layout","title":"Layout & structure","items":[{"name":"grid-chapters","title":"Grid Chapters","isNew":true},{"name":"network-globe","title":"Network Globe","isNew":true},{"name":"dock-footer","title":"Dock Footer","isNew":true},{"name":"flip-filter","title":"Flip Filter","isNew":true},{"name":"capsule-hero","title":"Capsule Hero","isNew":true},{"name":"wipe-slider","title":"Wipe Slider","isNew":true},{"name":"step-wipe","title":"Step Wipe","isNew":true},{"name":"gradient-footer","title":"Gradient Footer","isNew":true},{"name":"testimonial-globe","title":"Testimonial Globe","isNew":true},{"name":"drift-gallery","title":"Drift Gallery","isNew":true},{"name":"divider","title":"Divider"},{"name":"app-shell","title":"App Shell"}]},{"id":"data","title":"Data & charts","items":[{"name":"progress","title":"Progress","isNew":true},{"name":"counter","title":"Counter","isNew":true},{"name":"combo-chart-card","title":"Combo Chart","isNew":true},{"name":"contributions-card","title":"Contributions","isNew":true},{"name":"earnings-chart-card","title":"Earnings Chart","isNew":true},{"name":"funnel-chart-card","title":"Funnel Chart","isNew":true},{"name":"heatmap-chart-card","title":"Heatmap Chart","isNew":true},{"name":"most-active-days-card","title":"Most Active Days","isNew":true},{"name":"radar-chart-card","title":"Radar Chart","isNew":true},{"name":"radial-chart-card","title":"Radial Chart","isNew":true},{"name":"sankey-chart-card","title":"Sankey Chart","isNew":true},{"name":"scatter-chart-card","title":"Scatter Chart","isNew":true},{"name":"sleep-score-card","title":"Sleep Score","isNew":true},{"name":"stage-bars-card","title":"Stage Bars","isNew":true},{"name":"steps-card","title":"Steps Chart","isNew":true},{"name":"table","title":"Table"},{"name":"stat-cards","title":"Stat Cards"},{"name":"data-table","title":"Data Table"},{"name":"area-chart-card","title":"Area Chart","isNew":true},{"name":"line-chart-card","title":"Line Chart","isNew":true},{"name":"bar-list-card","title":"Bar List","isNew":true},{"name":"activity-rings-card","title":"Activity Rings","isNew":true},{"name":"chart-card","title":"Chart Card","isNew":true},{"name":"revenue-chart-card","title":"Revenue Chart"},{"name":"orders-chart-card","title":"Orders Chart"},{"name":"important-alerts-card","title":"Important Alerts"},{"name":"patient-info-card","title":"Patient Info"}]},{"id":"media","title":"Images & media","items":[{"name":"mesh-gradient","title":"Mesh Gradient","isNew":true},{"name":"dithering","title":"Dithering","isNew":true},{"name":"image-glitch","title":"Image Glitch","isNew":true},{"name":"gooey-hover-reveal","title":"Gooey Hover Reveal","isNew":true},{"name":"dither-cursor-trail","title":"Dither Cursor Trail","isNew":true},{"name":"image-dissolve-scroll","title":"Image Dissolve Scroll","isNew":true},{"name":"scatter-cluster","title":"Scatter Cluster","isNew":true},{"name":"multi-flip","title":"Multi Flip","isNew":true},{"name":"velocity-clip","title":"Velocity Clip","isNew":true},{"name":"svg-draw-path","title":"SVG Draw Path","isNew":true},{"name":"parallax","title":"Parallax","isNew":true},{"name":"mask-reveal","title":"Mask Reveal","isNew":true},{"name":"flip-zone","title":"Flip Zone","isNew":true},{"name":"element-reveal","title":"Element Reveal","isNew":true},{"name":"background-color","title":"Background Color","isNew":true},{"name":"animated-grid","title":"Animated Grid","isNew":true},{"name":"random-rotate","title":"Random Rotate","isNew":true},{"name":"photo-stack-gallery","title":"Photo Stack Gallery","isNew":true},{"name":"spiral-gallery","title":"Spiral Gallery","isNew":true},{"name":"lens-rail","title":"Lens Rail","isNew":true},{"name":"expanding-strip","title":"Expanding Strip","isNew":true},{"name":"index-wipe","title":"Index Wipe","isNew":true},{"name":"card-fan","title":"Card Fan","isNew":true},{"name":"radial-gallery","title":"Radial Gallery","isNew":true},{"name":"circular-slider","title":"Circular Slider","isNew":true},{"name":"infinite-parallax-slider","title":"Infinite Parallax Slider","isNew":true},{"name":"image-follow-list","title":"Image Follow List","isNew":true},{"name":"hoverable-list","title":"Hoverable List","isNew":true},{"name":"infinite-draggable-grid","title":"Infinite Draggable Grid","isNew":true},{"name":"infinite-marquee","title":"Infinite Marquee","isNew":true},{"name":"image-trail","title":"Image Trail","isNew":true},{"name":"3d-card-flip","title":"3D Card Flip","isNew":true},{"name":"image-fly-in","title":"Image Fly-In","isNew":true},{"name":"scale-slider","title":"Scale Slider","isNew":true},{"name":"curtain-slider","title":"Curtain Slider","isNew":true},{"name":"ascii-overlay","title":"ASCII Overlay","isNew":true},{"name":"carousel","title":"Carousel"}]},{"id":"scheduling","title":"Dates & scheduling","items":[{"name":"calendar","title":"Calendar","isNew":true},{"name":"date-picker","title":"Date Picker"},{"name":"date-range-picker","title":"Date Range Picker"},{"name":"meeting-scheduler","title":"Meeting Scheduler"}]},{"id":"ai","title":"AI & agents","items":[{"name":"ai-profile","title":"AI Profile","isNew":true},{"name":"ai-chat","title":"AI Chat","isNew":true},{"name":"ai-image-generation","title":"AI Image Generation","isNew":true},{"name":"agent-progress","title":"Agent Progress","isNew":true},{"name":"composer","title":"Composer","isNew":true},{"name":"task-list","title":"Task List","isNew":true},{"name":"web-search","title":"Web Search","isNew":true},{"name":"agent-limits-card","title":"Agent Limits","isNew":true},{"name":"agent-thinking","title":"Agent Thinking"},{"name":"composer-loader","title":"Composer Loader"},{"name":"agent-log","title":"Agent Log"},{"name":"agent-chat","title":"Agent Chat"}]},{"id":"pricing","title":"Pricing & billing","items":[{"name":"animated-pricing","title":"Animated Pricing","isNew":true}]},{"id":"screens","title":"Complete screens","items":[{"name":"home-dashboard","title":"Home Dashboard","isNew":true},{"name":"finance-dashboard","title":"Finance Dashboard","isNew":true},{"name":"marketing-dashboard","title":"Marketing Dashboard","isNew":true},{"name":"hr-management","title":"HR Management","isNew":true},{"name":"medical-profile","title":"Medical Profile","isNew":true}]},{"id":"foundations","title":"Design foundations","items":[{"name":"tokens","title":"Design tokens"}]},{"id":"utilities","title":"Hooks & utilities","items":[{"name":"cx","title":"cx"},{"name":"use-count-up","title":"useCountUp"},{"name":"use-dismiss-on-outside-press","title":"useDismissOnOutsidePress"}]}],"description":"An image workspace with animated generation, a gallery, styles and a cancellable generation adapter","detail":true,"children":"$L3"}],["$L4","$L5","$L6","$L7","$L8","$L9","$La","$Lb","$Lc","$Ld","$Le"],"$Lf"]}],"isPartial":"$@10","staleTime":"$11","varyParams":null},{"rsc":"$L12","isPartial":"$@13","staleTime":"$11","varyParams":null},{"rsc":"$L14","isPartial":"$@15","staleTime":"$11","varyParams":"$16"},{"rsc":"$L17","isPartial":"$@18","staleTime":"$11","varyParams":"$16"}],"isUpgradeableISRFallback":false,"a":"$@19","rootVaryParams":null,"needsRuntimeRequest":"$@1a"}
1b:I[126814,["/_next/static/chunks/1-kkngoowwvks.js","/_next/static/chunks/1l1ztpmvvr6un.js","/_next/static/chunks/3akuivfsf40p7.js","/_next/static/chunks/3_1pfg1675we5.js","/_next/static/chunks/3b-j35phbz363.js","/_next/static/chunks/2v6dx3y3pgbg2.js","/_next/static/chunks/3znghnjm2oz_l.js","/_next/static/chunks/3458ylrm-tu9f.js","/_next/static/chunks/1h9znjrekhrnu.js","/_next/static/chunks/052khci2jbxel.js","/_next/static/chunks/3fuakxesa5sfw.js","/_next/static/chunks/34kutq8p1wnfe.js","/_next/static/chunks/19s02zkpk1224.js"],"ComponentPage"]
1f:I[579014,["/_next/static/chunks/1-kkngoowwvks.js","/_next/static/chunks/1l1ztpmvvr6un.js"],"OutletBoundary"]
20:"$Sreact.suspense"
22:I[579014,["/_next/static/chunks/1-kkngoowwvks.js","/_next/static/chunks/1l1ztpmvvr6un.js"],"ViewportBoundary"]
23:I[579014,["/_next/static/chunks/1-kkngoowwvks.js","/_next/static/chunks/1l1ztpmvvr6un.js"],"MetadataBoundary"]
24:I[337280,["/_next/static/chunks/1-kkngoowwvks.js","/_next/static/chunks/1l1ztpmvvr6un.js"],"default"]
25:I[475701,["/_next/static/chunks/1-kkngoowwvks.js","/_next/static/chunks/1l1ztpmvvr6un.js"],"default"]
1c:T5c05,"use client";

import {
  RiAddLine,
  RiArrowRightSLine,
  RiCheckLine,
  RiCloseLine,
  RiCodeBoxLine,
  RiCustomerServiceLine,
  RiFileCopyLine,
  RiFolder3Line,
  RiFolderOpenLine,
  RiGalleryLine,
  RiGlobalLine,
  RiInfinityLine,
  RiMenuLine,
  RiMoreFill,
  RiRobot2Line,
  RiSearchLine,
  RiSettings4Line,
  RiShareBoxLine,
  RiSideBarLine,
  RiSparklingLine,
  RiTerminalLine,
} from "@remixicon/react";
import { motion, useReducedMotion } from "motion/react";
import { type ReactNode, useEffect, useRef, useState } from "react";
import { Avatar } from "../../base/avatar/avatar";
import { Button } from "../../base/buttons/button";
import { IconButton } from "../../base/buttons/icon-button";
import {
  Dropdown,
  DropdownItem,
  DropdownPopover,
  DropdownTrigger,
} from "../../base/dropdown/dropdown";
import { InputBase } from "../../base/input/input";
import { PillTab, PillTabList } from "../../base/tabs/pill-tab";
import { cx } from "../../utils/cx";
import { Composer, StatusBar } from "../composer/composer";
import { DashboardUserMenu } from "../dashboard/dashboard-user-menu";
import { SettingsModal } from "../settings/settings-modal";
import { ThemeToggle } from "../theme/theme-toggle";

const REPOSITORIES = [
  {
    name: "boardui",
    chats: [
      { name: "pro badge restyle", time: "2h" },
      { name: "installation docs page", time: "1d" },
    ],
  },
  {
    name: "vibl coding project",
    chats: [
      { name: "landing page design", time: "34m" },
      { name: "image generation", time: "now", href: "/templates/ai-image-generation" },
      { name: "coding scenario", time: "now", href: "/templates/ai-chat" },
      { name: "mobile app for vuejs…", time: "5h" },
      { name: "code refactor dropdown", time: "18h" },
    ],
  },
  {
    name: "strider landing page work",
    chats: [
      { name: "hero section animation", time: "3d" },
      { name: "pricing table copy", time: "4d" },
    ],
  },
  {
    name: "pirate mini game iOS",
    chats: [
      { name: "cannon physics tuning", time: "1w" },
      { name: "sprite sheet cleanup", time: "2w" },
    ],
  },
];

function AgentRepositorySidebar({
  selected,
  onNew,
  onClose,
  collapsed,
  onCollapse,
  className,
}: {
  selected: string;
  onNew: () => void;
  onClose?: () => void;
  collapsed: boolean;
  onCollapse?: () => void;
  className?: string;
}) {
  const [expanded, setExpanded] = useState<Record<string, boolean>>({
    "vibl coding project": true,
  });
  const [query, setQuery] = useState("");
  const [searchOpen, setSearchOpen] = useState(false);
  const [settings, setSettings] = useState(false);
  const [notice, setNotice] = useState<string | null>(null);
  const inputRef = useRef<HTMLInputElement>(null);
  useEffect(() => {
    if (searchOpen) inputRef.current?.focus();
  }, [searchOpen]);
  useEffect(() => {
    const listener = (event: KeyboardEvent) => {
      if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "l") {
        event.preventDefault();
        setSearchOpen(true);
      }
    };
    window.addEventListener("keydown", listener);
    return () => window.removeEventListener("keydown", listener);
  }, []);
  const rows = REPOSITORIES.filter(
    (repo) =>
      !query ||
      `${repo.name} ${repo.chats.map((chat) => chat.name).join(" ")}`
        .toLowerCase()
        .includes(query.toLowerCase()),
  );
  return (
    <aside
      className={cx(
        "flex h-full shrink-0 flex-col justify-between overflow-hidden rounded-3xl border border-border-button-white bg-background-secondary-default p-3 shadow-sidebar transition-[width] duration-300 motion-reduce:transition-none",
        collapsed ? "w-[60px]" : "w-[260px]",
        className,
      )}
    >
      <div className="flex min-h-0 flex-col gap-3">
        <div className="flex items-center justify-between gap-1">
          <DashboardUserMenu collapsed={collapsed} />
          {!collapsed && (
            <IconButton
              size="small"
              icon={onClose ? RiCloseLine : RiSideBarLine}
              aria-label={onClose ? "Close navigation" : "Collapse sidebar"}
              onClick={onClose ?? onCollapse}
            />
          )}
        </div>
        {collapsed ? (
          <IconButton
            size="small"
            icon={RiMenuLine}
            aria-label="Expand sidebar"
            onClick={onCollapse}
          />
        ) : searchOpen ? (
          <InputBase
            ref={inputRef}
            aria-label="Search repositories"
            placeholder="Search repositories"
            value={query}
            onChange={(event) => setQuery(event.target.value)}
            onKeyDown={(event) => {
              if (event.key === "Escape") {
                setQuery("");
                setSearchOpen(false);
              }
            }}
            leadingIcon={RiSearchLine}
          />
        ) : (
          <button
            type="button"
            onClick={() => setSearchOpen(true)}
            className="flex h-9 cursor-pointer items-center gap-2 rounded-full bg-background-tertiary-default px-2.5 text-body-medium text-text-secondary"
          >
            <RiSearchLine className="size-5" />
            <span className="flex-1 text-left">Quick Search</span>
            <span className="rounded-md bg-background-secondary-hover px-1 text-caption-1-medium">
              ⌘L
            </span>
          </button>
        )}
        <div className="flex flex-col gap-1">
          {[
            { label: "New agent", Icon: RiAddLine, action: onNew },
            { label: "Automations", Icon: RiRobot2Line, action: () => setNotice("Automations") },
            { label: "Customize", Icon: RiSparklingLine, action: () => setSettings(true) },
          ].map(({ label, Icon, action }) => (
            <button
              type="button"
              key={label}
              onClick={action}
              aria-label={label}
              className="flex h-9 cursor-pointer items-center gap-2 overflow-hidden rounded-2lg px-2 text-body-medium whitespace-nowrap text-text-secondary hover:bg-background-secondary-hover"
            >
              <Icon className="size-5 shrink-0" />
              {!collapsed && label}
            </button>
          ))}
        </div>
        {!collapsed && (
          <div className="flex min-h-0 flex-col gap-2 overflow-y-auto pt-2">
            <p className="px-0 text-body-medium text-text-secondary">Repositories</p>
            {rows.map((repo) => {
              const open = !!expanded[repo.name] || !!query;
              return (
                <div key={repo.name}>
                  <button
                    type="button"
                    aria-expanded={open}
                    onClick={() => setExpanded((old) => ({ ...old, [repo.name]: !open }))}
                    className="flex h-9 w-full cursor-pointer items-center gap-2 rounded-2lg px-2 text-left text-body-medium text-text-secondary hover:bg-background-secondary-hover"
                  >
                    {open ? (
                      <RiFolderOpenLine className="size-5" />
                    ) : (
                      <RiFolder3Line className="size-5" />
                    )}
                    <span className="truncate">{repo.name}</span>
                  </button>
                  {open && (
                    <ul className="relative ml-4 border-l border-separator-border">
                      {repo.chats.map((chat) => (
                        <li key={chat.name} className="relative">
                          <span
                            aria-hidden
                            className="absolute top-0 -left-px h-4 w-3 rounded-bl-lg border-b border-l border-separator-border"
                          />
                          {chat.href ? (
                            <a
                              href={chat.href}
                              aria-current={selected === chat.name ? "page" : undefined}
                              className={cx(
                                "-ml-4 flex h-8 items-center gap-2 rounded-2lg pr-2 pl-9 text-body-medium text-text-secondary hover:bg-background-secondary-hover",
                                selected === chat.name && "bg-background-tertiary-default",
                              )}
                            >
                              <span className="truncate">{chat.name}</span>
                              <span className="ml-auto rounded-sm bg-background-tertiary-default px-1 text-caption-1-regular">
                                {chat.time}
                              </span>
                            </a>
                          ) : (
                            <button
                              type="button"
                              onClick={() => setNotice(chat.name)}
                              className="-ml-4 flex h-8 w-[calc(100%+16px)] cursor-pointer items-center gap-2 rounded-2lg pr-2 pl-9 text-left text-body-medium text-text-secondary hover:bg-background-secondary-hover"
                            >
                              <span className="truncate">{chat.name}</span>
                              <span className="ml-auto rounded-sm bg-background-tertiary-default px-1 text-caption-1-regular">
                                {chat.time}
                              </span>
                            </button>
                          )}
                        </li>
                      ))}
                    </ul>
                  )}
                </div>
              );
            })}
            {rows.length === 0 && (
              <p className="p-2 text-body-regular text-text-tertiary">No repositories found.</p>
            )}
          </div>
        )}
      </div>
      <div className="flex flex-col gap-2 pt-4">
        {!collapsed && <ThemeToggle />}
        <a
          href="/foundations"
          aria-label="Support"
          className="flex h-8 items-center gap-2 overflow-hidden px-2 text-body-medium text-text-secondary"
        >
          <RiCustomerServiceLine className="size-4 shrink-0" />
          {!collapsed && "Support"}
        </a>
        <button
          type="button"
          onClick={() => setSettings(true)}
          aria-label="Settings"
          className="flex h-8 cursor-pointer items-center gap-2 overflow-hidden px-2 text-body-medium text-text-secondary"
        >
          <RiSettings4Line className="size-4 shrink-0" />
          {!collapsed && "Settings"}
        </button>
        <div className="flex items-center gap-2 rounded-2xl bg-background-tertiary-default p-2">
          <Avatar initials="B" color="blue" size="sm" />
          {!collapsed && (
            <>
              <div className="min-w-0 flex-1">
                <p className="text-body-medium text-text-primary">Board team</p>
                <p className="text-body-regular text-text-secondary">Pro Plan</p>
              </div>
              <Button variant="secondary" size="small" onClick={() => setSettings(true)}>
                Upgrade
              </Button>
            </>
          )}
        </div>
      </div>
      <SettingsModal isOpen={settings} onClose={() => setSettings(false)} />
      {notice && (
        <div className="absolute inset-3 z-30 flex flex-col justify-center gap-3 rounded-3xl border border-border-button-default bg-background-primary-default p-5 shadow-dropdown">
          <p className="text-headline-medium text-text-primary">{notice}</p>
          <p className="text-body-regular text-text-secondary">
            This workspace is ready for a new conversation.
          </p>
          <Button
            onClick={() => {
              setNotice(null);
              onNew();
            }}
          >
            Start a new agent
          </Button>
          <Button variant="secondary" onClick={() => setNotice(null)}>
            Back to workspace
          </Button>
        </div>
      )}
    </aside>
  );
}

export interface AgentWorkspaceProps {
  kind: "chat" | "image";
  children: ReactNode;
  panel: ReactNode;
  input: string;
  onInputChange: (value: string) => void;
  onSubmit: (value: string) => void;
  onStop: () => void;
  onNew: () => void;
  busy?: boolean;
  contained?: boolean;
  className?: string;
}

export function AgentWorkspace({
  kind,
  children,
  panel,
  input,
  onInputChange,
  onSubmit,
  onStop,
  onNew,
  busy,
  contained,
  className,
}: AgentWorkspaceProps) {
  const [collapsed, setCollapsed] = useState(false);
  const [navOpen, setNavOpen] = useState(false);
  const [panelOpen, setPanelOpen] = useState(false);
  const [copied, setCopied] = useState(false);
  const reduced = useReducedMotion();
  const scrollRef = useRef<HTMLDivElement>(null);
  const selected = kind === "chat" ? "coding scenario" : "image generation";
  useEffect(() => {
    const el = scrollRef.current;
    if (!el) return;
    const observer = new ResizeObserver(() => {
      el.scrollTop = el.scrollHeight;
    });
    const content = el.firstElementChild;
    if (content) observer.observe(content);
    return () => observer.disconnect();
  }, []);
  useEffect(() => {
    const listener = (event: KeyboardEvent) => {
      if (event.key === "Escape") {
        setNavOpen(false);
        setPanelOpen(false);
      }
    };
    window.addEventListener("keydown", listener);
    return () => window.removeEventListener("keydown", listener);
  }, []);
  const copy = async () => {
    try {
      await navigator.clipboard.writeText(scrollRef.current?.innerText ?? "");
      setCopied(true);
    } catch {
      setCopied(false);
    }
  };
  return (
    <div
      className={cx(
        "relative flex w-full gap-3 overflow-hidden bg-background-full p-3",
        contained ? "h-full min-h-[640px]" : "h-dvh",
        className,
      )}
    >
      <AgentRepositorySidebar
        selected={selected}
        onNew={onNew}
        collapsed={collapsed}
        onCollapse={() => setCollapsed((value) => !value)}
        className="relative z-10 hidden lg:flex"
      />
      <div className="absolute inset-y-3 left-3 lg:hidden" inert={!navOpen}>
        <AgentRepositorySidebar
          selected={selected}
          onNew={() => {
            onNew();
            setNavOpen(false);
          }}
          collapsed={false}
          onClose={() => setNavOpen(false)}
        />
      </div>
      <motion.section
        animate={{ x: navOpen ? 272 : 0 }}
        transition={{ duration: reduced ? 0 : 0.4, ease: [0.22, 1, 0.36, 1] }}
        className="relative z-20 flex h-full min-w-0 flex-1 flex-col overflow-hidden rounded-3xl bg-background-secondary-default lg:!transform-none"
      >
        <header className="absolute inset-x-0 top-0 z-10 flex h-12 items-center justify-between gap-2 px-4 text-body-2-medium text-text-secondary">
          <div className="flex min-w-0 items-center gap-2">
            <IconButton
              icon={navOpen ? RiCloseLine : RiMenuLine}
              size="small"
              aria-label="Open navigation"
              onClick={() => setNavOpen((value) => !value)}
              className="lg:hidden"
            />
            <RiFolder3Line className="hidden size-4 shrink-0 text-text-tertiary sm:block" />
            <span className="hidden truncate text-text-tertiary sm:inline">
              vibl coding project
            </span>
            <RiArrowRightSLine className="hidden size-4 shrink-0 sm:block" />
            <span className="truncate">{selected}</span>
          </div>
          <div className="flex shrink-0 items-center gap-1">
            <IconButton
              icon={copied ? RiCheckLine : RiShareBoxLine}
              size="small"
              aria-label="Copy conversation"
              onClick={copy}
            />
            <Dropdown>
              <DropdownTrigger
                aria-label="Conversation actions"
                className="flex size-7 items-center justify-center rounded-md hover:bg-background-secondary-hover"
              >
                <RiMoreFill className="size-4" />
              </DropdownTrigger>
              <DropdownPopover aria-label="Conversation actions">
                <DropdownItem onSelect={copy}>
                  <RiFileCopyLine className="size-4" />
                  Copy conversation
                </DropdownItem>
                <DropdownItem onSelect={onNew}>
                  <RiAddLine className="size-4" />
                  New agent
                </DropdownItem>
              </DropdownPopover>
            </Dropdown>
            <IconButton
              icon={kind === "chat" ? RiCodeBoxLine : RiGalleryLine}
              size="small"
              aria-label="Open side panel"
              className="xl:hidden"
              onClick={() => setPanelOpen(true)}
            />
          </div>
        </header>
        <div ref={scrollRef} className="min-h-0 flex-1 overflow-y-auto">
          <div className="flex min-h-full flex-col justify-end gap-4 px-4 pt-16 pb-3">
            {children}
          </div>
        </div>
        <div className="flex shrink-0 flex-col gap-2.5 px-2 pb-2">
          <Composer
            value={input}
            onValueChange={onInputChange}
            onSubmit={onSubmit}
            onStop={onStop}
            busy={busy}
          />
          <StatusBar className="px-1" />
        </div>
        {navOpen && (
          <button
            type="button"
            onClick={() => setNavOpen(false)}
            aria-label="Close navigation overlay"
            className="absolute inset-0 z-20 lg:hidden"
          />
        )}
      </motion.section>
      <aside className="hidden h-full w-[410px] shrink-0 flex-col overflow-hidden pt-2 xl:flex">
        {panel}
      </aside>
      {panelOpen && (
        <div className="absolute inset-0 z-40 xl:hidden">
          <button
            type="button"
            aria-label="Close panel backdrop"
            className="absolute inset-0 bg-black/40"
            onClick={() => setPanelOpen(false)}
          />
          <div className="absolute inset-y-0 right-0 flex w-[min(410px,calc(100%_-_12px))] flex-col bg-background-full p-3 shadow-sidebar">
            <div className="flex h-10 shrink-0 items-center justify-between px-1">
              <span className="text-headline-medium text-text-primary">
                {kind === "chat" ? "Code" : "Gallery"}
              </span>
              <IconButton
                icon={RiCloseLine}
                aria-label="Close panel"
                onClick={() => setPanelOpen(false)}
              />
            </div>
            <div className="flex min-h-0 flex-1 flex-col overflow-hidden pt-2">{panel}</div>
          </div>
        </div>
      )}
    </div>
  );
}

export function AgentChangesPanel({
  enableBrowserPreview = false,
}: {
  /** Show the embedded dashboard in Browser. Defaults to the reference's empty preview panel. */
  enableBrowserPreview?: boolean;
} = {}) {
  const [tab, setTab] = useState("changes");
  const [fileOpen, setFileOpen] = useState(true);
  const [terminal, setTerminal] = useState(false);
  const code = `"use client";\n\nimport { ThemeToggle } from "@zeus/ui/application";\nimport { Button } from "@zeus/ui/base";\n\nexport function WorkspaceToolbar() {\n  return (\n    <header className="flex items-center gap-3">\n      <ThemeToggle />\n      <Button variant="primary">\n        Create project\n      </Button>\n    </header>\n  );\n}\n\n// Semantic tokens switch with the selected theme.\n// This is sample code for the interactive preview.`;
  return (
    <>
      <div className="mb-2.5 flex h-[30px] shrink-0 items-center justify-between gap-2">
        <PillTabList aria-label="Panel view">
          {[
            { id: "changes", label: "Changes", Icon: RiInfinityLine },
            { id: "browser", label: "Browser", Icon: RiGlobalLine },
          ].map(({ id, label, Icon }) => (
            <PillTab key={id} onSelect={() => setTab(id)} isSelected={tab === id} icon={Icon}>
              {label}
            </PillTab>
          ))}
        </PillTabList>
        <IconButton
          size="small"
          icon={RiTerminalLine}
          aria-label="Toggle terminal"
          aria-pressed={terminal}
          onClick={() => setTerminal((value) => !value)}
          className="size-[22px] rounded-md border-0 bg-transparent p-0.5 shadow-none [&_svg]:size-[18px]"
        />
      </div>
      {tab === "browser" ? (
        enableBrowserPreview ? (
          <iframe
            src="/templates/dashboard"
            title="Workspace browser preview"
            className="min-h-0 w-full flex-1 rounded-2lg border border-separator-border bg-background-full"
          />
        ) : (
          <div className="flex min-h-0 w-full flex-1 items-center justify-center rounded-2lg bg-background-secondary-default">
            <span className="text-body-medium text-text-tertiary">Browser preview</span>
          </div>
        )
      ) : (
        <div className="min-h-0 flex-1 overflow-auto rounded-2xl border border-border-component-detail-container p-1">
          <div className="flex items-center gap-2 px-1.5 py-1 text-body-2-medium text-text-secondary">
            <span>12 Uncommitted changes</span>
            <span className="text-status-lime-text">+156</span>
            <span className="text-status-rose-text">-23</span>
          </div>
          <button
            type="button"
            onClick={() => setFileOpen((value) => !value)}
            aria-expanded={fileOpen}
            className="flex w-full cursor-pointer items-center gap-1 rounded-2lg bg-background-secondary-default px-1.5 py-1 text-left text-body-regular text-text-primary"
          >
            <RiCodeBoxLine className="size-4 shrink-0 text-chart-4" />
            <span className="truncate">notpritam/components/theme-toggle.tsx</span>
            <span className="text-status-lime-text">+74</span>
            <span className="ml-auto rounded-sm bg-background-tertiary-default px-1 text-body-2-regular text-text-secondary">
              New
            </span>
          </button>
          {fileOpen && (
            <pre className="overflow-x-auto py-5 font-mono text-body-2-regular text-text-secondary">
              <code>
                {code.split("\n").map((line, index) => (
                  <span key={`line-${index + 1}`} className="flex min-h-6">
                    <span className="w-8 shrink-0 pr-3 text-right text-text-tertiary">
                      {index + 1}
                    </span>
                    <span
                      className={cx(
                        "pr-3 whitespace-pre-wrap break-all",
                        /^(import|export)/.test(line) && "text-chart-7-active",
                      )}
                    >
                      {line || " "}
                    </span>
                  </span>
                ))}
              </code>
            </pre>
          )}
        </div>
      )}
      {terminal && (
        <div className="mt-2 h-40 shrink-0 overflow-auto rounded-xl bg-background-secondary-default p-3 font-mono text-body-2-regular text-text-secondary">
          <p>Workspace terminal</p>
          <p className="pt-2 text-text-tertiary">The example workspace is ready.</p>
          <a href="/components/theme-toggle" className="mt-2 block text-accent-600 hover:underline">
            Open the theme toggle source
          </a>
        </div>
      )}
    </>
  );
}
1d:T445b,"use client";

import {
  RiAddLine,
  RiArrowDownLine,
  RiCheckLine,
  RiCloseLine,
  RiDownloadLine,
  RiGalleryLine,
  RiSearchLine,
  RiSparklingLine,
} from "@remixicon/react";
import { AnimatePresence, motion, useReducedMotion } from "motion/react";
import { type CSSProperties, useCallback, useEffect, useRef, useState } from "react";
import { Dialog, Heading, Modal, ModalOverlay } from "react-aria-components";
import { Button } from "../../base/buttons/button";
import { IconButton } from "../../base/buttons/icon-button";
import { InputBase } from "../../base/input/input";
import { PillTab, PillTabList } from "../../base/tabs/pill-tab";
import { cx } from "../../utils/cx";
import { AgentWorkspace } from "./agent-workspace";
import { ConversationFeedback } from "./ai-chat";
import { type GalleryImage, IMAGE_GALLERY } from "./image-gallery-data";

export interface ImageGenerationResult {
  src: string;
  alt?: string;
}
export interface AIImageGenerationTemplateProps {
  /** Return the URL of your generated image. The default is a timed, local example. */
  onGenerate?: (
    prompt: string,
    options: { signal: AbortSignal; style: string },
  ) => Promise<ImageGenerationResult>;
  gallery?: GalleryImage[];
  autoRun?: boolean;
  /** Enable selectable Styles thumbnails. The reference default is an empty “Style presets” panel. */
  enableStylePresets?: boolean;
  contained?: boolean;
  className?: string;
}
const PROMPT =
  "Create a vintage editorial illustration of Lionel Messi dribbling in Argentina's home kit against a blue background.";
const RESULT = {
  src: "/ai-chat/generated-footballer.jpg",
  alt: "Vintage illustration of a footballer in Argentina's home kit against a blue background",
};
const STYLES = [
  "Editorial",
  "Photography",
  "Illustration",
  "3D render",
  "Screenprint",
  "Cinematic",
];

function GenerationRipple() {
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const reduced = useReducedMotion();
  useEffect(() => {
    const canvas = canvasRef.current;
    const ctx = canvas?.getContext("2d");
    if (!canvas || !ctx) return;
    let frame = 0;
    const start = performance.now();
    const ratio = Math.min(devicePixelRatio || 1, 2);
    canvas.width = 200 * ratio;
    canvas.height = 250 * ratio;
    ctx.scale(ratio, ratio);
    const draw = (now: number) => {
      const time = reduced ? 1 : (now - start) / 1000;
      ctx.clearRect(0, 0, 200, 250);
      for (let index = 0; index < 24; index++) {
        const radius = ((index * 7 + time * 13) % 168) + 3;
        const alpha = Math.sin(Math.min(radius / 168, 1) * Math.PI) * 0.24;
        ctx.beginPath();
        ctx.ellipse(100, 125, radius * 0.74, radius, Math.sin(time * 0.6) * 0.12, 0, Math.PI * 2);
        ctx.strokeStyle = `rgba(122,123,136,${alpha})`;
        ctx.lineWidth = 0.7;
        ctx.stroke();
      }
      if (!reduced) frame = requestAnimationFrame(draw);
    };
    draw(start);
    return () => cancelAnimationFrame(frame);
  }, [reduced]);
  return <canvas ref={canvasRef} aria-hidden className="absolute inset-0 size-full" />;
}

function GenerationImage({
  result,
  busy,
  seconds,
  animateEntry,
  onOpen,
}: {
  result: ImageGenerationResult | null;
  busy: boolean;
  seconds: number | null;
  animateEntry: boolean;
  onOpen: () => void;
}) {
  const reduced = useReducedMotion();
  return (
    <motion.div
      initial={!animateEntry || reduced ? false : { height: 0, opacity: 0 }}
      animate={{ height: 250, opacity: 1 }}
      transition={{ duration: reduced ? 0 : 0.7, ease: [0.22, 1, 0.36, 1] }}
      className="relative w-[200px] overflow-hidden rounded-2xl bg-background-tertiary-default"
    >
      {busy && (
        <>
          <GenerationRipple />
          <div className="absolute inset-0 flex flex-col items-center justify-center gap-2">
            <span className="text-body-regular text-text-secondary">Generating image</span>
            {seconds !== null && (
              <AnimatePresence mode="popLayout">
                <motion.span
                  key={seconds}
                  initial={!animateEntry || reduced ? false : { y: 10, rotateX: -80, opacity: 0 }}
                  animate={{ y: 0, rotateX: 0, opacity: 1 }}
                  exit={{ y: -10, rotateX: 80, opacity: 0 }}
                  transition={{ duration: reduced ? 0 : 0.3 }}
                  className="text-title-3-medium text-text-primary tabular-nums"
                >
                  {seconds}s
                </motion.span>
              </AnimatePresence>
            )}
          </div>
        </>
      )}
      {result && (
        <motion.button
          type="button"
          aria-label="Open generated image"
          className="group absolute inset-0 size-full cursor-zoom-in overflow-hidden rounded-2xl"
          onClick={onOpen}
          initial={
            reduced ? false : ({ "--reveal": "0%", opacity: 0, filter: "blur(12px)" } as never)
          }
          animate={{ "--reveal": "132%", opacity: 1, filter: "blur(0px)" } as never}
          transition={{ duration: reduced ? 0 : 1.6, ease: [0.22, 1, 0.36, 1] }}
          style={
            {
              maskImage:
                "radial-gradient(circle at 50% 50%, #000 calc(var(--reveal) - 26%), transparent var(--reveal))",
            } as CSSProperties
          }
        >
          <img
            src={result.src}
            alt={result.alt ?? "Generated image"}
            className="size-full object-cover"
          />
          <span className="absolute right-2 bottom-2 rounded-full bg-background-full p-1.5 text-text-primary opacity-0 transition-opacity group-hover:opacity-100 group-focus-visible:opacity-100">
            <RiSearchLine className="size-4" />
          </span>
        </motion.button>
      )}
    </motion.div>
  );
}

export function AIImageGenerationTemplate({
  onGenerate,
  gallery = IMAGE_GALLERY,
  autoRun = true,
  enableStylePresets = false,
  contained,
  className,
}: AIImageGenerationTemplateProps = {}) {
  const [input, setInput] = useState("");
  const [prompt, setPrompt] = useState(autoRun && !onGenerate ? PROMPT : "");
  const [busy, setBusy] = useState(autoRun && !onGenerate);
  const [result, setResult] = useState<ImageGenerationResult | null>(null);
  const [seconds, setSeconds] = useState(4);
  const [run, setRun] = useState(0);
  const [tab, setTab] = useState("gallery");
  const [style, setStyle] = useState("Editorial");
  const [query, setQuery] = useState("");
  const [images, setImages] = useState<GalleryImage[]>(gallery);
  const [opened, setOpened] = useState<GalleryImage | null>(null);
  const [error, setError] = useState("");
  const controller = useRef<AbortController | null>(null);
  const generation = useRef(0);
  const reduced = useReducedMotion();
  const cancel = () => {
    generation.current += 1;
    controller.current?.abort();
    setBusy(false);
  };
  useEffect(() => () => controller.current?.abort(), []);
  const complete = useCallback((image: ImageGenerationResult, completedPrompt: string) => {
    const resolvedImage = { ...image, alt: image.alt ?? completedPrompt };
    setResult(resolvedImage);
    setBusy(false);
    setImages((old) => [resolvedImage, ...old.filter((item) => item.src !== image.src)]);
  }, []);
  useEffect(() => {
    if (!busy || onGenerate) return;
    let active = true;
    const start = performance.now();
    const interval = setInterval(() => {
      if (active && generation.current === run)
        setSeconds(Math.max(1, 4 - Math.floor((performance.now() - start) / 1000)));
    }, 200);
    const timeout = setTimeout(() => {
      if (active && generation.current === run) complete(RESULT, prompt);
    }, 4300);
    return () => {
      active = false;
      clearInterval(interval);
      clearTimeout(timeout);
    };
  }, [busy, onGenerate, run, prompt, complete]);
  const reset = () => {
    cancel();
    setPrompt("");
    setInput("");
    setResult(null);
    setError("");
    setOpened(null);
  };
  const generate = async (value: string) => {
    const next = value.trim();
    if (!next) return;
    cancel();
    const id = generation.current;
    const abort = new AbortController();
    controller.current = abort;
    setPrompt(next);
    setInput("");
    setResult(null);
    setError("");
    setSeconds(4);
    setRun(id);
    setBusy(true);
    if (onGenerate) {
      try {
        const image = await onGenerate(next, { signal: abort.signal, style });
        if (generation.current === id && !abort.signal.aborted) complete(image, next);
      } catch (cause) {
        if (generation.current !== id || abort.signal.aborted) return;
        setBusy(false);
        setError(cause instanceof Error ? cause.message : "Image generation failed. Try again.");
      }
    }
  };
  const visible = images.filter((item) => item.alt.toLowerCase().includes(query.toLowerCase()));
  const panel = (
    <>
      <div className="mb-2.5 flex h-[30px] shrink-0 items-center justify-between gap-2">
        <PillTabList aria-label="Panel view">
          {[
            { id: "gallery", label: "Gallery", Icon: RiGalleryLine },
            { id: "styles", label: "Styles", Icon: RiSparklingLine },
          ].map(({ id, label, Icon }) => (
            <PillTab
              key={id}
              onSelect={() => setTab(id)}
              isSelected={tab === id}
              icon={Icon}
              className={id === "gallery" ? "[&_svg]:size-[18px]" : undefined}
            >
              {label}
            </PillTab>
          ))}
        </PillTabList>
        <IconButton
          size="small"
          icon={RiAddLine}
          aria-label="Start a new image"
          onClick={reset}
          className="size-[22px] rounded-md border-0 bg-transparent p-0.5 shadow-none [&_svg]:size-[18px]"
        />
      </div>
      {tab === "styles" ? (
        enableStylePresets ? (
          <div className="min-h-0 grid grid-cols-2 gap-2 overflow-y-auto">
            {STYLES.map((name, index) => (
              <button
                type="button"
                key={name}
                onClick={() => setStyle(name)}
                aria-pressed={name === style}
                className={cx(
                  "group relative cursor-pointer overflow-hidden rounded-2xl border-2 text-left",
                  name === style ? "border-border-focus-ring" : "border-transparent",
                )}
              >
                <img
                  src={gallery[index]?.src ?? RESULT.src}
                  alt=""
                  className="aspect-[4/5] w-full object-cover transition-transform duration-300 group-hover:scale-105 motion-reduce:transform-none motion-reduce:transition-none"
                />
                <span className="absolute inset-x-2 bottom-2 flex items-center justify-between rounded-lg bg-background-full px-2 py-1 text-body-medium text-text-primary">
                  {name}
                  {name === style && <RiCheckLine className="size-4" />}
                </span>
              </button>
            ))}
          </div>
        ) : (
          <div className="flex min-h-0 w-full flex-1 items-center justify-center rounded-2lg bg-background-secondary-default">
            <span className="text-body-medium text-text-tertiary">Style presets</span>
          </div>
        )
      ) : (
        <>
          <div className="mb-2">
            <InputBase
              aria-label="Search gallery"
              value={query}
              onChange={(event) => setQuery(event.target.value)}
              placeholder="Search images"
              leadingIcon={RiSearchLine}
            />
          </div>
          <div className="min-h-0 flex-1 overflow-y-auto">
            <div className="columns-3 gap-2">
              {visible.map((item) => (
                <motion.button
                  layout={!reduced}
                  type="button"
                  key={item.src}
                  onClick={() => setOpened(item)}
                  aria-label={`Open ${item.alt}`}
                  className="group relative mb-2 block w-full cursor-zoom-in break-inside-avoid overflow-hidden rounded-2lg"
                >
                  <img
                    src={item.src}
                    alt={item.alt}
                    loading="lazy"
                    className="w-full transition-transform duration-300 group-hover:scale-105 motion-reduce:transform-none motion-reduce:transition-none"
                  />
                  <span className="absolute right-1.5 bottom-1.5 rounded-full bg-background-full p-1 text-text-primary opacity-0 transition-opacity group-hover:opacity-100 group-focus-visible:opacity-100">
                    <RiArrowDownLine className="size-3" />
                  </span>
                </motion.button>
              ))}
            </div>
            {visible.length === 0 && (
              <p className="p-4 text-body-regular text-text-secondary">
                No images match your search.
              </p>
            )}
          </div>
        </>
      )}
    </>
  );
  return (
    <>
      <AgentWorkspace
        kind="image"
        input={input}
        onInputChange={setInput}
        onSubmit={(value) => void generate(value)}
        onStop={cancel}
        onNew={reset}
        busy={busy}
        contained={contained}
        className={className}
        panel={panel}
      >
        {!prompt && (
          <div className="flex flex-1 flex-col justify-center py-20 text-center">
            <h1 className="text-title-2-medium text-text-primary">Bring your ideas to life.</h1>
            <p className="pt-2 text-body-regular text-text-secondary">
              Describe an image to get started.
            </p>
          </div>
        )}
        {prompt && (
          <motion.p
            key={prompt}
            initial={run === 0 || reduced ? false : { opacity: 0, y: 8 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: reduced ? 0 : 0.3 }}
            className="ml-auto max-w-md rounded-2xl bg-background-tertiary-default px-4 py-3 text-body-regular text-text-primary"
          >
            {prompt}
          </motion.p>
        )}
        {prompt && !onGenerate && (
          <p className="text-body-2-regular text-text-tertiary">
            Example image generation · No generation service connected
          </p>
        )}
        {(busy || result) && (
          <GenerationImage
            key={run}
            busy={busy}
            seconds={onGenerate ? null : seconds}
            animateEntry={run > 0}
            result={result}
            onOpen={() => result && setOpened({ src: result.src, alt: result.alt ?? prompt })}
          />
        )}
        {result && (
          <div className="flex items-center gap-3">
            <ConversationFeedback text={prompt} onRetry={() => void generate(prompt)} />
            <a
              href={result.src}
              download
              className="flex items-center gap-1 text-body-2-medium text-text-secondary hover:text-text-primary"
            >
              <RiDownloadLine className="size-4" />
              Download
            </a>
          </div>
        )}
        {error && (
          <p role="alert" className="text-body-regular text-status-rose-text">
            {error}
          </p>
        )}
        {prompt && !busy && !result && !error && (
          <p role="status" className="text-body-regular text-text-secondary">
            Generation stopped.
          </p>
        )}
        {prompt && !busy && !result && (
          <Button
            size="small"
            variant="secondary"
            className="self-start"
            onClick={() => void generate(prompt)}
          >
            Try again
          </Button>
        )}
      </AgentWorkspace>
      <ModalOverlay
        isOpen={!!opened}
        onOpenChange={(open) => {
          if (!open) setOpened(null);
        }}
        isDismissable
        className="fixed inset-0 z-50 flex items-center justify-center bg-background-modal-overlay p-4 backdrop-blur-lg"
      >
        <Modal className="max-h-[95dvh] max-w-4xl overflow-auto rounded-3xl border border-border-button-default bg-background-full p-3 shadow-dropdown">
          <Dialog className="outline-none">
            <div className="mb-3 flex items-center justify-between gap-4">
              <Heading slot="title" className="truncate text-body-medium text-text-primary">
                {opened?.alt}
              </Heading>
              <IconButton
                icon={RiCloseLine}
                size="small"
                aria-label="Close image"
                onClick={() => setOpened(null)}
              />
            </div>
            {opened && (
              <img
                src={opened.src}
                alt={opened.alt}
                className="max-h-[75dvh] w-full rounded-xl object-contain"
              />
            )}
            <div className="mt-3 flex justify-end">
              <a
                href={opened?.src}
                download
                className="flex items-center gap-1 rounded-full bg-button-primary px-3 py-2 text-body-medium text-button-primary-foreground"
              >
                <RiDownloadLine className="size-4" />
                Download image
              </a>
            </div>
          </Dialog>
        </Modal>
      </ModalOverlay>
    </>
  );
}
1e:T851,export interface GalleryImage {
  src: string;
  alt: string;
}

export const IMAGE_GALLERY: GalleryImage[] = [
  {
    src: "/ai-chat/gallery/green-ape.webp",
    alt: "Green ape, screenprint halftone",
  },
  {
    src: "/ai-chat/gallery/cloud-crown.webp",
    alt: "Cloud crown, editorial portrait",
  },
  {
    src: "/ai-chat/gallery/cat-with-beer.webp",
    alt: "Cat with a beer, bold linework",
  },
  {
    src: "/ai-chat/gallery/hanok-bookshop.webp",
    alt: "Hanok bookshop, pastel duotone",
  },
  {
    src: "/ai-chat/gallery/nairobi-vibes.webp",
    alt: "Nairobi Vibes, blackletter poster",
  },
  {
    src: "/ai-chat/gallery/ronin-red.webp",
    alt: "Ronin in red, cel-shaded",
  },
  {
    src: "/ai-chat/gallery/bloom-swirl.webp",
    alt: "Bloom swirl, impasto abstraction",
  },
  {
    src: "/ai-chat/gallery/hoopoes.webp",
    alt: "Hoopoes in olive branches, gouache",
  },
  {
    src: "/ai-chat/gallery/girls-and-blooms.webp",
    alt: "Girls and blooms, painterly crop",
  },
  {
    src: "/ai-chat/gallery/biker-rest.webp",
    alt: "Biker resting, watercolour manga",
  },
  {
    src: "/ai-chat/gallery/beach-kid.webp",
    alt: "Beach kid, 35mm flash",
  },
  {
    src: "/ai-chat/gallery/reader-pink.webp",
    alt: "Reader on pink, crayon texture",
  },
  {
    src: "/ai-chat/gallery/underwater-highway.webp",
    alt: "Underwater highway, flat vector",
  },
  {
    src: "/ai-chat/gallery/perfume-still-life.webp",
    alt: "Perfume still life, grainy neon",
  },
  {
    src: "/ai-chat/gallery/yellow-cabs.webp",
    alt: "Yellow cabs, palette-knife oil",
  },
  {
    src: "/ai-chat/gallery/goldfish-living-room.webp",
    alt: "Goldfish living room, surreal collage",
  },
  {
    src: "/ai-chat/gallery/linen-campaign.webp",
    alt: "Linen campaign, crimson backdrop",
  },
  {
    src: "/ai-chat/gallery/helmet-portraits.webp",
    alt: "Helmet portraits, risograph grid",
  },
  {
    src: "/ai-chat/gallery/racing-suit.webp",
    alt: "Racing suit editorial, metallic green",
  },
  {
    src: "/ai-chat/gallery/massive-box.webp",
    alt: "MASSIV3 packaging, studio mockup",
  },
];
3:["$","$L1b",null,{"item":{"$schema":"https://myzeusui.com/schema/registry-item.json","name":"ai-image-generation","title":"AI Image Generation","description":"An image workspace with animated generation, a gallery, styles and a cancellable generation adapter","category":"templates","family":"ai","kind":"template","designSystem":"zeus","foundationDependencies":["tokens"],"iconLibraries":["remix"],"tags":["ai","image","generation"],"status":"stable","tier":"free","isNew":true,"dependencies":["@internationalized/date","@remixicon/react","motion","react-aria-components","tailwind-merge"],"registryDependencies":["agent-log","agent-progress","agent-thinking","ai-chat","avatar","button","checkbox","chevrons","chip","composer","composer-loader","cx","date-picker","dropdown","file-upload","icon-button","input","pagination","radio","select","settings-modal","sidebar","switch","tabs","task-list","theme-toggle","tokens","tooltip","use-dismiss-on-outside-press"],"files":[{"path":"application/templates/agent-workspace.tsx","target":"components/application/templates/agent-workspace.tsx","type":"component","content":"$1c"},{"path":"application/templates/ai-image-generation.tsx","target":"components/application/templates/ai-image-generation.tsx","type":"component","content":"$1d"},{"path":"application/templates/image-gallery-data.ts","target":"components/application/templates/image-gallery-data.ts","type":"component","content":"$1e"},{"path":"hooks/use-hydrated-reduced-motion.ts","target":"hooks/use-hydrated-reduced-motion.ts","type":"hook","content":"\"use client\";\n\nimport { useReducedMotion } from \"motion/react\";\nimport { useSyncExternalStore } from \"react\";\n\nconst subscribe = () => () => {};\nconst clientSnapshot = () => true;\nconst serverSnapshot = () => false;\n\n/** Keep the first server/client render identical, then apply the user's motion preference. */\nexport function useHydratedReducedMotion(): boolean {\n  const reduced = useReducedMotion();\n  const hydrated = useSyncExternalStore(subscribe, clientSnapshot, serverSnapshot);\n  return hydrated && reduced === true;\n}\n"},{"path":"public/ai-chat/gallery/beach-kid.webp","target":"public/ai-chat/gallery/beach-kid.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/biker-rest.webp","target":"public/ai-chat/gallery/biker-rest.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/bloom-swirl.webp","target":"public/ai-chat/gallery/bloom-swirl.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/cat-with-beer.webp","target":"public/ai-chat/gallery/cat-with-beer.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/cloud-crown.webp","target":"public/ai-chat/gallery/cloud-crown.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/girls-and-blooms.webp","target":"public/ai-chat/gallery/girls-and-blooms.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/goldfish-living-room.webp","target":"public/ai-chat/gallery/goldfish-living-room.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/green-ape.webp","target":"public/ai-chat/gallery/green-ape.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/hanok-bookshop.webp","target":"public/ai-chat/gallery/hanok-bookshop.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/helmet-portraits.webp","target":"public/ai-chat/gallery/helmet-portraits.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/hoopoes.webp","target":"public/ai-chat/gallery/hoopoes.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/linen-campaign.webp","target":"public/ai-chat/gallery/linen-campaign.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/massive-box.webp","target":"public/ai-chat/gallery/massive-box.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/nairobi-vibes.webp","target":"public/ai-chat/gallery/nairobi-vibes.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/perfume-still-life.webp","target":"public/ai-chat/gallery/perfume-still-life.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/racing-suit.webp","target":"public/ai-chat/gallery/racing-suit.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/reader-pink.webp","target":"public/ai-chat/gallery/reader-pink.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/ronin-red.webp","target":"public/ai-chat/gallery/ronin-red.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/underwater-highway.webp","target":"public/ai-chat/gallery/underwater-highway.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/gallery/yellow-cabs.webp","target":"public/ai-chat/gallery/yellow-cabs.webp","type":"asset","encoding":"base64"},{"path":"public/ai-chat/generated-footballer.jpg","target":"public/ai-chat/generated-footballer.jpg","type":"asset","encoding":"base64"}],"hash":"4b530bbeacc4"},"demoSource":"\"use client\";\n\nimport { AIImageGenerationTemplate } from \"@/components/application/templates/ai-image-generation\";\n\nexport default function Demo() { return <div className=\"h-[900px] w-[1440px] max-w-none\"><AIImageGenerationTemplate contained /></div>; }","previous":{"name":"ai-chat","title":"AI Chat"},"next":{"name":"agent-progress","title":"Agent Progress"}}]
4:["$","script","script-0",{"src":"/_next/static/chunks/3akuivfsf40p7.js","async":true}]
5:["$","script","script-1",{"src":"/_next/static/chunks/3_1pfg1675we5.js","async":true}]
6:["$","script","script-2",{"src":"/_next/static/chunks/3b-j35phbz363.js","async":true}]
7:["$","script","script-3",{"src":"/_next/static/chunks/2v6dx3y3pgbg2.js","async":true}]
8:["$","script","script-4",{"src":"/_next/static/chunks/3znghnjm2oz_l.js","async":true}]
9:["$","script","script-5",{"src":"/_next/static/chunks/3458ylrm-tu9f.js","async":true}]
a:["$","script","script-6",{"src":"/_next/static/chunks/1h9znjrekhrnu.js","async":true}]
b:["$","script","script-7",{"src":"/_next/static/chunks/052khci2jbxel.js","async":true}]
c:["$","script","script-8",{"src":"/_next/static/chunks/3fuakxesa5sfw.js","async":true}]
d:["$","script","script-9",{"src":"/_next/static/chunks/34kutq8p1wnfe.js","async":true}]
e:["$","script","script-10",{"src":"/_next/static/chunks/19s02zkpk1224.js","async":true}]
f:["$","$L1f",null,{"children":["$","$20",null,{"name":"Next.MetadataOutlet","children":"$@21"}]}]
12:["$","$1","h",{"children":[null,["$","$L22",null,{"children":[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]}],["$","div",null,{"hidden":true,"children":["$","$L23",null,{"children":["$","$20",null,{"name":"Next.Metadata","children":[["$","title","0",{"children":"AI Image Generation — Zeus UI"}],["$","meta","1",{"name":"description","content":"An image workspace with animated generation, a gallery, styles and a cancellable generation adapter"}],["$","meta","2",{"name":"keywords","content":"ai,image,generation"}]]}]}]}],["$","meta",null,{"name":"next-size-adjust","content":""}]]}]
14:["$","$1","c",{"children":[null,["$","$L24",null,{"parallelRouterKey":"children","template":["$","$L25",null,{}]}]]}]
17:["$","$1","c",{"children":[null,["$","$L24",null,{"parallelRouterKey":"children","template":["$","$L25",null,{}]}]]}]
1a:true
21:null
11:300
11:C
19:0
13:"$undefined"
15:"$undefined"
18:"$undefined"
10:"$undefined"
