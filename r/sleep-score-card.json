{
  "$schema": "https://myzeusui.com/schema/registry-item.json",
  "name": "sleep-score-card",
  "title": "Sleep Score",
  "description": "Segmented sleep score ring for React + Tailwind CSS with hover-focused sub-scores and metric rows",
  "category": "charts",
  "tags": [
    "chart",
    "data",
    "visualization"
  ],
  "status": "stable",
  "tier": "free",
  "isNew": true,
  "dependencies": [
    "@remixicon/react",
    "motion",
    "react-aria-components",
    "tailwind-merge"
  ],
  "registryDependencies": [
    "chart-card",
    "chevrons",
    "chip",
    "cx",
    "segmented-control",
    "select",
    "tokens",
    "use-count-up",
    "use-dismiss-on-outside-press"
  ],
  "files": [
    {
      "path": "application/charts/pro-chart.tsx",
      "target": "components/application/charts/pro-chart.tsx",
      "type": "component",
      "content": "\"use client\";\n\nimport {\n  RiArrowDownSLine,\n  RiArrowLeftSLine,\n  RiArrowRightSLine,\n  RiCalendarLine,\n} from \"@remixicon/react\";\nimport { motion } from \"motion/react\";\nimport { type ReactNode, useEffect, useRef, useState } from \"react\";\nimport {\n  SegmentedControl,\n  SegmentedControlItem,\n} from \"../../base/segmented-control/segmented-control\";\nimport { Select, SelectItem } from \"../../base/select/select\";\nimport { useHydratedReducedMotion } from \"../../hooks/use-hydrated-reduced-motion\";\nimport { cx } from \"../../utils/cx\";\nimport { formatNumber } from \"./chart-card\";\n\nexport const PALETTE = [2, 6, 5, 3, 8, 7, 1, 4, 9];\nexport const ink = (index: number) => `var(--color-chart-${PALETTE[index % PALETTE.length]})`;\nexport const safeRatio = (value: number, max: number) =>\n  Number.isFinite(value) && max > 0 ? Math.min(1, Math.max(0, value / max)) : 0;\nexport const numeric = (value: unknown) =>\n  typeof value === \"number\" && Number.isFinite(value) ? value : 0;\nexport const sum = (values: number[]) => values.reduce((total, value) => total + numeric(value), 0);\nexport const average = (values: number[]) => (values.length ? sum(values) / values.length : 0);\nexport const shortNumber = (value: number) =>\n  Math.abs(value) >= 1000 ? `${+(value / 1000).toFixed(1)}K` : `${+value.toFixed(1)}`;\nexport const MONTHS = [\n  \"Jan\",\n  \"Feb\",\n  \"Mar\",\n  \"Apr\",\n  \"May\",\n  \"Jun\",\n  \"Jul\",\n  \"Aug\",\n  \"Sep\",\n  \"Oct\",\n  \"Nov\",\n  \"Dec\",\n];\nexport const DAYS = [\"Mon\", \"Tue\", \"Wed\", \"Thu\", \"Fri\", \"Sat\", \"Sun\"];\nexport const chartAxis = {\n  tickLine: false,\n  axisLine: false,\n  tick: { fontSize: 12, fill: \"var(--color-text-tertiary)\" },\n} as const;\nexport interface ChartRange {\n  id: string;\n  label: string;\n  delta?: number;\n  headline?: number;\n}\nexport interface ChartCommonProps {\n  title?: string;\n  headline?: number;\n  delta?: number;\n  range?: string;\n  defaultRange?: string;\n  onRangeChange?: (id: string) => void;\n  className?: string;\n  format?: (value: number) => string;\n}\nexport function useChartRange<T extends ChartRange>(\n  ranges: T[] | undefined,\n  defaultRange?: string,\n  onChange?: (id: string) => void,\n) {\n  const [selected, setSelected] = useState(defaultRange ?? ranges?.[0]?.id);\n  const current = ranges?.find((range) => range.id === selected) ?? ranges?.[0];\n  const select = (id: string) => {\n    setSelected(id);\n    onChange?.(id);\n  };\n  return { current, id: current?.id, select };\n}\nexport function PeriodSelect({\n  ranges,\n  selected,\n  onChange,\n  label = \"This year\",\n}: {\n  ranges?: ChartRange[];\n  selected?: string;\n  onChange: (id: string) => void;\n  label?: string;\n}) {\n  const content = (\n    <>\n      <RiCalendarLine className=\"size-4 shrink-0 text-text-secondary\" />\n      <span>{label}</span>\n    </>\n  );\n  if (!ranges?.length)\n    return (\n      <span className=\"flex h-8 shrink-0 items-center gap-1.5 rounded-2lg border border-border-button-default bg-background-primary-default px-2 text-body-medium text-text-primary shadow-xs\">\n        {content}\n      </span>\n    );\n  return (\n    <Select\n      aria-label=\"Change period\"\n      selectedKey={selected}\n      onSelectionChange={(key) => key != null && onChange(String(key))}\n      triggerClassName=\"h-8 pl-2 pr-1.5 py-1 text-body-medium\"\n      className=\"shrink-0\"\n      renderValue={<span className=\"flex items-center gap-1.5\">{content}</span>}\n      renderIndicator={(open) => (\n        <RiArrowDownSLine\n          className={cx(\n            \"size-4 shrink-0 text-text-secondary transition-transform duration-200 ease-out\",\n            open && \"rotate-180\",\n          )}\n        />\n      )}\n    >\n      <>\n        {ranges.map((range) => (\n          <SelectItem key={range.id} id={range.id}>\n            {range.label}\n          </SelectItem>\n        ))}\n      </>\n    </Select>\n  );\n}\nexport function PeriodTabs({\n  ranges,\n  selected,\n  onChange,\n  plain = false,\n}: {\n  ranges: ChartRange[];\n  selected?: string;\n  onChange: (id: string) => void;\n  plain?: boolean;\n}) {\n  return (\n    <SegmentedControl\n      aria-label=\"Chart period\"\n      variant={plain ? \"plain\" : \"solid\"}\n      className={cx(\"shrink-0 bg-transparent\", plain && \"[&>button]:py-0\")}\n      selectedKeys={selected ? [selected] : []}\n      onSelectionChange={(keys) => {\n        const id = [...keys][0];\n        if (id != null) onChange(String(id));\n      }}\n    >\n      {ranges.map((range) => (\n        <SegmentedControlItem id={range.id} key={range.id}>\n          {range.label}\n        </SegmentedControlItem>\n      ))}\n    </SegmentedControl>\n  );\n}\nexport function WeekSwitcher({\n  label,\n  onPrevious,\n  onNext,\n}: {\n  label: string;\n  onPrevious: () => void;\n  onNext: () => void;\n}) {\n  return (\n    <div className=\"flex h-8 shrink-0 items-center gap-1 rounded-2lg border border-border-button-default bg-background-primary-default px-1 shadow-xs\">\n      <button\n        type=\"button\"\n        aria-label=\"Previous period\"\n        onClick={onPrevious}\n        className=\"rounded-md text-text-secondary outline-none focus-visible:ring-2 focus-visible:ring-border-focus-ring\"\n      >\n        <RiArrowLeftSLine className=\"size-4\" />\n      </button>\n      <span className=\"min-w-20 text-center text-body-medium text-text-primary\">{label}</span>\n      <button\n        type=\"button\"\n        aria-label=\"Next period\"\n        onClick={onNext}\n        className=\"rounded-md text-text-secondary outline-none focus-visible:ring-2 focus-visible:ring-border-focus-ring\"\n      >\n        <RiArrowRightSLine className=\"size-4\" />\n      </button>\n    </div>\n  );\n}\nexport function AnimatedNumber({\n  value,\n  format = formatNumber,\n}: {\n  value: number;\n  format?: (value: number) => string;\n}) {\n  const reduced = useHydratedReducedMotion();\n  const [display, setDisplay] = useState(value);\n  const current = useRef(value);\n  useEffect(() => {\n    if (reduced) {\n      current.current = value;\n      setDisplay(value);\n      return;\n    }\n    const from = current.current,\n      start = performance.now();\n    let frame = 0;\n    const tick = (time: number) => {\n      const t = Math.min(1, (time - start) / 300);\n      current.current = from + (value - from) * (1 - (1 - t) ** 3);\n      setDisplay(current.current);\n      if (t < 1) frame = requestAnimationFrame(tick);\n    };\n    frame = requestAnimationFrame(tick);\n    return () => cancelAnimationFrame(frame);\n  }, [value, reduced]);\n  return <>{format(reduced ? value : display)}</>;\n}\nexport function ChartFrame({\n  title,\n  value,\n  delta,\n  format,\n  suffix,\n  action,\n  children,\n  className,\n  height,\n  footer,\n  heading,\n}: {\n  title: string;\n  value?: number;\n  delta?: number;\n  format?: (value: number) => string;\n  suffix?: ReactNode;\n  action?: ReactNode;\n  children: ReactNode;\n  className?: string;\n  height?: number;\n  footer?: ReactNode;\n  heading?: ReactNode;\n}) {\n  return (\n    <section\n      className={cx(\n        \"flex w-full min-w-0 flex-col gap-4 rounded-2xl bg-background-secondary-default px-4 pt-4 pb-3\",\n        className,\n      )}\n      style={height ? { height } : undefined}\n    >\n      <div className=\"flex w-full flex-wrap items-start justify-between gap-3\">\n        <div className=\"flex min-w-0 flex-1 flex-col gap-0.5\">\n          <p className=\"truncate text-body-medium text-text-secondary\">{title}</p>\n          <div className=\"flex items-center gap-2\">\n            <p className=\"whitespace-nowrap text-title-1-medium text-text-primary tabular-nums\">\n              {heading ?? (value != null && <AnimatedNumber value={value} format={format} />)}\n              {suffix && (\n                <span className=\"ml-1 text-caption-1-medium text-text-secondary\">{suffix}</span>\n              )}\n            </p>\n            {delta != null && (\n              <span\n                className={cx(\n                  \"rounded-md px-1.5 py-0.5 text-body-medium whitespace-nowrap\",\n                  delta >= 0\n                    ? \"bg-status-lime-background text-status-lime-text\"\n                    : \"bg-status-rose-background text-status-rose-text\",\n                )}\n              >\n                {delta > 0 ? \"+\" : \"\"}\n                {+(delta * 100).toFixed(1)}%\n              </span>\n            )}\n          </div>\n        </div>\n        {action}\n      </div>\n      {children}\n      {footer}\n    </section>\n  );\n}\nexport interface StatTile {\n  label: string;\n  value: ReactNode;\n  color?: string;\n}\nexport function StatTiles({\n  items,\n  active,\n  onHover,\n  columns = 3,\n}: {\n  items: StatTile[];\n  columns?: number;\n  active?: number | null;\n  onHover?: (index: number | null) => void;\n}) {\n  return (\n    <div className=\"-mx-2 -mb-1 flex flex-wrap gap-2\">\n      {items.map((item, index) => (\n        <div\n          key={item.label}\n          tabIndex={onHover ? 0 : undefined}\n          onMouseEnter={() => onHover?.(index)}\n          onMouseLeave={() => onHover?.(null)}\n          onFocus={() => onHover?.(index)}\n          onBlur={() => onHover?.(null)}\n          className=\"flex min-w-0 flex-auto basis-[calc(33.333%-8px)] flex-col gap-px rounded-2lg bg-background-inner-default px-2.5 py-2 outline-none transition-opacity duration-200 motion-reduce:transition-none focus-visible:ring-2 focus-visible:ring-border-focus-ring\"\n          style={{\n            opacity: active != null && active !== index ? 0.35 : 1,\n            flexBasis: `calc(${100 / columns}% - 8px)`,\n          }}\n        >\n          <span className=\"flex items-center gap-1.5 truncate text-body-regular text-text-secondary\">\n            {item.color && (\n              <span className=\"size-3 shrink-0 rounded-[4px]\" style={{ background: item.color }} />\n            )}\n            {item.label}\n          </span>\n          <span className=\"text-body-medium text-text-primary tabular-nums\">{item.value}</span>\n        </div>\n      ))}\n    </div>\n  );\n}\nexport function PulseDot({ cx: x, cy: y, stroke }: { cx?: number; cy?: number; stroke?: string }) {\n  const reduced = useHydratedReducedMotion();\n  if (x == null || y == null) return null;\n  return (\n    <g>\n      <motion.circle\n        key={reduced ? \"reduced\" : \"animated\"}\n        cx={x}\n        cy={y}\n        fill={stroke}\n        initial={false}\n        animate={reduced ? { r: 7, opacity: 0.2 } : { r: [5, 11, 5], opacity: [0.25, 0, 0.25] }}\n        transition={reduced ? { duration: 0 } : { duration: 1.6, repeat: Infinity }}\n      />\n      <circle\n        cx={x}\n        cy={y}\n        r={3.5}\n        fill={stroke}\n        stroke=\"var(--color-background-inner-default)\"\n        strokeWidth={2}\n      />\n    </g>\n  );\n}\n"
    },
    {
      "path": "application/charts/sleep-score-card.tsx",
      "target": "components/application/charts/sleep-score-card.tsx",
      "type": "component",
      "content": "\"use client\";\nimport { motion } from \"motion/react\";\nimport { useState } from \"react\";\nimport { useHydratedReducedMotion } from \"../../hooks/use-hydrated-reduced-motion\";\nimport { cx } from \"../../utils/cx\";\nimport { AnimatedNumber, ChartFrame, safeRatio, sum, WeekSwitcher } from \"./pro-chart\";\nexport interface SleepMetric {\n  label: string;\n  detail: string;\n  value: number;\n  max: number;\n  color?: string;\n}\nexport interface SleepWeek {\n  id: string;\n  label: string;\n  metrics: SleepMetric[];\n}\nexport interface SleepScoreCardProps {\n  title?: string;\n  metrics?: SleepMetric[];\n  weeks?: SleepWeek[];\n  defaultWeek?: string;\n  onWeekChange?: (id: string) => void;\n  className?: string;\n}\nexport const SLEEP_METRICS: SleepMetric[] = [\n  { label: \"Duration\", detail: \"7h 50m\", value: 49, max: 50, color: \"var(--color-chart-5)\" },\n  { label: \"Bedtime\", detail: \"20m earlier\", value: 29, max: 30, color: \"var(--color-chart-3)\" },\n  {\n    label: \"Interruptions\",\n    detail: \"5m wake up\",\n    value: 20,\n    max: 20,\n    color: \"var(--color-chart-6)\",\n  },\n];\nconst WEEKS: SleepWeek[] = [\n  {\n    id: \"previous\",\n    label: \"22 Jun - 28 Jun\",\n    metrics: SLEEP_METRICS.map((m, i) => ({ ...m, value: m.value - [5, 4, 2][i]! })),\n  },\n  { id: \"current\", label: \"29 Jun - 5 Jul\", metrics: SLEEP_METRICS },\n  {\n    id: \"next\",\n    label: \"6 Jul - 12 Jul\",\n    metrics: SLEEP_METRICS.map((m, i) => ({ ...m, value: m.value - [2, 1, 0][i]! })),\n  },\n];\nexport function SleepScoreCard({\n  title = \"Sleep score\",\n  metrics,\n  weeks = WEEKS,\n  defaultWeek = \"current\",\n  onWeekChange,\n  className,\n}: SleepScoreCardProps = {}) {\n  const [week, setWeek] = useState(\n      Math.max(\n        0,\n        weeks.findIndex((w) => w.id === defaultWeek),\n      ),\n    ),\n    [active, setActive] = useState<number | null>(null),\n    reduced = useHydratedReducedMotion(),\n    current = weeks[week],\n    items = metrics ?? current?.metrics ?? [],\n    score = sum(items.map((m) => m.value)),\n    maximum = sum(items.map((m) => m.max)),\n    change = (delta: number) => {\n      const next = (week + delta + weeks.length) % Math.max(1, weeks.length);\n      setWeek(next);\n      setActive(null);\n      if (weeks[next]) onWeekChange?.(weeks[next]!.id);\n    };\n  let offset = 0;\n  return (\n    <ChartFrame\n      title={title}\n      heading={\n        safeRatio(score, maximum) >= 0.9\n          ? \"Excellent\"\n          : safeRatio(score, maximum) >= 0.75\n            ? \"Good\"\n            : \"Fair\"\n      }\n      className={cx(\"rounded-[20px]\", className)}\n      height={330}\n      action={\n        <WeekSwitcher\n          label={current?.label ?? \"This week\"}\n          onPrevious={() => change(-1)}\n          onNext={() => change(1)}\n        />\n      }\n    >\n      <div className=\"relative -mt-2 h-[104px] shrink-0\">\n        <svg\n          viewBox=\"0 0 120 120\"\n          className=\"h-full w-full\"\n          role=\"img\"\n          aria-label={`Sleep score: ${score} out of ${maximum}`}\n        >\n          {items.map((m, i) => {\n            const length = safeRatio(m.max, maximum) * 100,\n              start = offset;\n            offset += length;\n            return (\n              <motion.circle\n                key={`${m.label}-${reduced ? \"reduced\" : \"animated\"}`}\n                cx={60}\n                cy={60}\n                r={51}\n                pathLength={100}\n                transform=\"rotate(-82 60 60)\"\n                fill=\"none\"\n                stroke={m.color ?? `var(--color-chart-${i + 3})`}\n                strokeWidth={17}\n                strokeLinecap=\"round\"\n                strokeDashoffset={-start}\n                initial={reduced ? false : { strokeDasharray: \"0 100\", opacity: 1 }}\n                animate={{\n                  strokeDasharray: `${Math.max(0, length - 6)} 100`,\n                  opacity: active != null && active !== i ? 0.2 : 1,\n                }}\n                transition={{ duration: reduced ? 0 : 0.55 }}\n                onMouseEnter={() => setActive(i)}\n                onMouseLeave={() => setActive(null)}\n              />\n            );\n          })}\n        </svg>\n        <span className=\"pointer-events-none absolute inset-0 flex items-center justify-center text-title-1-medium text-text-primary\">\n          <AnimatedNumber value={active == null ? score : (items[active]?.value ?? 0)} />\n        </span>\n      </div>\n      <div className=\"-mx-1.5 mt-auto overflow-hidden rounded-2lg bg-background-inner-default\">\n        {items.map((m, i) => (\n          <button\n            type=\"button\"\n            key={m.label}\n            onMouseEnter={() => setActive(i)}\n            onMouseLeave={() => setActive(null)}\n            onFocus={() => setActive(i)}\n            onBlur={() => setActive(null)}\n            className=\"flex w-full items-center gap-1.5 border-b border-separator-border px-2.5 py-2.5 text-body-regular last:border-0 outline-none focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-border-focus-ring\"\n          >\n            <span\n              className=\"size-3 rounded-[4px]\"\n              style={{ background: m.color ?? `var(--color-chart-${i + 3})` }}\n            />\n            <span className=\"flex-1 text-left text-text-secondary\">\n              {m.label}: {m.detail}\n            </span>\n            <span className=\"text-body-medium text-text-primary\">\n              {m.value}/{m.max}\n            </span>\n          </button>\n        ))}\n      </div>\n    </ChartFrame>\n  );\n}\n"
    },
    {
      "path": "hooks/use-hydrated-reduced-motion.ts",
      "target": "hooks/use-hydrated-reduced-motion.ts",
      "type": "hook",
      "content": "\"use client\";\n\nimport { useReducedMotion } from \"motion/react\";\nimport { useSyncExternalStore } from \"react\";\n\nconst subscribe = () => () => {};\nconst clientSnapshot = () => true;\nconst serverSnapshot = () => false;\n\n/** Keep the first server/client render identical, then apply the user's motion preference. */\nexport function useHydratedReducedMotion(): boolean {\n  const reduced = useReducedMotion();\n  const hydrated = useSyncExternalStore(subscribe, clientSnapshot, serverSnapshot);\n  return hydrated && reduced === true;\n}\n"
    }
  ],
  "hash": "54a7f62919e5"
}
